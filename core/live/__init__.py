"""Живые обновления для всей системы: намёк «перечитай вот это» после фиксации.

Устройство и доводы — `docs/12-realtime.md`, разбивка на шаги — `docs/12-realtime-plan.md`.
Имя пакета `live`, а не `realtime`: `core/realtime.py` занят шиной мессенджера,
и переезд задел бы три места ввоза (§4 документа).

Чем это НЕ является: третьим видом подписчика `core/events.py`. События идут
синхронно в той же транзакции; намёк рождается ПОСЛЕ фиксации и ничего
отменить не может. Слой читает то, что записалось, и не участвует в записи.
"""
