"""Каждая точка API закрыта осознанно, а не по забывчивости.

Прав около сотни, маршрутов полторы сотни, и раскладывали их руками. Пропуск в
одном маршруте открывает раздел всем — и заметить это чтением нельзя: глазами
проверяют тот файл, который открыли, а открывают обычно не тот.

Поэтому перебор: берём ВСЕ точки API и требуем от каждой либо охранника с
правом, либо строку в списке исключений — с объяснением, почему охранника там
нет. Новый роутер попадает под проверку сам, и добавить его молча не выйдет.
"""

import pytest
from fastapi.routing import APIRoute

from web.main import app
from web.public import routes as public_routes

# Точки без охранника с правом, и почему. Каждая строка — решение, а не
# недосмотр; список короткий намеренно. Пути без префикса `/api/v1`: он
# добавляется при подключении роутера.
#: Маршруты без охранника — и почему это верно у каждого.
#:
#: Список короче, чем был: `/permissions` указывал в пустоту (маршрут давно
#: `/roles/matrix`), а у `/modules/{key}` охранник появился прямо в роуте
#: (`require_perm("settings", "manage")`) — исключение при нём перестало
#: что-либо исключать. Нашли это проверки в конце файла, и они же не дадут
#: списку зарасти снова: пустая строка тут страшнее лишней, потому что
#: страхует БУДУЩИЙ путь с тем же адресом.
EXEMPT: dict[str, str] = {
    "/auth/register": "регистрация: человека в системе ещё нет",
    "/auth/login": "вход: прав ещё нет, проверяется пароль",
    "/auth/logout": "выход доступен любому, кто вошёл",
    "/auth/me": "свой профиль — не раздел системы",
    "/auth/me/password": "свой пароль",
    "/auth/me/avatar": "свой аватар",
    "/auth/heartbeat": "отметка присутствия",
    "/modules": "состав блоков нужен интерфейсу, чтобы знать, что рисовать",
    "/workspace": "название и валюта общие для всех сотрудников",
    "/people": "список сотрудников для выбора ответственного",
    "/dashboard": "сводка сужается по правам внутри, а не закрывается целиком",
    "/search": "поиск отдаёт только то, на что есть право; отбор внутри",
    "/telephony/webhook": "запрос от АТС: сессии нет, проверяется подпись",
    "/telegram/webhook": "обновление от телеграма: сессии нет, проверяется секрет заголовка",
    "/alerts/webhook": (
        "тревога от Alertmanager: сессии у него нет, закрыто границей сети — "
        "запрос с X-Forwarded-For (то есть прошедший через nginx) отвергается"
    ),
    "/alerts/ready": (
        "тот же Alertmanager спрашивает, готовы ли мы принимать тревоги; "
        "закрыто той же границей сети, что и приём выше"
    ),
    # Ключ с полным префиксом, в отличие от соседей: роутер заявок с сайта
    # подключается без внешнего префикса и несёт свой путь целиком.
    "/api/v1/public/leads": "заявка с сайта: сессии нет, проверяется ключ приёма",
    "/arcade/scores": "змейка на странице обслуживания",
    "/arcade/leaderboard": "таблица рекордов змейки на странице обслуживания",
    "/roles/matrix": "состав прав нужен интерфейсу, чтобы прятать недоступное",
    "/metrics": "метрики для Prometheus: сессии нет, закрыто границей сети (nginx: deny all)",
    "/system/storage": "место на диске видят все: именно сотрудники загружают файлы",
}

# Точки, меняющие состояние, но закрытые правом на просмотр — и почему это
# верно. Список отдельный от EXEMPT: охранник там есть, вопрос только в том,
# достаточно ли его. Каждая строка объяснена в самом роуте.
WRITE_BY_VIEW_OK: dict[str, str] = {
    "/mail/accounts/{account_id}/sync": (
        "забрать почту — это её чтение; ждать того, кто правит настройки, "
        "ради свежих писем незачем"
    ),
    "/mail/messages/{message_id}/read": (
        "отметка «прочитано» — побочное следствие чтения, а не правка письма"
    ),
    "/telegram/chats/{chat_row_id}/messages/{message_id}/fetch": (
        "забрать видео, которое сразу не тянули, — это ЧТЕНИЕ переписки, а не "
        "изменение её: клиенту ничего не уходит, данные не меняются, файл лишь "
        "переезжает к нам с серверов телеграма. Потребуй здесь право писать — и "
        "читающий сотрудник не смог бы посмотреть присланное ему видео"
    ),
    "/telegram/chats/{chat_row_id}/read": (
        "«я это увидел» — следствие чтения, ровно как у письма выше. Граница "
        "прочитанного личная, лежит в Redis и только растёт; переписка от "
        "отметки не меняется и клиенту ничего не уходит. Потребуй здесь право "
        "писать — и у читающего сотрудника счётчик непрочитанного не снимался "
        "бы никогда, то есть перестал бы значить что-либо"
    ),
    "/telegram/chats/{chat_row_id}/presence": (
        "отметка «я смотрю этот чат» — следствие чтения, а не изменение данных: "
        "она живёт секунды в Redis и истекает сама. Потребуй здесь право писать "
        "— и читающий сотрудник исчез бы из баннера «в чате не только вы», "
        "оставаясь в чате. То есть предупреждение о конфликте молчало бы ровно "
        "про того, кто конфликт и создаёт"
    ),
}


def api_routes() -> list[APIRoute]:
    """Все точки API, включая вложенные роутеры.

    Подключённые роутеры лежат в `app.routes` не разложенными по одной точке, а
    целыми объектами, и настоящие маршруты живут в `original_router.routes`.
    Плоский перебор нашёл бы две точки из полутора сотен и был бы зелёным,
    ничего не проверив, — поэтому первый тест ниже проверяет сам перебор.

    Публичные страницы (витрина доски, состояние заказа по QR) исключаются по
    списку самого публичного роутера: у них своя защита — токен и PIN.
    """
    public = {route.path for route in public_routes.router.routes}
    found: list[APIRoute] = []
    for included in app.routes:
        original = getattr(included, "original_router", None)
        if original is None:
            continue
        for route in original.routes:
            if isinstance(route, APIRoute) and route.path not in public:
                found.append(route)
    return found


def guards_of(route: APIRoute) -> list[tuple[str, str]]:
    """Права, которыми закрыт маршрут, включая зависимости всего роутера."""
    found: list[tuple[str, str]] = []

    def walk(dependant) -> None:
        permission = getattr(getattr(dependant, "call", None), "opencrm_permission", None)
        if permission is not None:
            found.append(permission)
        for child in dependant.dependencies:
            walk(child)

    walk(route.dependant)
    return found


def test_the_sweep_sees_the_whole_api():
    """Пустой перебор сделал бы все проверки ниже зелёными и бессмысленными."""
    assert len(api_routes()) > 100, "маршруты не собрались — проверка ничего не значит"


@pytest.mark.parametrize(
    "route", api_routes(), ids=lambda r: f"{sorted(r.methods)[0]} {r.path}"
)
def test_every_route_is_guarded_or_listed(route: APIRoute):
    if route.path in EXEMPT:
        return
    assert guards_of(route), (
        f"{sorted(route.methods)} {route.path} не закрыт правом. Либо повесь "
        "require_perm, либо внеси в EXEMPT с объяснением, почему охранника нет."
    )


@pytest.mark.parametrize(
    "route", api_routes(), ids=lambda r: f"{sorted(r.methods)[0]} {r.path}"
)
def test_writing_routes_are_not_guarded_by_view_only(route: APIRoute):
    """Изменять — не то же самое, что смотреть.

    Самая правдоподобная ошибка механической раскладки: на POST или DELETE
    повесили `view`, потому что он стоял выше по файлу. Раздел при этом
    выглядит закрытым, а меняет его всякий, кто может его открыть.
    """
    if route.path in EXEMPT:
        return
    if not (route.methods & {"POST", "PATCH", "PUT", "DELETE"}):
        return
    if route.path in WRITE_BY_VIEW_OK:
        return
    guards = guards_of(route)
    if not guards:
        return  # об этом скажет соседний тест, не дублируем отказ
    assert any(action != "view" for _area, action in guards), (
        f"{sorted(route.methods)} {route.path} закрыт только правом на просмотр: "
        f"{guards}. Изменение должно требовать своего действия."
    )


# --- сам список исключений ----------------------------------------------------


def test_v_spiske_isklyucheniy_net_ukazyvayushchikh_v_pustotu():
    """Исключение без маршрута — приглашение открыть раздел молча.

    Путь переименовали или убрали, а строка осталась. Сама по себе безвредна,
    но живёт вечно и читается как «здесь охранник не нужен». А если завтра тот
    же путь заведут заново — уже под другую задачу, — он окажется освобождён от
    проверки прав ЗАРАНЕЕ, и заметить это будет нечем: тест зелёный, строка
    старая, объяснение при ней убедительное.
    """
    puti = {route.path for route in api_routes()}
    propavshie = sorted(set(EXEMPT) - puti)
    assert propavshie == [], (
        "исключения указывают на несуществующие маршруты:\n  "
        + "\n  ".join(propavshie)
        + "\nУберите: пустая строка тут страхует будущий незакрытый путь."
    )


def test_v_spiske_isklyucheniy_net_uzhe_zakrytykh_marshrutov():
    """У каждого исключения обязана быть причина — прямо сейчас, а не когда-то.

    Проверка обратная обычной. Пока путь назван в `EXEMPT`, охранника у него не
    спрашивают вовсе. Значит запись, у которой охранник УЖЕ появился, ничего не
    исключает — а выглядит как обдуманное решение и мешает заметить, что список
    пора сокращать.

    Тот же разбор, что у `ALLOWED` в `tests/test_db_boundary.py`: там четыре
    записи из семи не исключали ничего, и все четыре пережили код, ради
    которого заводились. Список исключений гниёт быстрее прочего — его
    пополняют осознанно, а вычищать некому.
    """
    lishnie = sorted(
        route.path
        for route in api_routes()
        if route.path in EXEMPT and guards_of(route)
    )
    assert lishnie == [], (
        "у этих маршрутов охранник уже есть, а они всё ещё в списке исключений:\n  "
        + "\n  ".join(lishnie)
        + "\nУберите из EXEMPT — иначе он прикрывает пустоту."
    )


def test_spisok_pishushchikh_pod_prosmotrom_ne_protuh():
    """То же для второго списка: он разрешает менять по праву на просмотр.

    Запись, у которой маршрут исчез или сменил охранника на пишущего, — это
    разрешение, выданное неизвестно кому.
    """
    puti = {route.path for route in api_routes()}
    propavshie = sorted(set(WRITE_BY_VIEW_OK) - puti)
    assert propavshie == [], (
        "разрешение «менять по праву просмотра» выдано несуществующим "
        "маршрутам:\n  " + "\n  ".join(propavshie)
    )
