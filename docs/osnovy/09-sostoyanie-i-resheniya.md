# 09 — Состояние проекта и решения

> **Состояние на 02.09.2026.** Система развёрнута на боевом сервере с реальным
> доменом и HTTPS, обновляется оттуда сама и пережила там уже несколько
> разобранных аварий — разборы по датам в [08-razvyortyvanie.md](../ekspluatatsiya/08-razvyortyvanie.md).
> Этапы 0–6 закрыты и ушли вниз, в исторический раздел; текущее состояние
> держится таблицей блоков.
>
> Под кодом — **больше двух тысяч проверок в 121 файле** `tests/`: заявки и воронка, склад и
> себестоимость, деньги, бланки, права, телеграм, наблюдение за сервером,
> переносимость в MySQL, автообновление и конфигурация развёртывания. Набор
> гоняется против настоящей MySQL (`docker/docker-compose.tests.yml`), а зовёт
> его `deploy/updater.py` **до** подмены сайта: красные тесты означают, что
> обновление никуда не поедет.

## Чем это стало

План начинался вокруг витрины. Она считалась ключевым продуктовым риском
(«лицо проекта»), поэтому вертикальный срез «доска → ссылка → витрина» появился
максимально рано — этапы 0–5 ровно про это, и порядок себя оправдал.

Продукт с тех пор развернулся. OpenCRM — система для небольшого дела
([01-obzor.md](01-obzor.md)), и стержень у неё другой: **клиент и заявка**.
Несущих блоков всего два (`core=True` в `core/modules.py`), остальные
четырнадцать включаются и выключаются по отдельности — доски с витриной в их
числе. Выключенный блок обязан исчезать целиком: то, что витрина продолжала
отдавать работы по уже разосланным ссылкам после выключения `boards`,
разбиралось как дыра, а не как недоделку (SEC-09 в
[24-audit-bezopasnosti.md](../ekspluatatsiya/24-audit-bezopasnosti.md), коммит `47e82e5`).

Старое обоснование порядка этапов оставлено ниже без правок. Оно объясняет,
почему доски и витрина написаны раньше склада и денег, и переоткрывать этот
вопрос не нужно.

## Что готово

Восемнадцать блоков реестра `core/modules.py`. Правило реестра: **блок в нём
появляется, когда он написан**, — переключателя для обещания не бывает, поэтому
строк со статусом «в разработке» здесь нет ни одной.

Прочерк в правой колонке значит, что по блоку **нет открытых пунктов плана**, а
не что блок безупречен: находки приходят с живого сервера и закрываются на
месте.

| Блок | Что закрыто | Открыто |
|---|---|---|
| **Клиенты** (`clients`, несущий) | Карточка, теги, источник, единая лента (заметки, звонки, письма, встречи), файлы, адрес отправки со страной по коду номера, мягкое удаление с восстановлением по праву `clients.restore` | — |
| **Заявки** (`deals`, несущий) | Канбан с перетаскиванием, воронка из готовых наборов, состав заявки строками (товары со склада и свои траты одним полем, набор сканером), сумма и ожидаемая прибыль из строк, предоплата, срок, ответственный, история этапов, списания материалов, заказ из заявки и обратно, доски | — |
| **Свои юрлица** (`companies`) | Реквизиты, подписант, печать бланка от выбранной фирмы; инвариант «ровно одна основная» держится запросом | — |
| **Бланки** (`documents`) | Квитанция приёмки, акт работ, бланк заказа; сквозная нумерация внутри года, статусы с историей, печать на русском, английском и украинском, штрихкод и QR, публичная страница состояния по коду | — |
| **Напоминания** (`tasks`) | Срок, счётчик просрочки в меню, необязательная привязка к клиенту или заявке | — |
| **Шаблоны сообщений** (`templates`) | Подстановки имени клиента, названия заявки и ссылки на доску; незаполненные поля видно до отправки | — |
| **Доски работ** (`boards`) | Медиапайплайн, редактор, публичные ссылки с PIN и сроком, витрина на модульной сетке, кейсы, учёт просмотров | Секции внутри доски, темы витрины, общее публичное портфолио, реакции и комментарии клиента |
| **Склад** (`warehouse`) | Несколько складов, приход/расход/списание/возврат/корректировка, перемещения, остаток как сумма движений, артикул выдаётся сам, бронь под открытую заявку и «кто держит товар», списание при закрытии ровно один раз, себестоимость списаний по заявке, акт работ | Бронь не разрезана по складам: остаток по складам есть, «доступно» общее. Отложено решением владельца 02.09.2026, разбор — [17](../bloki/17-nakladnye.md) §17 п.7 |
| **Наклейки** (`labels`) | Несколько кодов на позицию, печать по размеру рулона, поиск сканером; штрихкоды рисует само приложение | — |
| **Заказы** (`orders`) | Заказ покупателя и поставщику, резерв, сборка сканом, отгрузка и приёмка, откат проводки | — |
| **Накладные** (`waybills`) | Отгрузка и приёмка отдельной бумагой: позиции, проведение (остаток падает), подтверждение приёмки, сторнирование черновиком; накладная по заказу одним нажатием. Правится только черновик | — |
| **Отчёты** (`reports`) | Воронка с конверсией, выручка по месяцам, источники клиентов; каждый выгружается в CSV (`/reports/funnel.csv`, `revenue.csv`, `sources.csv`); список клиентов — `/clients/export.csv` | — |
| **Почта** (`mail`) | IMAP/SMTP-ящик фирмы, привязка письма к клиенту по адресу, письма в общей ленте | — |
| **Телефония** (`telephony`) | Вебхук АТС с подписью и идемпотентностью, журнал звонков, звонок из карточки, задача «перезвонить» | — |
| **Телеграм** (`telegram`) | Приём и отправка (текст, файлы), привязка диалога к карточке, заведение и обновление карточки прямо из диалога (и из меню шапки — в третьей колонке они недостижимы на ноутбучном экране), заявка и задача из диалога, шаблоны в поле ввода, срок хранения переписки, живое состояние на Redis с полосой и запасным перечитыванием при мёртвой шине, права канала признаются интерфейсом, экран-мессенджер, настройки бота (токен, чат сводки, чат тревог, «отправить сводку сейчас»), приглашение с QR, отбор диалогов по метке источника, метка приглашения набирается в настройках, список диалогов дочитывается страницами, файл уходит с подписью и цитатой | Три пункта чек-листа [13-telegram-v-crm.md](../ustroystvo/13-telegram-v-crm.md): два ждут решения владельца, третий — Telegram Business |
| **Деньги** (`finance`) | Статьи прихода и расхода, планы на период, прибыль за отрезок, правила начисления, оплаты по бланку; операция неизменяема | — |
| **Глобус** (`globe`) | Клиенты, связи и гости витрин точками на планете; очертания вшиты, подробные докачиваются при интернете | Кластеры и тепловые карты, определение места по IP |
| **Наблюдение за сервером** (`monitoring`) | Стек под профилем compose (prometheus, alertmanager, blackbox, loki, promtail, grafana, node-exporter, экспортёры MySQL и Redis, свой сборщик по контейнерам), экран `/server`, тревоги в телеграм с кнопками «принято» и «заглушить на час» | Проверка снаружи, с чужой машины (см. ниже) |

### Помимо блоков

- **Блоки системы и наборы под тип дела.** Реестр — `core/modules.py`, состояние
  в базе — `core/services/modules_service.py`, экраны `SettingsModules.tsx` и
  `Setup.tsx` («один вопрос при первом входе: чем вы занимаетесь»). Устройство и
  правила — [11-bloki-i-svyaznost.md](11-bloki-i-svyaznost.md), сторожа — `tests/test_modules.py`,
  `test_module_presets.py`, `test_setup_wizard.py`.
- **Роли и права вместо пары root/manager.** Матрица строится **из реестра
  блоков** (`core/permissions.py`), роли живут в базе (`database/models/role.py`,
  миграция `c5e19a3d7b46`), конструктор — экран `SettingsRoles.tsx` и
  `web/api/routes/roles.py`. Право ограничивает сервер, а не экран. Сторожа —
  `tests/test_roles.py`, `test_route_guards.py`, `test_staff_roles.py`.
- **Заявки с сайта по ключу приёма.** Публичный приём — `web/public/leads.py`
  (ловушка для ботов, ограничитель по IP), настройки — экран
  `screens/LeadsSettings.tsx` на `/settings/leads`: ключ создаётся и показывается
  один раз, там же выбирают ответственного и берут готовую команду для того, кто
  пишет форму. Пустой ключ — это и есть выключатель. Карточка получает источник
  `site`, который лежит в общем справочнике источников и потому выбирается и
  руками: человек, посмотревший сайт и позвонивший, пришёл оттуда же, а форму не
  заполнял. Ручки — [04-api.md](04-api.md), сторожа — `tests/test_leads.py`.
- **Журнал действий.** `core/services/audit_service.py`, экран `Audit.tsx`,
  только на чтение и переписать его нельзя (`tests/test_audit.py`).
- **Утренняя сводка** в телеграм (`core/services/svodka_service.py`,
  `scripts/svodka.py`), и её **неприход — сама по себе тревога**: тишину нельзя
  отличить от «всё хорошо», пока о ней не сообщают вслух.
- **Ручной режим обслуживания** (`core/services/maintenance_mode.py`): посетители
  видят заглушку, root продолжает работать.
- **Змейка с таблицей лидеров** на странице обновления. Игра — в
  `docker/nginx/maintenance/maintenance.html`, счёт проверяется на достижимость
  сервером (`core/services/arcade_service.py`, потолок 20 партий с адреса в час),
  ручки — `GET /arcade/leaderboard` и `POST /arcade/scores`, сторожа —
  `tests/test_arcade.py`.

- **Руководство внутри системы.** Восемь разделов и тридцать статей,
  `web/frontend/crm/src/lib/rukovodstvo.ts`, экран `screens/Docs.tsx`. Прячет
  то, чего у читателя нет, — тем же `allowed`, что и меню. Сторож
  `tests/test_rukovodstvo_polnoe.py` требует, чтобы каждый блок `core/modules.py`
  был в нём назван: новый блок больше не появится в системе молча. Заодно
  ссылки «открыть экран» сверяются с маршрутами `App.tsx`.

## Что не закрыто

Открытых пунктов нет: список задач `ZADACHI.md` опустел и удалён 05.09.2026
(`CLAUDE.md` §4). Ниже — запись о том, что закрыто, и она остаётся.

- [x] **Сборка заказа из одного места.** Сделано 31.08–01.09.2026, шесть
      пунктов из семи: артикул выдаётся сам и стал обязательным
      (`c4f92a1e8d73`), адрес клиента со страной по коду номера
      (`d7a15c93b402`), строки заявки (`e2b64f1a7c85`), бронь под заявку и
      «кто держит товар», списание при закрытии ровно один раз, заказ из
      заявки и обратно с отметкой «собран». План и разбор решений —
      [19-sborka-zakaza.md](../bloki/19-sborka-zakaza.md).
      **Остался седьмой пункт — витрина**: `/site/availability` не существует,
      и делать его надо вместе с публичным API магазина (`ZADACHI.md` 2.2). Резерв там
      обязан считать `reserve_service`, а не свой запрос по заказам.
      Учёт брони ПО СКЛАДАМ отложен решением владельца 02.09.2026: наивный
      фильтр вычел бы ноль (склад заказу не проставляется) и отдал бы чужой
      товар. Взамен «доступно» подписано общим по всем складам — разбор в
      [17](../bloki/17-nakladnye.md) §17 п.7.
- ~~**Витрина: секции внутри доски, темы оформления, общее публичное
      портфолио, реакции и комментарии клиента.**~~ **Снято решением владельца
      02.09.2026: не нужно.** Пункты висели открытыми с первого ТЗ и ни разу не
      были попрошены повторно; стержень продукта другой — клиент и заявка, а
      доски с витриной обычный выключаемый блок. Пункт, висящий открытым год,
      это не план, а укор: по такому списку решают, чего ещё «надо», и решение
      принимается по несуществующему обязательству. Понадобится — заведём заново
      с настоящим поводом.
- [x] **Выгрузка клиентов в CSV.** Сделано 28.08.2026: `GET /clients/export.csv` тем же отбором, что у списка, и кнопка рядом с поиском. Ответственный подписан именем, а не номером; больше десяти тысяч строк — честный отказ, а не молчаливое обрезание.
- [x] **Телеграм: доведённое до ручки, но не до экрана.** Все четыре места
      закрыты, проверено чтением кода 02.09.2026: отбор диалогов по метке —
      полоса чипов со счётчиками (`Telegram.tsx:972`, ручка `/telegram/sources`);
      метка приглашения набирается в настройках и перерисовывает QR
      (`TelegramSettings.tsx:367`, `label` у ручки — умолчание, а не константа);
      список диалогов дочитывается страницами со слиянием по `id`, чтобы живое
      обновление первой страницы не задваивало дочитанное
      (`Telegram.tsx:349`); файл уходит с подписью и цитатой
      (`Telegram.tsx:919`, ручка принимает `caption` и `reply_to_id`).

## Решения, к которым не возвращаемся

Записаны, чтобы их не переоткрывали.

- **Двух баз не будет: база одна, MySQL.** Одноразовый переезд и слой
  совместимости с SQLite вырезаны целиком (`9622037`, `c2b3ef9`), скрипта
  `scripts/migrate_to_mysql.py` больше нет. Адрес базы, не начинающийся на
  `mysql`, останавливает старт с объяснением (`config/settings.py`). Причина не в
  чистоте: пока баз было две, диалектные различия находились на боевом сервере, а
  не в наборе тестов — `SUM()` отдавал `Decimal` и ронял остаток склада
  пятисоткой, откат миграций падал на 14 шагах из 32. Теперь набор гоняется на
  той же СУБД, что и продукт.
- **Прогона тестов внутри сборки образа больше нет.** Набор требует живой MySQL,
  а во время `docker build` сети между контейнерами не существует вовсе. Этап
  `tests` в `docker/Dockerfile` только собирает окружение; запускают его снаружи
  через `docker/docker-compose.tests.yml`. Оставить `pytest` внутри значило бы
  красную сборку на каждом деплое.
- **Остаток склада не хранится** — он равен сумме движений и считается запросом.
  Хранимое число однажды разойдётся с историей, и узнать, какое из двух верное,
  будет неоткуда.
- **Справочник API не перечисляет все коды отказов, и это выбор глубины.** В
  службах их 262, названы около сотни. Правило одно: **у ручки, которую зовёт
  внешний** (приём заявок, три вебхука, тревоги, API сайта), каждый отказ назван
  статусом и кодом — вызывающий не должен узнавать о нём в бою. Остальные
  (`client_not_found`, `file_too_large`) самоочевидны, и переписывать их скопом
  значило бы удвоить документ ради того, что читается из имени. Сторожа на этот
  класс нет намеренно: список исключений вышел бы длиннее самой проверки.
  Закрыто 03.09.2026.
- **Инварианты «ровно один» держатся запросами, а не частичными индексами** — в
  MySQL их не существует.
- **Отмены проведения заказа нет и не будет** (владелец, 05.09.2026).
  `POST /orders/{id}/revert` удалён: проведённый заказ — свершившееся, и
  «отменить» его значило бы переписать историю. Назад — только возвратом
  покупателя со своей бумагой, суммой, описанием и статистикой
  ([22](../bloki/22-vozvraty.md)). Возврат поставщику отдельной бумагой не делали —
  разбор там же, §9.

---

# История: этапы 0–6

Ниже — запись о том, что и в каком порядке делалось, вместе с доводами. Читать
её надо как историю, а не как описание сегодняшнего дня.

Одна оговорка на весь раздел: там, где написано «(root)», сегодня стоит право из
матрицы. Пары ролей `root`/`manager` в коде нет — `require_root` не встречается
ни в одном маршруте, всё идёт через `require_perm`. Особым root остался только в
одном: у него есть все права, и отобрать их нельзя, иначе раздавать доступ стало
бы некому.

## Этап 0 — Каркас проекта ✅

- [x] Структура каталогов по [02-arhitektura.md](02-arhitektura.md), `pyproject.toml`.
- [x] FastAPI-приложение, конфиг из env (Pydantic Settings).
- [x] SQLAlchemy + Alembic, начальная миграция, фабрика сессий.
- [x] Bootstrap root-аккаунта при первом запуске (+ чистка истёкших сессий на старте).
- [x] Vite + React + TypeScript; дизайн-токены в `styles.css` (CSS custom properties вместо Tailwind — см. [02-arhitektura.md](02-arhitektura.md)).
- [x] Dev-запуск, pytest.

## Этап 1 — Аутентификация и сотрудники ✅

- [x] Вход/выход, серверные сессии, CSRF (double-submit), rate limit на вход.
- [x] Регистрация менеджера → `pending` → экран ожидания.
- [x] Раздел «Сотрудники» (root): заявки, одобрение/отклонение, деактивация (с мгновенным отзывом сессий), сброс пароля.
- [x] Принудительная смена пароля (root при первом входе, после сброса) — отдельный экран в SPA.
- [x] Профиль: имя, смена пароля, переключение языка (en по умолчанию, выбор — в БД).
- [x] i18n en/ru: SPA и серверные страницы витрины.

## Этап 2 — CRM: клиенты ✅

- [x] CRUD клиентов, список с живым поиском (имя/компания/телефон/email/теги).
- [x] Карточка клиента: inline-редактирование контактов, вкладки История/Файлы/Доски.
- [x] Лента истории: заметки/звонки/встречи/письма, быстрое добавление (Enter).
- [x] Файлы клиента: drag-and-drop загрузка (magic-bytes валидация), скачивание через сессию, удаление.
- [x] Мягкое удаление + восстановление root'ом.

## Этап 3 — Доски и медиапайплайн ✅

- [x] CRUD досок; привязка к клиенту; черновик/публикация.
- [x] Загрузка работ: `processing` → фоновая генерация large/card/thumb WebP + blurhash.
- [x] Видео: mp4/webm, постер и длительность через ffmpeg (без ffmpeg — деградация без постера).
- [x] Редактор доски: drag-and-drop порядок, обложка звёздочкой, подписи работ, мультизагрузка с поллингом статуса.
- [x] Удаление работы с очисткой файлов.

## Этап 4 — Публичные ссылки и витрина ✅

- [x] Ссылки: токен 128 бит, отзыв, перегенерация, срок действия, PIN (bcrypt + rate limit + подписанная cookie).
- [x] Блок «Поделиться» в редакторе: копирование, переключатель, срок, PIN, перегенерация, «Посмотреть как клиент».
- [x] Витрина `/b/{token}`: SSR + OG-теги, blurhash-плейсхолдеры (клиентский декодер), ленивые изображения.
      Раскладка на тот момент — masonry с балансировкой колонок на три брейкпоинта; заменена модульной
      сеткой в этапе 5, см. ниже.
- [x] Лайтбокс по макету: счётчик и закрытие сверху, круглые стрелки, подпись под работой, клавиатура/свайпы, видео.
- [x] Анимации по [06-dizayn-vitriny.md](../dizayn/06-dizayn-vitriny.md) + `prefers-reduced-motion`.
- [x] Служебные экраны по макетам: PIN из ячеек с автоотправкой и shake-ошибкой, «доступ закрыт» с кнопками контактов, пустая доска.
- [x] Учёт просмотров (хэш IP) + статистика в CRM.

## Этап 5 — Настройки сайта и полировка ✅

- [x] Настройки root: бренд, слоган, логотип, акцентный цвет, контакты, соцсети, язык витрины, дефолтная OG-картинка.
- [x] Дашборд CRM: приветствие по имени и времени суток, метрики, график просмотров за 7 дней,
      недавние доски/клиенты (эндпоинт `/api/v1/dashboard`). С появлением заявок перестроен:
      деньги (в работе, закрыто за месяц, средний чек) → воронка по этапам → мои задачи на сегодня →
      просмотры витрин → хранилище → недавние доски и клиенты. Просмотры витрин — метрика портфолио,
      а не бизнеса, и стоят ниже денег; порядок описан в [05-dizayn-crm.md](../dizayn/05-dizayn-crm.md).
- [x] Пустые состояния, подтверждения опасных действий, тосты.
- [x] Сквозной прогон дизайна по макетам `design/*.dc.html` (CRM и витрина).
- [x] Командная палитра `Ctrl+K`: клиенты, доски, заявки и быстрые действия, навигация клавиатурой
      (эндпоинт `/api/v1/search`). Заявки сужаются теми же правами, что и списки — `deals.view_others`
      и `deals.view_amounts`: поиск не должен становиться обходным путём к чужим суммам.
- [x] Регистронезависимый поиск для кириллицы (см. [03-baza-dannyh.md](03-baza-dannyh.md)).
- [x] Контроль свободного места: карточка «Хранилище» на дашборде, баннер в сайдбаре, раздел «Обслуживание» с очисткой корзины, блокировка загрузок при нехватке места. Работает без прав root (`statvfs`), Ubuntu и Fedora одинаково.
- [x] Удаление публичной ссылки (`DELETE /shares/{id}`) с явным разделением: ссылка ≠ файлы доски.
- [x] Аудит безопасности (эксплуатация + закрытие, см. [07-bezopasnost.md](../ekspluatatsiya/07-bezopasnost.md)): закрыт обход
      rate-limit подбора PIN подменой `X-Forwarded-For` (доверенный IP за прокси + `--no-proxy-headers`);
      смена пароля отзывает прочие сессии; добавлен Content-Security-Policy на витрину, CRM и медиа.
      Регрессии — `tests/test_security.py`.
- [x] Настройки: удаление логотипа и OG-картинки (сброс к дефолту, файл стирается с диска).
- [x] Удаление доски уносит файлы работ сразу (доски невосстановимы; каскад БД + чистка диска),
      корзина осталась только для клиентов. Менеджер файлов (root, `/files`): все медиа досок с
      размером на диске, датой загрузки и последним просмотром, точечное удаление. Регрессии — `tests/test_files.py`.
- [x] Аватары пользователей (self-upload в профиле, обрезка в квадрат webp, SVG не принимается) +
      присутствие: онлайн/офлайн и «последний раз в сети» (`last_seen_at` на пользователе, heartbeat
      фронта, порог 150 c). Показ — профиль, сайдбар, список сотрудников. Регрессии — `tests/test_profile.py`.
- [x] Витрина: кейсы — у работы своя ссылка на проект клиента (`works.project_url`), при наведении
      фото затемняется и из-под него выезжает двухсоставная кнопка «название проекта → View case»
      (уход проигрывает анимацию назад). Курсор-кружок, шрифт Montserrat (self-hosted), кнопка
      «Return to the site» из настройки `studio_site_url`. См. [10-vitrina-keysy.md](../dizayn/10-vitrina-keysy.md),
      регрессии — `tests/test_project_links.py`.
- [x] Витрина: кнопка возврата по центру шапки с логотипом сайта (автоподбор иконки с сайта —
      с защитой от SSRF, — либо загрузка вручную); колонка расширена до 2100px, чтобы работы не
      жались в центре широкого экрана; видео в сетке играет само, без звука и зациклено.
      Регрессии — `tests/test_site_logo_fetch.py`, `tests/test_showcase_video.py`.
- [x] Витрина: masonry заменён **модульной сеткой** — готовые композиции на 1–7 работ с перекрытием
      плиток, одна работа во всю ширину, больше семи — несколько модулей (каждый второй зеркалится).
      Наведение поднимает работу над соседями. На телефоне — колонка без кадрирования. Работы теперь
      рендерятся один раз вместо трёх копий DOM на брейкпоинт. См. [06-dizayn-vitriny.md](../dizayn/06-dizayn-vitriny.md),
      регрессии — `tests/test_layout.py`.
- [x] Управление сотрудниками (root): смена роли `manager ↔ root` (администраторов может быть несколько,
      последний root и своя роль защищены) и окончательное удаление аккаунта (сессии — каскадом, авторство
      в клиентах/досках/заметках обнуляется, а не пропадает). Регрессии — `tests/test_staff_roles.py`.
      Пара ролей позже заменена конструктором доступов — см. «Помимо блоков».

## Этап 6 — Production ✅

- [x] `docker/Dockerfile` (multi-stage: node → python + ffmpeg) и `docker/docker-compose.yml`;
      `nginx/templates/` (медиа напрямую, блок оригиналов, лимиты, HTTP/HTTPS-варианты).
      Состав compose вырос до пятнадцати служб: приложение, MySQL, Redis, nginx, certbot плюс
      стек наблюдения под профилем `monitoring`. Состояние по-прежнему живёт в домашнем каталоге
      (`${OPENCRM_HOME:-${HOME}/opencrm}`), а не внутри контейнеров.
- [x] Бэкапы: `scripts/backup.sh` (ежедневные + еженедельные, ротация) и `scripts/restore.sh`.
- [x] Очистка мягко удалённого: `scripts/purge_deleted.py` (карантин 30 дней, `--dry-run`).
- [x] Переезд на MySQL состоялся, SQLite вырезан из кода целиком — подробности выше, в разделе
      «Решения, к которым не возвращаемся».
- [x] `/healthz` + healthcheck в compose.
- [x] Автообновление из GitHub: демон на хосте (`deploy/`, `scripts/autoupdate.py`) опрашивает ветку
      и передеплоивает сам. Тесты нового кода до подмены — их гоняет
      `docker/docker-compose.tests.yml` рядом со своей MySQL, запуск из `deploy/updater.py`;
      снимок базы до миграций, health-check живыми страницами, автоматический откат с возвратом
      базы, журнал обновлений и уведомления в Telegram. Упавший коммит не пробуется по кругу.
      CI на push/PR — `.github/workflows/tests.yml`. См. [08-razvyortyvanie.md](../ekspluatatsiya/08-razvyortyvanie.md),
      регрессии — `tests/test_autoupdate.py`.
- [x] `opencrm.sh` — установка одной командой и меню управления: сам ставит Docker, генерирует
      секреты, подставляет UID:GID, поднимает стек, проверяет A-запись перед выпуском сертификата
      и включает автообновление (systemd, а без него cron). Повторный запуск секреты не трогает.
      Проверено полным развёртыванием на Ubuntu 24.04 (26 проверок: сайт, вход, идемпотентность,
      команды, меню). Попутно закрыт `.dockerignore` — контекст сборки упал с ~230 МБ до 6 КБ,
      а `config/.env` перестал запекаться в слой образа.
- [x] Флаг `Secure` у cookie — по схеме `OPENCRM_BASE_URL`, а не по имени окружения. Раньше
      документированный сценарий «локальная сеть без домена» ломался вчистую: вход возвращал 200,
      браузер выбрасывал Secure-cookie, пришедшую по HTTP, и человека сразу выкидывало обратно.
      См. [07-bezopasnost.md](../ekspluatatsiya/07-bezopasnost.md), регрессии — `tests/test_config.py`.
- [x] Обвязка сервера в мастере установки: ufw (порт SSH ищется по подключению, слушающим
      сокетам и конфигу — на Ubuntu 24.04 ssh поднимается сокетом systemd, и `sshd_config`
      может врать), ежедневные копии таймером systemd (раньше скрипты бэкапа были, а запускать
      их было некому), предел на логи контейнеров, проверка памяти и места перед сборкой.
      См. [07-bezopasnost.md](../ekspluatatsiya/07-bezopasnost.md), [08-razvyortyvanie.md](../ekspluatatsiya/08-razvyortyvanie.md).
- [x] Страница обслуживания на время обновления: nginx отдаёт её вместо «502 Bad Gateway»,
      503 + `Retry-After`, самовозврат на сайт. Собственные ошибки приложения не подменяются,
      проверка Let's Encrypt продолжает работать. Змейка с таблицей лидеров из задачи сделана
      отдельно — см. «Помимо блоков».
- [x] Адрес приложения nginx перечитывает на каждом запросе (`resolver` + переменная в
      `proxy_pass`). Без этого пересозданный при обновлении контейнер получал другой адрес,
      а nginx до перезапуска стучался по старому: сайт лежал при живом приложении, и откат
      автообновления краснел ровно по той же причине. Регрессии — `tests/test_deploy_config.py`.
- [x] Развёртывание на боевом VPS: реальный домен, HTTPS (Let's Encrypt) — с начала августа
      2026. Сервер живёт, обновляется сам, и на нём уже разбирались настоящие аварии
      (`08-razvyortyvanie.md`: пять суток замороженного конфига nginx 7–12 августа, две причины,
      однажды остановившие обновление наглухо).
- [x] Аудит безопасности прогнан дважды и записан в
      [24-audit-bezopasnosti.md](../ekspluatatsiya/24-audit-bezopasnosti.md): шестнадцать находок SEC-01…SEC-16, все
      закрыты либо приняты осознанно. Второй прогон шёл не глазами, а перебором путей по
      живому стенду с настоящим сертификатом.
- [x] Репетиция восстановления из бэкапа: на стенде Ubuntu 24.04 заведён клиент через API,
      снята копия, данные испорчены, восстановление сделано через меню — лишняя запись исчезла,
      запись из копии на месте, прежняя база отложена в `*.before-restore-*`, сайт поднялся сам.

---

## Definition of Done (для каждого этапа)

- Код соответствует границам слоёв из [02-arhitektura.md](02-arhitektura.md);
  запросы живут только в `database/` (`tests/test_db_boundary.py`).
- Правка моделей едет **в том же коммите**, что и миграция, а `downgrade`
  работает: откат — главный способ починки на боевом сервере.
- Сервисы покрыты unit-тестами, API — интеграционными (happy path + основные ошибки).
- Миграции накатываются на чистую БД и на БД предыдущего этапа; живая схема
  сходится с `Base.metadata` (`tests/test_schema_check.py`,
  `tests/test_schema_conventions.py`).
- UI проверен на en и ru, на desktop и мобильном.
- Документация обновлена, если этап изменил контракты.
