# 04 — API

## Общее

- Базовый префикс внутреннего API: `/api/v1`. Публичные страницы витрины — вне API (см. раздел «Публичные маршруты»).
- Формат — JSON; загрузка файлов — `multipart/form-data`.
- Аутентификация — сессионная cookie (`HttpOnly`, `Secure`, `SameSite=Lax`). SPA и API живут на одном домене, поэтому cookie проще и безопаснее JWT в localStorage.
- Ошибки — единый формат:

```json
{ "error": { "code": "board_not_found", "message": "Board not found" } }
```

- Пагинация — у длинных списков: `?page=1&per_page=50`, ответ содержит `items`, `total`,
  `page`, `per_page` (`schemas.paginated`). Справочники и короткие наборы страниц не имеют
  вовсе и отвечают одним `items`: сотрудники, роли, коллеги, шаблоны, напоминания, фирмы,
  этапы воронки, блоки, склады, файлы клиента, лента переписки. Страница у списка из шести
  строк обязывает клиента её листать и ничего взамен не даёт.
- Все mutation-запросы защищены CSRF-токеном (double-submit cookie).
- Таблицы раздела «API сайта магазина» показываются и внутри системы — статья
  «Ручки API сайта» на экране «Документация» (только ручки под ключом и снимок
  товара: внутренний API зовут экраны, внешнему он не нужен). Их порождает
  `scripts/spravochnik_api.py`; после правки таблиц скрипт надо перезапустить,
  иначе `tests/test_spravochnik_api.py` красный.

## Права

| Обозначение | Кто |
|---|---|
| 🔓 | без аутентификации |
| 👤 | любой активный сотрудник |
| 🔑 | нужно право «раздел.действие» из матрицы доступов |

Права раздаются ролями, роли создаёт пользователь — разбор в
[07-bezopasnost.md](../ekspluatatsiya/07-bezopasnost.md), реестр в `core/permissions.py`. Отказ —
`403 permission_denied` с именем недостающего права в сообщении. Порядок
проверок: **блок включён → есть право**, поэтому у выключенного блока ответ
`403 module_disabled`, даже если права нет тоже.

**Значка «только root» в таблицах нет ни у одного маршрута.** Зависимости
`require_root` в `web/api/deps.py` больше не существует: «нужен root» перестало
быть ответом на вопрос «кому можно» — должность «гендиректор» заводит ящики и
переключает блоки, не будучи владельцем системы. Останься эта зависимость, она
стала бы удобным способом закрыть очередной маршрут мимо матрицы, и матрица
начала бы врать о том, что настраивает. Сам root никуда не делся:
`permissions_service.has` отдаёт ему все права всегда, поэтому 🔑 он проходит
везде.

## Аутентификация и аккаунт

| Метод | Путь | Права | Описание |
|---|---|---|---|
| POST | `/auth/register` | 🔓 | Заявка на аккаунт менеджера: `name, email, password`. Ответ — «ожидайте одобрения» |
| POST | `/auth/login` | 🔓 | Вход. `403 account_pending` — не одобрен, `403 account_disabled` — деактивирован |
| POST | `/auth/logout` | 👤 | Выход |
| GET | `/auth/me` | 👤 | Текущий пользователь: имя, `role` (root или нет), `role_id`/`role_name` (должность), `permissions` — текущий набор прав, `locale`, `must_change_password`, `avatar_url`, `is_online`, `last_seen_at`. Права читаются на каждый запрос, а не запоминаются при входе |
| PATCH | `/auth/me` | 👤 | Смена имени, `locale` (en/ru — сохраняется в БД) |
| POST | `/auth/me/password` | 👤 | Смена пароля (старый + новый); сбрасывает `must_change_password` |
| GET | `/auth/heartbeat` | 👤 | Пинг присутствия: обновляет `last_seen` (фронт шлёт раз в ~45 c, пока вкладка активна) |
| POST | `/auth/me/avatar` | 👤 | Загрузить свой аватар (растр по сигнатуре, не SVG; обрезается в квадрат 256, webp) |
| DELETE | `/auth/me/avatar` | 👤 | Убрать аватар (файл стирается с диска) |

Присутствие: `last_seen_at` на пользователе обновляется на активность не чаще раза в минуту и
переживает logout (остаётся «последний раз в сети»); `is_online` = активность за последние 150 c.
Аватары отдаёт `GET /avatars/{uuid}.webp` (публичный путь, как `/branding`).

## Сотрудники

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/staff` | 🔑 `staff.view` | Список сотрудников с фильтром `?status=pending`; у каждого `role_id`, `role_name` и `deals_open` — открытые заявки, где он ответственный (одним запросом на штат) |
| POST | `/staff/{id}/approve` | 🔑 `staff.manage` | Одобрить заявку |
| POST | `/staff/{id}/reject` | 🔑 `staff.manage` | Отклонить заявку (запись удаляется) |
| POST | `/staff/{id}/disable` | 🔑 `staff.manage` | Деактивировать: сессии гаснут, войти нельзя |
| POST | `/staff/{id}/enable` | 🔑 `staff.manage` | Вернуть деактивированного в строй |
| POST | `/staff/{id}/reset-password` | 🔑 `staff.manage` | Выдать временный пароль с принудительной сменой |
| POST | `/staff/{id}/role` | 🔑 `roles.manage` | Сделать root'ом или вернуть обратно (`{"role": "root"\|"manager"}`) — это признак владельца системы, а не должность. Только активным; свою нельзя (`403 cannot_change_own_role`), последнего root не снять (`403 last_root`), `409 not_active` |
| DELETE | `/staff/{id}` | 🔑 `staff.manage` | Удалить аккаунт безвозвратно. Себя нельзя (`403 cannot_delete_self`), последнего root нельзя (`403 last_root`). Авторство сохраняется, но обнуляется |

## Роли и доступы

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/roles/matrix` | 👤 | Из чего собирается роль: строки по реестру блоков плюс системные области, столбцы-действия, пресеты. Открыт всем — интерфейс по нему решает, что показывать |
| GET | `/roles` | 🔑 `roles.view` | Роли с их правами и числом сотрудников |
| GET | `/roles/{id}` | 🔑 `roles.view` | Одна роль |
| POST | `/roles` | 🔑 `roles.manage` | Создать: `{name, permissions: ["deals.view", …]}`. Несуществующее право — `422 unknown_permission`, занятое имя — `409 role_name_taken` |
| POST | `/roles/from-preset` | 🔑 `roles.manage` | Создать из готового набора: `{preset, name?}`. `422 unknown_preset` |
| PATCH | `/roles/{id}` | 🔑 `roles.manage` | Переименовать и/или заменить набор прав. Снять `roles.manage` с последней такой роли нельзя — `403 last_roles_manager` |
| POST | `/roles/{id}/default` | 🔑 `roles.manage` | Какую роль получает новый сотрудник при регистрации |
| DELETE | `/roles/{id}` | 🔑 `roles.manage` | Удалить. Занятую нельзя (`409 role_in_use`), роль по умолчанию нельзя (`422 role_is_default`) |
| POST | `/roles/assign/{user_id}` | 🔑 `roles.manage` | Назначить должность (`{"role_id": 3}`; `null` — снять). Себе нельзя (`403 cannot_change_own_role`), root'у нельзя (`403 cannot_assign_role_to_root`), последнего управляющего правами не снять (`403 last_roles_manager`) |

## Коллеги и рабочее пространство

Две ручки, открытые любому сотруднику. Обе существуют потому, что соседние
разделы закрыты правом, а нужное с них — нет.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/people` | 👤 | Коллеги для выпадающего списка «ответственный»: `id`, имя, аватар активных сотрудников. Отдельно от `/staff`: назначать заявку на коллегу должен всякий, а знать при этом чужие адреса, статусы и даты одобрения — незачем |
| GET | `/workspace` | 👤 | `brand_name`, `currency`, `deal_term`. Полные настройки читает не каждый, а валюта и слово для заявки нужны на каждом экране: без них суммы показываются без обозначения, а разделы называются чужими словами |

`/workspace` завёлся вместо копирования этих трёх полей в ответы заявок, досок и
клиентов: копий набралось три, и они начали расходиться.

## Блоки системы

Реестр блоков — `core/modules.py`, в базе только «включён/выключен». На эти
ручки опираются все упоминания `module_disabled` в остальном документе.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/modules` | 👤 | Что включено, кем и когда. Права нет намеренно: интерфейсу надо знать, что рисовать в меню, — а это ровно каждый сотрудник |
| GET | `/modules/records` | 🔑 `settings.manage` | Сколько живых записей стоит за каждым блоком (`{items: {ключ: число}}`) — подпись под выключателем; отдельной ручкой, чтобы `GET /modules` не подорожал на четырнадцать счётов |
| GET | `/modules/presets` | 🔑 `settings.manage` | Наборы блоков под тип дела: состав, `will_enable` (чего ещё нет), воронка и слово для заявки. Состав считает сервер — список ключей во фронтенде разошёлся бы с реестром на первом же новом блоке |
| POST | `/modules/presets` | 🔑 `settings.manage` | Применить набор: `{key, apply_pipeline?, apply_deal_term?}`. Набор **только включает** — блоки вне него остаются как есть |
| POST | `/modules/{key}` | 🔑 `settings.manage` | Переключить один блок: `{"enabled": true\|false}` |

Забрать раздел с данными одним нажатием — самый быстрый способ потерять доверие,
поэтому пресет не выключает ничего. Воронка и слово для заявки ставятся вместе с
блоками: человек отвечал на вопрос «чем вы занимаетесь», а не «какие разделы
показать»; отменяется это двумя флагами — для того, кто вернулся к экрану уже с
настроенной воронкой.

## Поиск и дашборд

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/search?q=` | 👤 | Общий поиск для командной палитры (Ctrl+K). Три группы в порядке левой колонки — `clients`, `deals`, `boards`, — по первой странице (6 записей) в каждой. Пустой `q` — недавние записи |
| GET | `/search/{area}?q=&page=` | 👤 | Продолжение ОДНОЙ группы — «показать ещё» в палитре. `area` — ключ группы из общего поиска, неизвестный даёт 404. Отвечает той же формой, что группа: `items`, `total`, `has_more` |
| GET | `/dashboard` | 👤 | Сводка: деньги с начала месяца, воронка со счётчиками, мои задачи на сегодня, просмотры за 7 дней, недавние доски и клиенты |

Группа поиска **пустеет, но не исчезает**: выключенный блок или отсутствие
`<область>.view` дают пустой список под тем же ключом. Форма ответа одна при
любом наборе блоков и прав, и клиенту не приходится знать, какие ключи сегодня
бывают. Обе проверки обязательны: без первой доски находились бы поиском и вели
в раздел, которого нет ни в меню, ни в маршрутах; без второй поиск стал бы
обходом доступов — через него видны имена и названия из закрытого раздела.
Заявки дополнительно сужает `deals.view_others`, суммы прячет `deals.view_amounts`.

**Выдача листается по группам, а не обрывается на первой странице.** Шестёрка —
размер страницы, а не потолок: одно время она была потолком, и на «be» палитра
показывала ровно шесть досок, а седьмую нельзя было достать ничем. Номер
страницы приходит только в `/search/{area}`: общий поиск отдаёт первые страницы
всех групп разом, а один `page` на три группы означал бы вторую страницу у
досок, у которых нашлось двадцать, и её же у клиентов, у которых нашлось два.
Группы кончаются порознь, потому и листаются порознь. Продолжение закрыто теми
же проверками, что и первая страница (сначала блок, потом право), — иначе оно и
было бы обходом: закрытый раздел не отдаёт первую страницу, но отдаёт вторую.

**Точного числа найденного в поиске нет и не будет.** `total` — это длина
показанного, а «есть ли ещё» отвечает `has_more`. Точный счёт означал второй
полный проход по таблице на каждую группу и стоил ровно столько же, сколько сама
выборка: на 400 000 заявок 3128 мс против 3165 мс — секунды на число, которое
клиент выбрасывал не глядя. Полноту выдачи это не ограничивает: листается она по
`has_more`, а не по остатку от `total`. Точный `total` живёт на экранах разделов,
где из него считается номер последней страницы.

Ключи ответа `/dashboard`: `currency`, `money_in_work`, `money_won_this_month`,
`money_received_this_month`, `money_basis`, `avg_check`, `won_count_this_month`,
`deals_by_stage` (у этапа `count` и `amount` — сумма заявок этапа, пусто без `deals.view_amounts`), `my_tasks`,
`clients_total`, `clients_this_month`, `clients_this_week`, `clients_without_deals` (ни одной заявки), `boards_total`, `boards_published`,
`views_7d`, `views_prev_7d`, `unique_viewers_7d`, `views_by_day`, `last_view_at`,
`recent_boards`, `recent_clients`; с 06.09.2026 — `money_due` (цена открытых
заявок минус предоплата, только с правом на суммы), `orders_week` (с `overdue_count` — открытые заказы с прошедшим сроком)
(`shipped_count`, `returns_count`, `refund_amount` за 7 дней; `null` без блока
заказов), `recent_orders` (пять свежих: номер, вид, состояние, клиент, сумма),
`low_stock` и `low_stock_total` (товары с остатком не выше порога или нулём;
блок склада), `tasks_counters` (`overdue`, `today`, `mine`, `open` того, кто
смотрит; блок напоминаний), `calls_24h` (`vsego`, `propushcheno`; блок
телефонии). **При выключенном блоке ключи остаются, а значения пустеют** — то
же правило, что у поиска. Деньги считаются с начала
календарного месяца, а не за последние 30 дней: владелец сверяет их с месячной
отчётностью, и скользящее окно давало бы число, которое ни с чем не сходится.
`deals.view_others` сужает и числа тоже — спрятать чужие карточки и оставить их
сумму значит не запретить ничего, узнать оборот фирмы и было целью. Без
`deals.view_amounts` денежные плитки приходят пустыми, кроме `won_count`:
«сколько сделок я выиграл» — не сумма, и прятать его вместе с суммами значило бы
отнимать число, которое человек и так видит в своём списке.

`money_received_this_month` — сколько **пришло в кассу** с первого числа; по
решению «банк один» это и есть выручка, и именно здесь владелец видел пустой
месяц при полной кассе. Плитка стоит рядом с `money_won_this_month`, а не вместо
неё: это два разных вопроса («получили» и «продали на»), и расхождение между ними
— полезное число. Пусто и `money_basis == "deals"` — блок «Финансы» выключен,
полученных денег система не ведёт, и экран говорит это подписью.

## Хранилище и состояние системы

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/system/storage` | 👤 | Место на диске: занято/свободно, размер `storage/`, объём корзины, уровень тревоги, блокирована ли загрузка |
| POST | `/system/storage/purge` | 🔑 `settings.manage` | Окончательно удалить мягко удалённое: доски, работы, клиентов, заявки и файлы клиентов. Отвечает счётчиками по каждому виду и свежим состоянием хранилища |
| GET | `/system/files` | 🔑 `settings.manage` | Менеджер файлов: все работы досок — размер на диске, дата загрузки, дата последнего просмотра доски, превью |
| DELETE | `/system/files/{work_id}` | 🔑 `settings.manage` | Удалить одну работу вместе с файлами; отвечает свежим статусом хранилища |
| GET | `/system/schema` | 🔑 `settings.manage` | Сходится ли живая база с моделями — подробно, для разбора |
| GET | `/system/github` | 👤 | Звёзды проекта на GitHub из суточного кэша. `null` — «не спрашивали или не дозвонились»; ноль был бы утверждением, которого мы не делали |
| GET | `/system/monitoring` | 🔑 `monitoring.view` | Состояние стека наблюдения и путь к панели |
| GET | `/system/backups` | 🔑 `settings.manage` | Копии с экрана: есть ли ключ, последние работы, итог последней проверки. Разбор — [15-kopii-s-shifrovaniem.md](../ekspluatatsiya/15-kopii-s-shifrovaniem.md) §10 |
| POST | `/system/backups/key` | 🔑 `settings.manage` | Породить ключ копий; показывается один раз и до подтверждения не действует. `409 backup_key_exists` — ключ уже есть, менять через `{"replace": true}` |
| POST | `/system/backups/key/confirm` | 🔑 `settings.manage` | Подтвердить ключ последними восемью знаками. `404 backup_key_not_pending`, `422 backup_key_fragment_mismatch` |
| POST | `/system/backups/db` | 🔑 `settings.manage` | Снять копию базы в потоке; отвечает работой `{id, status}`. `409 backup_key_missing` — ключа нет, `409 backup_busy` — другая работа идёт |
| POST | `/system/backups/storage` | 🔑 `settings.manage` | То же для архива файлов (`storage`): фотографии, вложения, оформление. Отдельным файлом — решение владельца, docs/15 §0 |
| GET | `/system/backups/jobs/{id}` | 🔑 `settings.manage` | Состояние работы: `running / done / failed`, имя и размер файла, таблицы и строки, итог проверки ключом |
| GET | `/system/backups/jobs/{id}/file` | 🔑 `settings.manage` | Готовая копия файлом; пишется в журнал и ставит отметку увоза. `404 backup_not_ready`, `404 backup_gone` (копия старше суток убрана) |
| POST | `/system/backups/jobs/{id}/check` | 🔑 `settings.manage` | Ещё раз открыть копию нынешним ключом — так обнаруживается потерянный или заменённый ключ |
| DELETE | `/system/backups/jobs/{id}` | 🔑 `settings.manage` | Убрать копию с сервера раньше суток; идущую нельзя (`409 backup_busy`). Пишется в журнал `backup.deleted` |
| POST | `/system/backups/restore` | 🔑 `backups.manage` | Заменить базу (или дополнить файлы) из зашифрованной копии: multipart `kind=db|storage`, `file`. Отказы до того, как тронута база: `422 backup_not_encrypted`, `backup_bad_key`, `backup_truncated`, `backup_unknown_revision`; `409 backup_busy`. Дальше — работа, за которой следят по `/jobs/{id}` |

`purge` — **единственное место в системе, где данные исчезают безвозвратно**,
поэтому вызов пишется в журнал действий (`ACTION_STORAGE_PURGED`) с перечнем
того, что ушло. Раньше не писался вовсе: человек освобождал диск, а что при этом
удалилось, выяснить потом было не по чему. Клиента с выигранной заявкой purge не
трогает — это полученные деньги.

`/system/schema` отвечает тем же отчётом, который снят на старте приложения
(`web/main.py`): схема не меняется, пока процесс живёт. Коротко о том же говорит
`/healthz`, но там деталей нет намеренно — он открыт наружу. Это и есть способ
посмотреть состояние на живой машине, на который ссылается `CLAUDE.md`; разбор
самой гарантии — в [08-razvyortyvanie.md](../ekspluatatsiya/08-razvyortyvanie.md).

У мониторинга **своё право, а не `settings.manage`**: дежурного по серверу можно
пустить к панели, не отдавая ему логотип сайта и переключатели блоков. Заодно это
даёт правильный порядок «блок включён → есть право» даром — при выключенном
мониторинге сюда приходит `module_disabled`. Отдельная ручка, а не поле в
`/modules`: там отвечают на вопрос «нажат ли флажок в базе», и ответ обязан быть
мгновенным, его ждёт каждое меню, — а здесь ходят по сети к соседним контейнерам.

## Клиенты

Адрес для отправки: `country` (код ISO 3166-1 alpha-2), `city`, `zip_code`,
`address` — принимаются в `POST` и `PATCH`, отдаются в ответе. Не назвали
`country`, но назвали телефон — страна определяется по коду набора и
**едет за номером**, пока её не назвали руками. Общие коды (`+1`) страны не
дают: флаг наугад хуже отсутствующего. Отказы: `country_invalid` (не два
знака), `city_too_long`, `zip_too_long`, `address_too_long`.

Определяется она только по номеру, написанному **международно** (`+380…`,
`0038…`), либо по любому, если в настройках назван `default_country_code` —
тогда местный номер уже дописан этим кодом. Местный номер без кода страны не
даёт: `4951234567` при подборе по длине читается как `49` (Германия), и
магазину уехал бы чужой тариф доставки.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/clients` | 🔑 `clients.view` | Список: `?search=`, `?tag=`, `?manager_id=`, пагинация, сортировка по обновлению. В строках — `deals_open`, `deals_open_amount` (пусто без права на суммы), `deals_won`, `last_contact_at`: по два запроса на страницу, чужие заявки не в счёт |
| GET | `/clients/export.csv` | 🔑 `clients.view` | Тот же отбор файлом, целиком и без страниц. Больше 10 000 строк — отказ `export_too_large`, а не молчаливое обрезание. Право то же, что на просмотр: выгрузка отдаёт ровно то, что человек и так видит |
| POST | `/clients` | 🔑 `clients.create` | Создать карточку |
| GET | `/clients/{id}` | 🔑 `clients.view` | Карточка целиком: контакты, последние заметки, файлы, заявки, `svodka` — заявки по виду этапа (`open_count`/`open_amount`, `won_count`/`won_amount`, `lost_count`), `received_12m` (касса за год; блок денег и право на суммы), `last_contact` (последняя запись ленты), `last_call_at`, `papers` по видам, `manager_name`. Чужие заявки в счёт не идут, суммы пустеют без права |
| PATCH | `/clients/{id}` | 🔑 `clients.edit` | Обновить поля |
| DELETE | `/clients/{id}` | 🔑 `clients.delete` | Мягкое удаление |
| POST | `/clients/{id}/restore` | 🔑 `clients.restore` | Вернуть из корзины |
| GET | `/clients/{id}/notes` | 🔑 `clients.view` | Лента истории (пагинация). Фильтры `kind` и `deal_id` |
| POST | `/clients/{id}/notes` | 🔑 `clients.edit` | Добавить запись: `kind` (note/call/meeting/email), `body`, `happened_at`, `direction`, `deal_id` |
| DELETE | `/clients/{id}/notes/{note_id}` | 🔑 `clients.edit` | Удалить запись |
| GET | `/clients/{id}/files` | 🔑 `clients.view` | Список файлов (одним `items`, без страниц) |
| POST | `/clients/{id}/files` | 🔑 `clients.edit` | Загрузить файл (multipart) |
| GET | `/clients/{id}/files/{file_id}/download` | 🔑 `clients.view` | Скачать (через приложение, с проверкой сессии) |
| DELETE | `/clients/{id}/files/{file_id}` | 🔑 `clients.edit` | Удалить файл |

`restore` — отдельное действие, а не оборотная сторона `delete`: удалять рутина,
поднимать удалённое — разбор происшествия, и это разные полномочия. Запись ленты
и файл ходят под `clients.edit`, а не под своими правами: они и есть правка
карточки, а дробить «изменить телефон» и «дописать заметку» не на что.

## Заявки (блок `deals`, несущий)

Перевод заявки на выигрышный этап **списывает со склада** то, что она
обещала и что ещё не ушло: `в строках − уже списанное с этим deal_id`.
Повторное закрытие не списывает ничего. Проигранная не списывает вовсе.

Строки заявки: `GET/POST /deals/{id}/lines`, `PATCH/DELETE .../lines/{line_id}`.
Товар — по `product_id`, `sku` или `code` (скан); ничего из них — своя трата, и
тогда обязателен `name`. Количество в тысячных разбирает сервер (`quantity`:
`"1.5"`), цена — целая в минорных единицах. Ответ несёт `kind`
(`product`/`extra`) и `total_minor` — они считаются на отдаче, в базе их нет.
Итог заявки складывается сам и ложится в `amount`.

**`total_minor` итога равен сумме `total_minor` строк, а не точной сумме
произведений.** Цена в минорных, количество в тысячных: 3,33 за 1,5 метра — это
4,995, и строка отдаёт 4,99. Складывающий столбец обязан получить то же число,
что стоит под ним; на двадцати строках разница между двумя способами доходит до
двадцати копеек. Тем же правилом считается `cost_minor`. Рядом приходят `cost_minor`
и `profit_minor` — ожидаемая себестоимость по снимкам строк и прибыль; оба
`null`, если себестоимость известна не у всех строк: неполная завысила бы
прибыль там, где по ней решают о скидке. Суммы, себестоимость и прибыль
закрыты правом `deals.view_amounts`. Раздел закрыт блоком
`warehouse` целиком. Отказы: `deal_closed`, `quantity_not_positive`,
`name_required`, `name_too_long`, `name_is_snapshot`, `product_not_found`,
`product_deleted`, `line_not_found`, `line_has_no_warehouse` (склад прислан своей
трате или услуге — брать им неоткуда; исключение одно: при добавлении
СКАНОМ (`code`) склад у услуги молча отбрасывается, потому что звонящий не
мог знать, что за кодом), `deal_amount_too_big` (итог не влезает в
колонку: каждый сомножитель в своём пределе, а произведение уже нет),
`barcode_unknown` (скан не нашёл товар — с самим кодом внутри, потому что пустой
ответ после писка сканера читается как «сканер сломался»).

Бронь под заявку, списание при закрытии и заказ из заявки — сделаны, разбор
решений в [19](../bloki/19-sborka-zakaza.md) §Р3, §Р4, §6.3. Из непокрытого остаётся
витрина: `/site/availability` не существует, и резерв там обязан будет считать
`reserve_service`, а не свой запрос по заказам.

Заявка — стержень системы: на ней сходятся этапы, лента, бланки, склад и деньги.
Блок объявлен несущим в `core/modules.py` и выключить его нельзя — это рассыпало
бы систему на несвязанные списки, ради связывания которых всё и затевалось.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/deals/board` | 🔑 `deals.view` | Канбан: колонки по этапам воронки целиком (этап отдаётся не ключом, а записью — фронт рисует названия этой фирмы), `amount_total` над колонкой, `currency` |
| GET | `/deals` | 🔑 `deals.view` | Список: `search`, `stage`, `client_id`, `manager_id`, `include_closed`, пагинация |
| POST | `/deals` | 🔑 `deals.create` | Завести заявку |
| GET | `/deals/{id}` | 🔑 `deals.view` | Карточка: поля, история этапов, валюта, телефон клиента, доски заявки (если блок досок включён) |
| PATCH | `/deals/{id}` | 🔑 `deals.edit` | Изменить поля |
| POST | `/deals/{id}/move` | 🔑 `deals.move_stage` | Перетаскивание в канбане: `{stage, sort_order?, lost_reason?}` |
| DELETE | `/deals/{id}` | 🔑 `deals.delete` | Удалить |
| GET | `/deals/{id}/feed` | 🔑 `deals.view` | Лента заявки: звонки, письма, встречи и заметки одним потоком, фильтр `kind` |
| POST | `/deals/{id}/feed` | 🔑 `deals.edit` | Дописать в ленту (то же тело, что у заметки клиента) |
| GET | `/deals/{id}/lines` | 🔑 `deals.view` | Состав заявки: товары и свои траты, итог, себестоимость и ожидаемая прибыль. Блок `warehouse` |
| POST | `/deals/{id}/lines` | 🔑 `deals.edit` | Добавить строку: `product_id`, `sku` или `code` (скан); ничего из них — своя трата |
| PATCH | `/deals/{id}/lines/{line_id}` | 🔑 `deals.edit` | Количество, цена, склад; название — только у своей траты |
| DELETE | `/deals/{id}/lines/{line_id}` | 🔑 `deals.edit` | Убрать строку |
| POST | `/deals/{id}/order` | 🔑 `orders.create` | Завести заказ по заявке, перенеся её товары. Блок `orders` |

**Двигать заявку — отдельное право.** Вести заявку и решать, что она
«выполнена», — разные полномочия: на этапе `won` считается выручка.

**Один и тот же список у двух сотрудников разный, и это два сужения, а не
настройка интерфейса.**

- `deals.view_others` решает, ЧЬИ заявки видно. Без права человек видит только
  те, где он ответственный, — и это фильтр в запросах, а не признак на экране:
  спрятать чужие карточки в вёрстке значит оставить их доступными по адресу, в
  поиске, на сводке и в выгрузке отчёта. Прямой запрос чужой заявки отвечает
  `403 permission_denied` с именем недостающего права, а не «не найдено»: заявка
  существует, и сотрудник о ней обычно знает — коллега прислал ссылку. Молчаливое
  404 солгало бы дважды и не подсказало, что просить у того, кто раздаёт доступы.
- `deals.view_amounts` решает, видно ли деньги. Самая частая просьба и главная
  причина, по которой прав на раздел недостаточно: менеджер ведёт заявку, но не
  видит её маржу. Право закрывает суммы **и на запись тоже** — не видит сумм, не
  называет их ни при заведении, ни при правке. Иначе оно было бы наполовину
  декоративным: сумму не показали, но её можно перезаписать вслепую, а прежнее
  значение при этом теряется без следа. Итог над колонкой канбана прячется вместе
  с карточками — одно без другого это скрытая сумма, которую видно в шапке.

Ответ на `PATCH` и `POST /move` — **такая же карточка, как у `GET`**. Разная
форма уже ломала экран: фронт клал ответ `PATCH` в состояние, `stage_history`
оказывалась `undefined`, и после смены этапа карточка уходила в белое.

Отдельной ленты у заявки в базе нет — это записи ленты клиента с проставленным
`deal_id`. Отдельная точка, а не фильтр в ленте клиента, потому что спрашивать
события заявки через клиента значит каждый раз помнить, чей это клиент.

## Воронка

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/pipeline/stages` | 🔑 `deals.view` | Этапы по порядку. Читать должен всякий, кто видит заявки, — без этого не нарисовать доску |
| GET | `/pipeline/presets` | 🔑 `settings.manage` | Готовые воронки под отрасль: ключ, название, подсказка, состав |
| POST | `/pipeline/preset` | 🔑 `settings.manage` | Применить готовую воронку целиком |
| POST | `/pipeline/stages` | 🔑 `settings.manage` | Добавить этап: `{name, kind, after?}` |
| PATCH | `/pipeline/stages/{key}` | 🔑 `settings.manage` | Переименовать, сменить вид, цвет, порядок |
| DELETE | `/pipeline/stages/{key}` | 🔑 `settings.manage` | Убрать этап с доски |

Правка воронки — структура работы всей фирмы, и случайное переименование задевает
всех сразу; поэтому пять из шести ручек под `settings.manage`, а не под правами
на заявки.

Удаление этапа — **не удаление**: `pipeline_service.archive_stage` убирает его с
доски, а заявки из него переезжают на первый открытый. Этап, унёсший с собой
десяток заявок, был бы худшим из возможных ответов на нажатие «убрать колонку».
На «первый открытый этап» опирается и приём заявок с сайта (см. ниже).

**Три вида этапа обязаны остаться, и это проверяется на обоих путях.** Отказы:
`last_stage_of_kind` — воронка не может остаться без «выиграна» или «провалена»
(некуда закрыть заявку, а конверсия и потери перестают считаться);
`last_open_stage` — без открытого этапа. Второй отказ приходит и от `DELETE`, и
от `PATCH`: раньше убрать последний открытый с доски система отказывалась, а
перевести его в «выиграна» тем же `PATCH` — позволяла молча, и итог получался
хуже. Новая заявка идёт на первый открытый; без него берётся просто первый по
порядку — то есть каждая заявка, заведённая с сайта или кнопкой, рождалась бы
закрытой, и человек этого не выбирал.

**Смена вида этапа НЕ закрывает заявки, которые в нём стоят** — и это нарочно.
Проверено: перевели этап из открытого в «выиграна» — `closed_at` у его заявок
остаётся пустым, они по-прежнему в списках и по-прежнему держат бронь. Заявка
закрывается только переездом (`POST /deals/{id}/move`), потому что закрытие —
это событие с датой и последствиями: по нему считается конверсия и по нему
списывается товар со склада. Проставить тридцати старым заявкам «закрыта
сегодня» значило бы переписать историю, а разослать им событие переезда — снять
со склада тридцать списаний разом, которых никто не просил. Побочное следствие
названо вслух: до первого переезда столбец «выиграно» показывает незакрытые
заявки, и отчёт по видам этапов расходится с отчётом по датам закрытия.

## Напоминания (блок `tasks`)

Роутер закрыт `require_module("tasks")`: при выключенном блоке любой из адресов
отвечает `403 module_disabled`.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/tasks` | 🔑 `tasks.view` | Список одним `items`; у записи рядом с номерами — `assignee_name`, `client_name`, `deal_title`. Фильтры `scope` (по умолчанию `open`), `assignee_id`, `client_id`, `deal_id` |
| GET | `/notifications` | 👤 | Свои уведомления страницей (`?page=`): `kind`, `params`, `link`, `read`. Подпись собирает экран по `kind` |
| GET | `/notifications/summary` | 👤 | Число непрочитанных — для колокольчика |
| POST | `/notifications/read` | 👤 | Отметить прочитанными: `{ids}` или все свои |
| GET | `/tasks/summary` | 🔑 `tasks.view` | Счётчики для навигации |
| POST | `/tasks` | 🔑 `tasks.create` | Завести: `title`, `due_at`, `assignee_id`, `client_id`, `deal_id` |
| PATCH | `/tasks/{id}` | 🔑 `tasks.edit` | Изменить любое поле, включая `is_done` |
| DELETE | `/tasks/{id}` | 🔑 `tasks.delete` | Удалить |

`summary` — отдельная точка, потому что счётчики спрашивают часто и без списка:
полоса с числом просроченных висит на каждом экране, и тянуть ради неё все
напоминания фирмы незачем.

Срок приходит абсолютным моментом со смещением — «18:00 по Киеву», а не «18:00»
вообще. Заявки с сайта и пропущенные звонки заводят напоминание этим же блоком;
выключен он — событие принимается без напоминания, а не отвергается.

## Доски и работы (блок `boards`)

Роутер закрыт `require_module("boards")`: при выключенном блоке любой из адресов
отвечает `403 module_disabled`.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/boards` | 🔑 `boards.view` | Список: `?search=`, `?client_id=`, счётчики работ и просмотров; у каждой доски `created_by_name` (кто завёл; `null` у досок до колонки автора) и `created_at` со временем |
| POST | `/boards` | 🔑 `boards.create` | Создать: `title`, `description?`, `client_id?`, `deal_id?` |
| GET | `/boards/{id}` | 🔑 `boards.view` | Доска + работы по порядку + **все** ссылки доски (ключ `shares`) |
| PATCH | `/boards/{id}` | 🔑 `boards.edit` | Название, описание, `client_id`, `deal_id`, `cover_work_id`, `is_published` |
| DELETE | `/boards/{id}` | 🔑 `boards.delete` | Мягкое удаление; все ссылки доски перестают открываться |
| POST | `/boards/{id}/works` | 🔑 `boards.create` | Загрузка файла работы (multipart). Ответ `202` + `work` со `status=processing` |
| GET | `/boards/{id}/works/{work_id}` | 🔑 `boards.view` | Одна работа (поллинг статуса обработки) |
| PATCH | `/boards/{id}/works/{work_id}` | 🔑 `boards.edit` | `title`, `description`, `project_url` (только `http(s)`, иначе `422 bad_project_url`), `preview_focus` — видимый фрагмент работы (0…1): только у картинок с известными сторонами (иначе `422 not_a_croppable_work`), значение подрезается до диапазона, `null` возвращает показ от верха |
| PUT | `/boards/{id}/works/order` | 🔑 `boards.edit` | Новый порядок: `{"work_ids": [5, 2, 9, ...]}` (drag-and-drop) |
| DELETE | `/boards/{id}/works/{work_id}` | 🔑 `boards.delete` | Удалить работу и её файлы |
| GET | `/boards/{id}/works/{work_id}/download` | 🔑 `boards.view` | Исходник работы файлом (`attachment`). Имя — то, под которым файл загрузили, расширение — настоящее (вид определяется по сигнатуре). Работа обязана принадлежать НАЗВАННОЙ доске: чужой номер получает `404`, а не чужой файл |
| GET | `/boards/{id}/download` | 🔑 `boards.view` | Все исходники доски одним архивом, потоком (в памяти не собирается). Имена внутри пронумерованы порядком доски; пустая доска — `422 board_has_no_files` |

`deal_id` — заявка, ради которой доска сделана. Необязательный: доски
существовали до заявок и обязаны работать без них.

В карточке доски у готовых работ есть ещё два считаемых поля: `place_ratio` —
форма места работы на витрине, и `is_cropped` — обрежется ли она в этом месте.
Оба считает сервер, потому что раскладку знает он же: повтори редактор это
правило у себя — рамка обрезки и плитка у клиента разъехались бы молча.

## Публичные ссылки

Тот же блок `boards`: выключили доски — делиться нечем, и роутер `/shares` закрыт
`require_module("boards")` целиком.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/boards/{id}/shares` | 🔑 `boards.view` | Все ссылки доски: токен, статус, срок, есть ли PIN, просмотры |
| POST | `/boards/{id}/shares` | 🔑 `boards.edit` | Создать: `expires_at?`, `pin?` (4–8 цифр). Ответ содержит полный URL |
| PATCH | `/shares/{id}` | 🔑 `boards.edit` | Изменить: `is_active` (отзыв/включение), `expires_at`, `pin` (null — убрать) |
| DELETE | `/shares/{id}` | 🔑 `boards.delete` | Удалить ссылку и журнал её просмотров. Файлы работ не трогаются: они принадлежат доске, а у доски может быть несколько ссылок |
| POST | `/shares/{id}/regenerate` | 🔑 `boards.edit` | Отозвать текущую + создать новую с теми же настройками |
| GET | `/shares/{id}/views` | 🔑 `boards.view` | Журнал просмотров: когда, сколько уникальных |

Списки ссылок отдают **все** записи, включая отозванные и просроченные:
`shares_repo.list_for_board` фильтрует только по доске. Спрятать отозванную
ссылку значило бы спрятать и ответ на вопрос «а эту я точно закрыл».

## Фирмы (блок `companies`)

Свои юрлица: от чьего имени выдаётся бланк, чьи реквизиты на нём напечатаны.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/companies` | 🔑 `companies.view` | Список одним `items` |
| GET | `/companies/{id}` | 🔑 `companies.view` | Одна фирма |
| POST | `/companies` | 🔑 `companies.create` | Завести |
| PATCH | `/companies/{id}` | 🔑 `companies.edit` | Изменить. Присланное пустым значит «сотри реквизит», а не «оставь как было» |
| POST | `/companies/{id}/default` | 🔑 `companies.edit` | Сделать основной; отвечает всем списком |
| DELETE | `/companies/{id}` | 🔑 `companies.delete` | Удалить |

**«Сделать основной» — отдельная точка, а не поле в `PATCH`.** Назначение
задевает другую запись — ту, с которой признак снимается, — и прятать это внутрь
обычного сохранения формы значит менять две строки там, где человек правил одну.
Инвариант «ровно одна фирма по умолчанию» держится запросом
(`companies.make_default`), а не частичным индексом: их в MySQL нет.

## Настройки сайта

Раздел целиком закрыт на `settings.view`, а **каждая правка дополнительно требует
`settings.manage`**. Разделение не формальное: бухгалтеру нужны реквизиты и
валюта, а логотип сайта и режим обслуживания — нет, и оно позволяет дать первое,
не отдавая второго.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/settings` | 🔑 `settings.view` | Все настройки: бренд, контакты, соцсети, язык витрины, валюта |
| PATCH | `/settings` | 🔑 `settings.manage` | Обновить значения. `studio_site_url` — только `http(s)`, иначе `422 bad_site_url` |
| GET | `/settings/maintenance` | 🔑 `settings.view` | Закрыт ли сайт на работы, с какой запиской и кем |
| POST | `/settings/maintenance` | 🔑 `settings.manage` | Закрыть сайт на работы или открыть обратно: `{enabled, note}` |
| POST | `/settings/logo` | 🔑 `settings.manage` | Загрузить логотип (multipart). Путь возвращается с меткой версии `?v=` |
| DELETE | `/settings/logo` | 🔑 `settings.manage` | Убрать логотип |
| POST | `/settings/site-logo` | 🔑 `settings.manage` | Логотип сайта для кнопки «Return to the site» |
| POST | `/settings/site-logo/fetch` | 🔑 `settings.manage` | Попробовать достать логотип с сайта из настроек. Не вышло — `422 logo_fetch_failed`, и логотип грузят руками |
| DELETE | `/settings/site-logo` | 🔑 `settings.manage` | Убрать логотип сайта |
| POST | `/settings/og-image` | 🔑 `settings.manage` | Картинка для соцсетей по умолчанию |
| DELETE | `/settings/og-image` | 🔑 `settings.manage` | Убрать её |

**Режим обслуживания — не мелочь среди логотипов.** Включённый, он отвечает
`503` и сотруднику, и посетителю витрины; владелец при этом проходит по
устройству режима. Лежит он в настройках, то есть в базе, и потому переживает
пересборку контейнера — а включает его не только человек: его ставит и снимает
переезд базы (`migrate_maintenance` в `opencrm.sh`). Убитый посреди переноса
переезд оставляет сайт закрытым навсегда, и проверено делом, что этого не видит
никто: `/healthz` отвечал «всё в порядке», докер, автообновление и мониторинг
молчали, а единственный, кто может починить, видел рабочий сайт. Поэтому режим
попал отдельной строкой в `/healthz` (`maintenance: on|off`) — но **кода ответа
не меняет**: на 200 завязаны проверка здоровья docker и откат обновления, а они
умеют одно — заменить контейнер приложения. Закрытый сайт этим не чинится, зато
цикл перезапусков поверх обслуживания превратил бы объявленную работу в
настоящую аварию. Разбор целиком — в `web/main.py:healthz` и
[08-razvyortyvanie.md](../ekspluatatsiya/08-razvyortyvanie.md).

## Журнал действий

Только чтение. Ручек записи и удаления **нет и не будет**: журнал дописывается
сервисами в той же транзакции, что и само изменение, а ручка «добавить запись»
позволила бы дописать в него что угодно и когда угодно — то есть отменила бы
весь смысл затеи не хуже, чем ручка «удалить» (см. [07-bezopasnost.md](../ekspluatatsiya/07-bezopasnost.md)).

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/audit` | 🔑 `audit.view` | Журнал. Фильтры: `actor_id`, `entity_type`, `entity_id`, `source`, `action`, `search` (по имени человека и названию объекта), `since`, `until`; `page`/`per_page` (до 100) |

У области `audit` единственное действие — `view`, и журнал можно отдать роли, не
делая человека владельцем системы. `edit` и `delete` не объявлены: запрет на
правку стоит в самой модели, и завести здесь такие права значило бы обещать то,
чего система не позволит никому, включая root. Право, которое ничего не даёт,
хуже его отсутствия.

Запись ответа: `action`, `actor_id`, `actor_name` (снимок), `source`,
`source_ref`, `entity_type`, `entity_id`, `entity_label` (снимок),
`value_before`, `value_after`, `created_at`. `actor_id: null` бывает только у
`source` = `telephony_webhook` и `mail_sync`. `value_before: null` означает «величины
не было», пустая строка — «величина была пустой».

## Почта (блок `mail`)

Весь роутер закрыт двумя зависимостями сразу: `require_module("mail")` и
`require_perm("mail", "view")`. При выключенном блоке любой маршрут отвечает
**403 `module_disabled`** — не 404, потому что маршрут существует, его выключили;
без права `mail.view` не открывается ни одна ручка раздела, включая настройки
ящиков. Сам ящик — настройка уровня фирмы, поэтому его CRUD и проверка соединения
поверх этого требуют `settings.manage`: заводить почтовый ящик и читать почту —
разные по весу решения.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/mail/accounts` | 🔑 `settings.manage` | Ящики фирмы. **Пароля в ответе нет никогда** — только `has_password` |
| POST | `/mail/accounts` | 🔑 `settings.manage` | Завести ящик: адрес, IMAP/SMTP, логин, `password` (уходит только внутрь) |
| PATCH | `/mail/accounts/{id}` | 🔑 `settings.manage` | Изменить. Пустой `password` = «не менять», а не «стереть» |
| DELETE | `/mail/accounts/{id}` | 🔑 `settings.manage` | Удалить ящик вместе с зеркалом его писем. Лента клиента остаётся |
| POST | `/mail/accounts/{id}/check` | 🔑 `settings.manage` | Проверка входа по IMAP и SMTP. Ответ `{ok, error}` — ошибка текстом, без пароля |
| GET | `/mail/senders` | 🔑 `mail.create` | Ящики, из которых можно писать: только `id`, `title`, `address`, только включённые |
| POST | `/mail/accounts/{id}/sync` | 🔑 `mail.view` | Ручная синхронизация. Своего права не требует: это чтение почты фирмы, а не правка настроек — ждать управляющего ради свежих писем незачем |
| GET | `/mail/unread` | 🔑 `mail.view` | Сколько входящих не прочитано — число на пункт меню; пересчитывается по намёку блока почты |
| GET | `/mail/messages` | 🔑 `mail.view` | Список: `account_id`, `client_id`, `deal_id`, `direction=in\|out`, `unread`, `search`. Тела писем в списке не отдаются |
| GET | `/mail/messages/{id}` | 🔑 `mail.view` | Письмо целиком, включая `body_text`/`body_html` |
| POST | `/mail/messages/{id}/read` | 🔑 `mail.view` | `{"is_read": true\|false}` |
| POST | `/mail/send` | 🔑 `mail.create` | Отправить: `to[]`, `subject`, `body`, `account_id?`, `client_id?`, `deal_id?`, `reply_to_id?` |

### Ответ встаёт в переписку, а не рядом с ней

`reply_to_id` — id письма, на которое отвечают. Из него берутся заголовки
цепочки (`In-Reply-To`, `References`), а клиент и заявка наследуются, если
своих не назвали: иначе переписка по заявке рвалась бы на первом же ответе —
входящее письмо привязано к клиенту, а ответ на него ни к чему.

**Проверено на двух живых почтовых серверах**, а не рассуждением. В лаборатории
(два независимых maddy, свой DNS, свой удостоверяющий центр) ответ из CRM
приходил собеседнику ОТДЕЛЬНЫМ письмом: почтовые клиенты собирают переписку по
паре `In-Reply-To` + `References`, а транспорт не ставил ни того, ни другого.
Человек получал ответ, не понимая, на что именно.

Одного `In-Reply-To` мало. У ветки длиннее двух писем клиенты смотрят на
`References`, а собрать её неоткуда, кроме как из того письма, на которое
отвечают, — поэтому цепочка сохраняется ПРИ СИНХРОНИЗАЦИИ (`in_reply_to`,
`references` у письма) и уносится в ответ целиком, последними двадцатью
ссылками. У переписки на сотню писем заголовок разросся бы до килобайтов, а
ветку клиенты собирают по последним ссылкам.

Заголовки ставятся **только когда есть на что отвечать**: пустой `In-Reply-To`
хуже отсутствующего — часть клиентов считает его ссылкой в никуда и прячет
письмо из переписки вовсе.

**И только когда есть чем.** В колонке `message_id` лежит ключ идемпотентности,
а не всегда сам заголовок: у письма с Message-ID длиннее 320 знаков (рассылка,
тикет-система) туда кладётся отпечаток `opencrm-sha256:…`. Поставь мы его в
`In-Reply-To` — собеседник получил бы ссылку на письмо, которого у него нет, а
наш внутренний ключ уехал бы в заголовки чужого сервера. Настоящего заголовка у
нас в этом случае нет нигде, и выдумать его нельзя: ответ уходит без цепочки,
как и раньше, но без вранья.

`references` входящего **обрезается хвостом** до тех же двадцати ссылок.
Колонка `TEXT` — это 65 535 БАЙТ, а не символов; письмо из очень длинной ветки
дало бы строку длиннее, MySQL ответил бы `1406 Data too long`, письмо
откатилось бы и не попало в CRM **никогда**: `last_uid` уезжает вперёд по
соседям, и второго захода за ним не будет.

### Заголовок не дописывает чужих заголовков

Тема и адреса уходят в письмо одной строкой: перевод строки внутри значения
дописывает в письмо ЧУЖИЕ заголовки. Тема вида `Счёт\nBcc: vor@example.com`
отправила бы копию постороннему. Библиотека такое отвергает, но отвергает
`ValueError` — он летит мимо `MailTransportError`, и наружу это выходило
пятисотой ошибкой вместо внятного отказа.

### Писать письма может не только владелец

`/mail/accounts` закрыт `settings.manage` и закрыт правильно: там хосты, порты
и логины — настройка уровня фирмы. Но чтобы ОТПРАВИТЬ письмо, всего этого знать
не надо, нужен выбор из двух-трёх адресов, — и для него есть `/mail/senders`,
открытый тому, у кого есть право писать.

Пока этой ручки не было, экраны почты, клиента и заявки спрашивали общий
список. Менеджеру с правом писать он отвечал отказом: ящиков ноль, выбирать не
из чего, кнопки отправки и ответа спрятаны — **письма мог писать только
владелец системы**. Ни типами, ни тестами это не ловилось; нашлось разбором.

### Отказ по части адресатов — не отправка и не провал

`smtplib.send_message` бросает исключение, только когда отвергнуты ВСЕ
адресаты; при частичном отказе он молча возвращает словарь тех, кому не
досталось. Пока этот словарь выбрасывался, письмо на двоих доходило одному, а в
CRM появлялась запись «отправлено» — и узнать об этом было неоткуда. Замерено
на живых серверах именно так.

Теперь письмо **записывается**, а отвергнутые адресаты называются: в ответе
ручки полем `refused`, в ящике — строкой последней ошибки. Бросить отказ здесь
нельзя, и это исправление исправления: к моменту, когда сервер называет
отвергнутых, письмо остальным УЖЕ доставлено. Отказ означал бы, что записи в
CRM нет, — человек нажмёт «отправить» снова, и получившие получат его дважды.
Отозвать письмо нельзя ничем.

### Что делает CRM, чтобы письмо не сочли спамом

Спам решается тремя записями — SPF, DKIM и DMARC, — и **все три живут в DNS
домена, а не в приложении**. Подписывает письмо DKIM'ом почтовый сервер, через
который CRM отправляет (`submission`), а не CRM: у неё нет и не должно быть
закрытого ключа домена. Значит задача приложения здесь ровно одна — не мешать:

- отправлять через SMTP владельца домена, а не напрямую с VPS (так и сделано:
  ящик задаётся настройками, своего MTA у CRM нет);
- ставить корректный `From` — адрес того самого ящика, из которого шлём. Конверт
  (`MAIL FROM`) smtplib берёт из него же, и выравнивание, которого требует
  DMARC, получается само;
- ставить `Message-ID`, `Date`, `MIME-Version` и кодировку — иначе письмо
  выглядит самодельной рассылкой;
- отвечать в цепочку: письмо, вставшее в существующую переписку, у любого
  фильтра весит меньше, чем письмо от того же адреса «ниоткуда».

Измерено в лаборатории: письмо, отправленное CRM через `alpha.test` на
независимый сервер `beta.test`, получило от получателя вердикт
`spf=pass; dkim=pass; dmarc=pass`.

Чего проверить локально нельзя и обещать нечестно: репутацию адреса и сети,
попадание в чёрные списки, поведение конкретных Gmail и Outlook. Это решается
не кодом, а доменом, его историей и настройками у провайдера почты.

Письмо не заводит собственной истории: при появлении оно порождает запись общей ленты (`client_notes`, `kind='email'`, `direction`, `happened_at` = момент отправки письма). Входящее ищет клиента по адресу отправителя; нашли — привязали, не нашли — письмо остаётся непривязанным и **клиента не создаёт**: в ящик фирмы валятся рассылки, спам и ответы роботов, а «клиент» — решение человека.

## Шаблоны сообщений (блок `templates`)

Заготовка на то, что пишут снова и снова: первый ответ на заявку, ссылка на доску, напоминание об оплате. Шаблон — это `name` (уникальное), `body` с подстановками и `channel` (`any` / `email` / `note`) — где он применим. Универсальный шаблон попадает в выдачу по любому каналу: он для того и есть, чтобы один и тот же текст не держать в двух экземплярах.

Весь роутер закрыт `require_module("templates")`. Читать шаблоны может всякий, кто пишет клиентам (`templates.view`), править — тот, кому дано право.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/templates/fields` | 🔑 `templates.view` | **Закрытый набор полей**: `{key, needs}`, где `needs` — `""` / `client` / `deal`. Отдаётся с сервера, чтобы список не существовал во втором экземпляре во фронтенде |
| GET | `/templates` | 🔑 `templates.view` | Список по алфавиту. `?channel=email` — годные для письма (включая `any`); незнакомый канал — `422 unknown_channel`, а не пустая выдача, неотличимая от «шаблонов нет» |
| GET | `/templates/{id}` | 🔑 `templates.view` | Один шаблон |
| GET | `/templates/{id}/render` | 🔑 `templates.view` | Готовый текст: `?client_id=`, `?deal_id=` (оба необязательны). Ответ — `{template_id, name, channel, text, missing[], unknown[]}` |
| POST | `/templates` | 🔑 `templates.create` | Завести: `{name, body, channel?}`. Занятое название — `409 template_name_taken` |
| PATCH | `/templates/{id}` | 🔑 `templates.edit` | Изменить любое из трёх полей |
| DELETE | `/templates/{id}` | 🔑 `templates.delete` | Удалить. Уже отправленные сообщения не меняются: применённый шаблон давно стал самостоятельным письмом или записью ленты и связи с шаблоном не хранит |

`render` сделан `GET`'ом намеренно: подстановка ничего не создаёт и не меняет. POST пришлось бы закрывать правом на правку — и смотреть готовый текст смог бы только тот, кому разрешено править сами шаблоны, то есть ровно наоборот тому, ради чего блок заводился.

### Подстановки

Пишутся как `{client_name}`. Всё остальное в фигурных скобках (`{ }`, кусок JSON) — обычный текст: подстановка — это скобки с именем внутри, и отвергать всё подряд значило бы запретить написать скобку, которую имели в виду буквально.

**Набор закрыт и объявлен в коде** (`core/services/template_service.py`). Сейчас в нём шесть полей: `client_name`, `client_company`, `deal_title`, `deal_number`, `board_url`, `company_name`. Открытый набор («любое поле модели») означал бы письмо клиенту с себестоимостью его же заказа, поэтому значения считает поимённый список, а не `getattr` по присланному имени.

Четыре правила, которые важнее самих полей:

- **Неизвестная подстановка не проходит молча.** При сохранении — `422 unknown_placeholder` с перечислением **всех** виноватых (`{clietn_name}`, `{deal_amount}`). Тихая пустота означала бы «Здравствуйте, !» в письме клиенту, и это увидели бы все, кроме автора шаблона.
- **Поле, убранное из набора после того, как шаблон написали**, подставляется видимой пометкой `[?имя]` и попадает в `unknown`. Пустотой оно не становится никогда.
- **Законно пустого значения не бывает «просто пусто».** У клиента может не быть фирмы, у заявки — доски; такое поле подставляется тире (`—`) и называется в `missing`. Прочерк без списка приняли бы за опечатку в шаблоне, список без прочерка не спас бы от «Фирма: » посреди предложения. Слово вместо тире не подставляется намеренно: тело пишут на своём языке, и английская заплатка посреди русского письма хуже честного прочерка.
- **Подставленное не управляет подстановкой.** Значение вставляется буквально (`\1` и `<b>` в имени клиента остаются собой), один проход (клиент по имени «ООО {board_url}» не вытащит чужую ссылку) и в одну строку (имя с переводами строк не дописывает в письмо абзац «P.S. переведите деньги на другой счёт»).

`missing` и `unknown` приходят **всегда**, пустыми списками в том числе: форма ответа не зависит от того, повезло ли с данными, — то же правило, что у выключенных блоков в сводке.

Откуда берутся значения: `deal_number` — это `#{id}` заявки (своего ряда номеров у заявок нет: номер печатают на бланке, а заявку называют словами). `board_url` — **живая** ссылка доски заявки (отозванная и просроченная не годятся: они ведут на страницу с отказом); из нескольких берётся самая ранняя, чтобы ответ не плавал между вызовами. `company_name` — фирма заявки → основная фирма → название бизнеса из настроек. Выключенные блоки `boards` и `companies` в шаблон **не подтекают**: поле становится прочерком, иначе «выключено» означало бы лишь «не видно в меню».

Клиент и заявка обязаны сходиться: заявка чужого клиента — `422 deal_other_client`, тот же код, что у письма и у звонка. Заявка указана, клиент нет — клиент берётся у заявки. Без обоих `render` тоже отвечает: это предпросмотр, где поля про них прочерки.

## Бланки и акты (блок `documents`)

Бланк — это квитанция приёмки: что взяли, у кого, номер и состояние. Акт —
обратная сторона: что сделано и принято. Живут в одном блоке и на одном экране,
потому что это два конца одной работы.

Роутер закрыт `require_module("documents")` целиком, а не по маршруту:
пропущенный маршрут остался бы открытым, и выключенный блок продолжал бы отвечать
тому, кто помнит адрес.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/documents` | 🔑 `documents.view` | Список: `search`, `status`, `client_id`, `deal_id`, `kind` (повторяемый), `sort`, пагинация. В ответе сверх обычного — `counts`: сколько бумаг каждого вида. В строках — `total` по строкам бумаги (пусто у бумаги без строк) |
| POST | `/documents` | 🔑 `documents.create` | Завести бланк |
| GET | `/documents/by-number/{number}` | 🔑 `documents.view` | Поиск сканом: сюда приходит то, что прочитал сканер штрихкода |
| GET | `/documents/{id}` | 🔑 `documents.view` | Бланк + история состояний с именами авторов |
| POST | `/documents/{id}/status` | 🔑 `documents.issue` | Сменить состояние: `{status, note}` |
| DELETE | `/documents/{id}` | 🔑 `documents.edit` | Удалить квитанцию или акт, заведённые по ошибке: не закрытые, без движений склада, денег и накладных по основанию (`422 document_in_use`). Пишется в журнал `document.deleted` |
| GET | `/documents/{id}/print` | 🔑 `documents.view` | **HTML на печать**, а не JSON: две одинаковые половины с линией отреза, штрихкод и QR. `?locale=` переопределяет язык бумаги. Заказ и накладная — отказ (`document_is_an_order`, `document_is_a_waybill`): у них свои формы |
| POST | `/documents/acts` | 🔑 `documents.create` | Завести акт выполненных работ |

#### Порядок и категории списка бумаг

**`sort` — закрытый перечень, а не имя колонки.** Известны `new` (свежие
сверху, умолчание), `old`, `number`, `status`; незнакомый — `422 unknown_sort`, и
в отказе перечислены известные. Имя колонки снаружи означало бы `ORDER BY` по
чему угодно, включая то, чего нет в индексах.

Порядок в каждой строке перечня **полный сам по себе** — с первичным ключом в
той же стороне. Оставлять разрешитель ничьей на `page_of` здесь нельзя: он
дописывает ключ всегда по убыванию, а `created_at` хранится с точностью до
секунды, и «старые сверху» на бумагах, заведённых в одну секунду, в точности
совпадали со «свежими сверху».

**`kind` повторяется**, а не перечисляется через запятую: `?kind=intake&kind=act`.
Экран снимает категории по одной, и «покажи квитанции и акты» — это один запрос.
Незнакомый вид — `422 unknown_kind`. Без этого отбора «скрыть категорию» на
экране означало бы прятать уже приехавшие строки, то есть врать в счётчике
«всего N».

**`counts` считается БЕЗ отбора по виду** — и это главное свойство ответа. Считай
мы с ним, ответ на `?kind=intake` содержал бы одну строку счёта, остальные
категории исчезли бы с экрана вместе со своими числами, а с ними и способ
вернуть скрытое. Прочие отборы (`search`, `status`, `client_id`, `deal_id`) на
счёт действуют: он обязан описывать тот же экран, что и список. Сумма по
категориям сходится с `total`, пока `kind` не задан.

У списка заказов (`GET /orders`) то же самое, но категория там — **состояние**:
вид у заказа выбирается двумя чипами, а «что делать сейчас» отвечает статус.
Поэтому `counts` там по статусам и считается без отбора по статусу; `sort` —
тот же перечень, список-то один и тот же (`documents`).
| GET | `/documents/acts/{id}` | 🔑 `documents.view` | Акт: позиции и история |
| POST | `/documents/acts/{id}/lines` | 🔑 `documents.edit` | Добавить строку работ |
| DELETE | `/documents/acts/{id}/lines/{line_id}` | 🔑 `documents.edit` | Убрать строку |
| POST | `/documents/acts/{id}/complete` | 🔑 `documents.issue` | Провести акт |
| POST | `/documents/acts/{id}/cancel` | 🔑 `documents.edit` | Отменить непроведённый. Склада и воронки не касается — их не трогали |
| GET | `/documents/acts/{id}/print` | 🔑 `documents.view` | **HTML на печать**: перечень работ, итог и две подписи на одном листе |

**`issue` — не то же самое, что `create`.** Квитанцию печатает приёмщик, а
закрывает работу мастер, и через «видит / не видит раздел» эта просьба не
описывается. Поэтому смена состояния и проведение акта отделены от заведения.

**Проведение акта — один маршрут на три действия**, и это и есть задача: списать
израсходованное, зафиксировать работы, двинуть заявку. Три отдельные ручки
означали бы, что состояние «списано, но заявка не переведена» достижимо обычным
нажатием, а не только сбоем; здесь оно недостижимо вовсе — всё едет одной
транзакцией запроса, и отказ любой части отменяет остальные.

Печать делает браузер, а не сервер: сервер на VPS, принтер на столе. Язык бумаги
переопределяется параметром, потому что бланк печатают под клиента, а не под
сотрудника: приехал турист — печатаем по-английски, интерфейс мастера при этом
остаётся прежним. Себестоимости на бумаге нет и быть не может — клиенту
показывают, во сколько работа обошлась ему.

## Заказы (блок `orders`, по умолчанию выключен, зависит от `documents`)

Связь с заявкой в обе стороны сделана: `POST /deals/{id}/order` заводит заказ по
заявке, `POST /orders/{id}/deal` цепляет и отцепляет уже заведённый. Отдельной
ручкой, а не полем в `PATCH /orders/{id}`: у заказа правится только эта связь,
и общий `PATCH` пришлось бы объяснять, какие ещё поля он сегодня читает.

Заказ — вид бланка, поэтому номер, состояния, печать и поиск сканом живут в
`documents`; здесь только то, чего у квитанции нет: перечень позиций, сборка
сканером, отгрузка и приёмка.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/orders` | 🔑 `orders.view` | Список заказов: `search`, `kind`, `status`, `sort` (в том числе `due` — ближайшие сверху), `client_id`, `deal_id`, `overdue=1` (открытые со сроком раньше «сейчас»), пагинация. В ответе `counts` — сколько заказов в каждом состоянии; у заказа `due_at` и `overdue` (считает сервер) |
| POST | `/orders` | 🔑 `orders.create` | Завести: `kind`, по желанию `client_id` (или `client_name`/`client_phone`/`client_email` — поиск карточки, разбор в docs/19), `due_at` — срок. В ответе всегда `client_name` — имя или `null` |
| PATCH | `/orders/{id}` | 🔑 `orders.edit` | Срок открытого заказа: `{"due_at": …}`, `null` — снять; переход в историю. Закрытый — `422 order_finished` |
| POST | `/orders/{id}/reserve` | 🔑 `orders.edit` | Продлить бронь заказа с сайта: `{"days": 3}` (1–90) от «сейчас»; без брони — `422 no_reservation`, закрытый — `422 order_finished`; запись в историю |
| POST | `/orders/{order_id}/cancel` | 🔑 `orders.edit` | Отменить открытый заказ. `422 already_shipped_by_waybill` — товар уже уехал накладной, путь назад — сторно (05.09.2026) |
| POST | `/orders/{order_id}/client` | 🔑 `orders.edit` | Кому отгружаем: `client_id` или `null` — привязать или отвязать, пока заказ открыт. `422 order_finished` у проведённого и отменённого (записанное не переписывается), `404 client_not_found` |
| GET | `/orders/{id}` | 🔑 `orders.view` | Заказ с позициями, выписанными по нему накладными (`waybills`, ключа нет вовсе при выключенном блоке) и историей переходов (`events`). У открытого заказа при включённых накладных в строках — `shipped_milli`: сколько уехало проведёнными накладными (`order_service.otgruzheno_po_tovaram`, та же формула, что у закрытия по накладной) |
| POST | `/orders/{id}/lines` | 🔑 `orders.edit` | Добавить позицию |
| PATCH | `/orders/{id}/lines/{line_id}` | 🔑 `orders.edit` | Изменить позицию |
| DELETE | `/orders/{id}/lines/{line_id}` | 🔑 `orders.edit` | Убрать позицию |
| POST | `/orders/{id}/pick` | 🔑 `orders.edit` | Отметить собранным по отсканированному коду: `{code, quantity_milli}`. Код, не найденный среди штрихкодов, пробуется как артикул (с 04.09.2026) |
| POST | `/orders/{id}/ready` | 🔑 `orders.edit` | Заказ собран |
| POST | `/orders/{id}/close` | 🔑 `orders.issue` | Провести: отгрузить покупателю или принять от поставщика. **Отмены проведения нет** (05.09.2026): назад — только возвратом, `POST /orders/{id}/returns` |
| POST | `/orders/{id}/returns` | 🔑 `orders.create` | Завести черновик возврата по проведённому заказу покупателя: строки предзаполнены тем, что ещё можно вернуть. `422 order_not_closed`, `not_a_sales_order`, `nothing_to_return`. Разбор — [22](../bloki/22-vozvraty.md) |
| POST | `/orders/{id}/cancel` | 🔑 `orders.edit` | Отменить непроведённый. Резерв снимется сам — он не хранится |
| DELETE | `/orders/{id}` | 🔑 `orders.edit` | Удалить заказ, заведённый по ошибке: непроведённый, без движений, денег и накладных (`422 document_in_use`). Журнал — `document.deleted` |
| POST | `/orders/{id}/deal` | 🔑 `orders.edit` | Прицепить заказ к заявке или отцепить (`deal_id: null`) |
| GET | `/orders/{id}/print` | 🔑 `orders.view` | **HTML на печать**: таблица позиций и итог |

**`edit` против `issue`.** Набирать позиции и двигать склад — разные полномочия:
сборщик набирает, отгружает старший. Поэтому `pick`, `ready` и `cancel` живут на
`edit`, а `close` и проведение возврата — на `issue`, как выпуск бланка.

Собранное живёт отдельно от заказанного: расхождение «заказано пять, собрано
четыре» видно построчно до отгрузки, а не на выдаче.

**Отказы заказов.** Перечислены здесь, а не под каждой строкой таблицы: половина
приходит от нескольких ручек сразу.

- заведение по заявке (`POST /deals/{id}/order`): `deal_closed` — заявка
  закрыта; `no_product_lines` — переносить нечего (свои траты и услуги в заказ не
  идут: по заказу собирают коробки, и строка «упаковка» показывала бы «собрано 0
  из 1»); **`409 deal_order_exists`** — у заявки уже есть незакрытый заказ.
  Второй заказ повторил бы те же строки, и бронь удвоилась бы: три штуки в
  заявке стали бы шестью, и продавец отказал бы покупателю, глядя на товар,
  лежащий на полке;
- связь с заявкой (`POST /orders/{id}/deal`): `deal_not_found`, `deal_closed`.
  Заявку спрашивают и на область видимости: прицепленный заказ ПЕРЕНИМАЕТ её
  бронь, и без проверки менеджер «только со своими» уводил бы товар из-под
  чужого покупателя, ни разу не открыв его заявку;
- заведение (`POST /orders`): `not_an_order` — вид не заказ; `client_ambiguous`,
  `client_phone_mismatch`, `client_name_only` — по присланным имени и телефону
  клиент не определяется однозначно. Все три `409`, а не «завести нового»:
  выбрать за человека значит однажды расколоть историю клиента надвое;
- сборка (`POST /orders/{id}/pick`): `product_not_in_order` — отсканирован код
  товара, которого в заказе нет;
- состояния: `order_not_new` (`ready` — заказ уже не новый), `order_finished`
  (заказ проведён или отменён), `order_not_closed` (возврат — по непроведённому
  возвращать нечего),
  `already_shipped_by_waybill` — товар по этому заказу уже ушёл накладной, и
  проведение отгрузило бы его вторично; в отказе названы номера накладных.

`orders.view_amounts` закрывает **закупочную** цену в интерфейсе. В печатной
форме суммы показываются всегда: тот, кто печатает заказ клиенту, обязан видеть
цены — иначе бумага уйдёт с пустым столбцом.

**И закрывает во ВСЕХ ответах, включая ответы кнопок.** Разбор: список и
карточка право спрашивали, а `ready`, `close`, `cancel` и заведение
отдавали заказ с суммами всегда — `amounts=True` стояло прямо в вызове
сериализатора. Сборщик без права не видел цен нигде, но стоило ему нажать
«готов», как ответ приходил с итогом и ценами строк. Та же беда, что была у
карточки товара: **`amounts` в сериализаторе по умолчанию `True`, и всякая
ручка, забывшая спросить право, становится обходом.** Стережёт
`tests/test_roles.py`.

**Заказ не закрывается ручкой бланков.** Заказы и бланки живут в ОДНОЙ таблице
(`documents`, разница в `kind`), а `POST /documents/{id}/status` брал запись по
номеру, ни о чём не спрашивая. С правом `documents.issue` заказ переводился в
«выдан»: статус менялся, история перехода писалась, **а склад не двигался
вовсе**. Обход права `orders.issue` — полбеды; хуже второе: остаток остаётся на
месте, товар уезжает, и расхождение всплывает на инвентаризации через месяц,
когда концов уже не найти. Теперь ручки бланков отвечают
`422 document_is_an_order`. Правило стоит на ГРАНИЦЕ, у ручек, а не в общей
смене статуса: `order_service` зовёт её сам и законно.

**Печать была из этого правила исключена, и напрасно.** `GET
/documents/{id}/print` брал запись тем же общим `get`, и заказ с накладной
печатались КВИТАНЦИЕЙ ПРИЁМА: две половины с линией отреза, пустые поля «что
приняли», ни перечня позиций, ни сумм. Своя форма есть у обоих. Теперь печать
бланка проверяет вид ровно так же, как остальные ручки.

**История заказа отдаётся карточкой, а не отдельной ручкой** — тем же составом,
что у бланка и акта: время, из чего во что, примечание и имя автора снимком.
Заведена по беде: закрытие при выключенном складе пишет в примечание «движений
нет», и это единственное место, где человек отличает «списали» от «не списали»
— остаток не изменился, бумаги нет, а почему, не сказано нигде.

## Возвраты (блок `orders`, права `orders.*`)

Возврат — вид бумаги (`kind = return`) по проведённому заказу покупателя:
товар назад на склад, деньги назад клиенту. Заводится с карточки заказа
(`POST /orders/{id}/returns`, таблица выше); здесь — всё, что происходит с
заведённым. Разбор решений — [22](../bloki/22-vozvraty.md).

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/returns` | 🔑 `orders.view` | Список: `search`, `status`, `sort`, `client_id`, `order_id`, пагинация; `counts` по состояниям |
| GET | `/returns/{id}/print` | 🔑 `orders.view` | Акт возврата (HTML на печать): перечень, итог по позициям, возвращённые деньги, подписи «Сдал»/«Принял», штрихкод; `?locale=ru|en|uk`. Черновик — `422 return_not_posted`; цены — с правом на суммы |
| GET | `/returns/stats` | 🔑 `orders.view` | Статистика за период (`from`, `to`, `tz_offset`): сколько и на сколько, средний возврат, доля от проведённых заказов покупателя по числу, по месяцам, что возвращают чаще. Деньги — только с `orders.view_amounts` |
| GET | `/returns/{id}` | 🔑 `orders.view` | Возврат: строки, `files`, `order_lines` (что ещё можно вернуть и сколько, `max_milli`), `waybills` (приходная по возврату; ключа нет при выключенном блоке накладных), `events` |
| PATCH | `/returns/{id}` | 🔑 `orders.edit` | Черновик: `note`, `refund` (минорные единицы, ноль — обмен без денег), `client_id`, `category_id` (доходная статья для денег) |
| POST | `/returns/{id}/lines` | 🔑 `orders.edit` | Строка — только товар из заказа: `product_id`, `quantity`. `422 product_not_in_order`, `line_exists` |
| PATCH | `/returns/{id}/lines/{line_id}` | 🔑 `orders.edit` | Количество |
| DELETE | `/returns/{id}/lines/{line_id}` | 🔑 `orders.edit` | Убрать строку |
| POST | `/returns/{id}/files` | 🔑 `orders.edit` | Приложить фото или видео (multipart `file`): jpg, png, webp, gif, mp4, webm, mov. `422 file_type_not_allowed`, `file_content_mismatch`, `file_too_large`, `file_empty`, `disk_full` |
| GET | `/returns/{id}/files/{file_id}/download` | 🔑 `orders.view` | Файл: отдаётся `inline`, тип из расширения |
| DELETE | `/returns/{id}/files/{file_id}` | 🔑 `orders.edit` | Убрать вложение (черновик) |
| POST | `/returns/{id}/post` | 🔑 `orders.issue` | Провести: `warehouse_id`. Товар на склад (приходной или движениями `return`), деньги клиенту минусом по статье. `422 return_is_empty`, `refund_required`, `return_exceeds_shipped`, `refund_needs_category`, `refund_needs_income`; `409 document_status_changed` |
| POST | `/returns/{id}/cancel` | 🔑 `orders.edit` | Отменить черновик: `note` |
| DELETE | `/returns/{id}` | 🔑 `orders.edit` | Удалить черновик вместе с вложениями. Проведённый — `422 document_in_use` |

У любой правки проведённого — `422 return_not_draft`. Возврат живёт в живой
теме `orders` и в журнале действий (`return.posted`, `finance.refund_made`).

## Накладные (блок `waybills`, по умолчанию выключен, зависит от `documents`)

Накладная — вид бланка, поэтому номер, состояния и поиск сканом живут в
`documents`; здесь только то, чего у квитанции нет: склад, основание и позиции.
Устройство и решения — [17](../bloki/17-nakladnye.md).

Два вида: `waybill_out` (отгрузка, остаток падает) и `waybill_in` (приёмка,
остаток растёт). Состояния — `draft` → `issued` → `closed`, и `draft` →
`cancelled`. Правится **только черновик**, и признак приходит полем `pravitsya`
готовым: правило живёт в службе, и экран, вычисляющий его заново по статусу,
завёл бы второй ответ на тот же вопрос.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/waybills` | 🔑 `waybills.view` | Список: `?search=`, `?kind=`, `?status=`, `?client_id=`, `?deal_id=`, `?basis_id=`, пагинация. В строках — `basis_number`, `basis_kind` (основание словами, одним запросом на страницу) |
| POST | `/waybills` | 🔑 `waybills.create` | Черновик: `kind` обязателен, дальше `client_id`, `deal_id`, `basis_id`, `warehouse_id`, `locale`, `note` |
| POST | `/waybills/from-order/{order_id}` | 🔑 `waybills.create` | Черновик, заполненный позициями заказа. Живой черновик по заказу уже есть (зеркало, [21](../bloki/21-svyaz-blokov.md) §1) — отдаёт его с `200`, новый заводит только без него (`201`) |
| GET | `/waybills/{id}` | 🔑 `waybills.view` | Накладная с позициями; `basis_number`, `basis_kind` — заказ, возврат или накладная, по которой выписана (пусто у ручной) |
| GET | `/waybills/{id}/reversals` | 🔑 `waybills.view` | Что выписано на основании этой: сторно |
| POST | `/waybills/{id}/lines` | 🔑 `waybills.edit` | Добавить позицию: `product_id` либо `name`, `quantity`, `price` |
| PATCH | `/waybills/{id}/lines/{line_id}` | 🔑 `waybills.edit` | Количество, цена; название — только у строки без товара |
| DELETE | `/waybills/{id}/lines/{line_id}` | 🔑 `waybills.edit` | Убрать позицию |
| POST | `/waybills/{id}/post` | 🔑 `waybills.issue` | Провести: товар уехал, остаток падает. `{confirm_negative: true}` — согласие отгрузить больше, чем лежит |
| POST | `/waybills/{id}/confirm` | 🔑 `waybills.edit` | Получатель подтвердил приёмку. Склад не двигает |
| POST | `/waybills/{id}/cancel` | 🔑 `waybills.edit` | Отменить черновик. Проведённую — нельзя, для неё сторно |
| DELETE | `/waybills/{id}` | 🔑 `waybills.edit` | Удалить черновик (или отменённую), заведённый по ошибке. Проведённую — нельзя никак (`422 document_in_use`) |
| POST | `/waybills/{id}/reverse` | 🔑 `waybills.issue` | Сторнирующая накладная — черновиком, на основании этой |
| GET | `/waybills/{id}/print` | 🔑 `waybills.view` | Печатная форма (HTML). `?locale=ru|en|uk` — язык бумаги |

Отказы: `not_a_waybill`, `unknown_kind`, `waybill_is_empty`, `waybill_is_final`,
`waybill_not_posted`, `waybill_already_reversed`, `basis_is_not_order`,
`basis_already_shipped`, `order_is_empty`, `line_name_required`,
`bad_line_quantity`, `service_has_no_stock`, `line_not_found`,
`document_status_changed`, `not_enough_stock`.

**Нехватка останавливает проведение, а не предупреждает.** Отгрузить нечего
физически, и отказ `not_enough_stock` перечисляет, чего именно не хватает.
Согласиться уйти в минус можно только явно — `confirm_negative`.

Суммы и цены закрыты правом `waybills.view_amounts`; без него `total` и цены
строк приходят пустыми, а не отсутствуют — тот же приём, что у заказов.

**Печатается только проведённая** (`issued`, `closed`); черновик и отменённая
отвечают `waybill_not_posted`. Черновик правится целиком, и лист с подписью
получателя под перечнем, который назавтра станет другим, обходит то самое
деление, вокруг которого построен весь модуль.

На бумаге столбцы денег **исчезают** без `waybills.view_amounts`, а не пустеют:
пустая колонка «Сумма» под подписью читается как «бесплатно». Та же дыра уже
находилась у наклеек — печать не спрашивала о правах вовсе.

Имя в строке «Отпустил» — снимок из события проведения, а не ссылка на
сотрудника: `created_by` объявлен `SET NULL`, и после увольнения кладовщика
бумага задним числом перестала бы отвечать, кто отдал товар.

## Склад (блок `warehouse`, по умолчанию выключен)

Артикул выдаётся сам (`A-000123`) и обязателен; свой можно назвать при
заведении, отказ — `sku_taken`.

`GET /warehouse/products/{id}/availability` — остаток, бронь, ожидается,
доступно и `holders`: кто именно держит товар (заявки и заказы, каждый со
своим количеством). Бронь считается запросом и не хранится; заказ, заведённый
из заявки, её **перенимает, а не удваивает** — разбор в
[19](../bloki/19-sborka-zakaza.md) §Р3.

Держатели подчиняются области видимости заявок. Сотруднику «только свои заявки»
чужие приходят одной безымянной строкой: `id` и `title` — `null`, количество на
месте. Убрать их совсем нельзя — тогда «в брони 5» стояло бы рядом с пустым
списком, и человек искал бы недостачу, которой нет; назвать их тоже нельзя —
заголовок заявки несёт клиента и суть работы. Заказы не сужаются: своей области
видимости у них нет вовсе.

Роутеров у блока два — `/warehouse` (товары, движения, переезды) и `/warehouses`
(склады как места), — и оба закрыты `require_module("warehouse")`: при
выключенном блоке любой из этих адресов отвечает `403 module_disabled`.

**Остаток не хранится.** Он равен сумме движений и считается запросом, поэтому
колонки «остаток» у товара нет, а `stock_milli` в ответах — всегда результат
агрегата. Количество — целое в тысячных долях единицы (1.25 кг → `1250`), деньги —
целые в минимальных единицах, как везде. У услуги `stock_milli` равен `null`, а
не `0`.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/warehouse/products` | 🔑 `warehouse.view` | Список с остатками. `search`, `low_only` (мало **или** кончилось), `include_services`, `warehouse_id`, `sort` (`name` по умолчанию, `stock` — мало сверху, `stock_desc` — много сверху; по сумме движений на всех складах; незнакомый — `422 unknown_sort`), пагинация. При включённых заказах у товара `sales_30d` — продано проведёнными заказами за 30 дней (одним запросом на страницу). В карточке `low_stock` — остаток на пороге или ниже, `out_of_stock` — ноль или минус независимо от порога; у услуги оба `false` |
| POST | `/warehouse/products` | 🔑 `warehouse.create` | Создать позицию. `sku` уникален, но необязателен (пусто → `NULL`); `409 sku_taken` |
| GET | `/warehouse/products/{id}` | 🔑 `warehouse.view` | Карточка с остатком; при включённых заказах — `reserved_milli`, `expected_milli`, `available_milli`, `sales_30d`/`sales_90d` (количество и заказов по проведённым заказам покупателя), `returns_90d` (по проведённым возвратам); без блока ключей нет |
| GET | `/warehouse/products/{id}/availability` | 🔑 `warehouse.view` | Остаток, бронь, ожидается, доступно и `holders` — кто держит: у каждого `amount` (сумма заявки / заказа; пусто без права на суммы и у чужих), `at` (заведён), `due_at` (срок) |
| PATCH | `/warehouse/products/{id}` | 🔑 `warehouse.edit` | Изменить. Товар с остатком нельзя сделать услугой (`422 product_has_stock`) |
| DELETE | `/warehouse/products/{id}` | 🔑 `warehouse.delete` | Мягкое удаление. Движения остаются |
| POST | `/warehouse/products/{id}/restore` | 🔑 `warehouse.restore` | Вернуть из корзины |
| GET | `/warehouse/products/{id}/photos` | 🔑 `warehouse.view` | Снимки позиции в заданном порядке |
| POST | `/warehouse/products/{id}/photos` | 🔑 `warehouse.edit` | Приложить снимок (форма, поле `file`). Тип по подписи файла, не по расширению; SVG не принимается |
| GET | `/warehouse/products/{id}/photos/{photo_id}` | 🔑 `warehouse.view` | Отдать снимок. `?size=view` (по умолчанию) или `thumb` |
| DELETE | `/warehouse/products/{id}/photos/{photo_id}` | 🔑 `warehouse.edit` | Убрать снимок вместе с файлами |
| PUT | `/warehouse/products/{id}/photos/order` | 🔑 `warehouse.edit` | Задать порядок: `{"order": [id, …]}` целиком |
| GET | `/warehouse/products/{id}/moves` | 🔑 `warehouse.view` | История по товару + `stock_milli` (агрегат, а не сумма страницы). `kind` — один вид движения (`in`, `out`, `return`, `writeoff`, `adjust`), незнакомый — `422 unknown_kind` |
| GET | `/warehouse/moves` | 🔑 `warehouse.view` | Движения. Фильтры `product_id`, `deal_id`, `warehouse_id`; при `deal_id` в ответе `cost` — себестоимость списанного под заявку |
| POST | `/warehouse/moves` | 🔑 `warehouse.create` | Записать движение. Виды: `in`, `out`, `writeoff`, `adjust`, `return` |
| GET | `/warehouses` | 🔑 `warehouse.view` | Склады как места, одним `items` + признак `many` |
| POST | `/warehouses` | 🔑 `warehouse.manage` | Завести склад |
| PATCH | `/warehouses/{id}` | 🔑 `warehouse.manage` | Переименовать, сменить адрес, пометку |
| DELETE | `/warehouses/{id}` | 🔑 `warehouse.manage` | Закрыть склад. Последний и непустой закрыть нельзя |
| GET | `/warehouse/transfers` | 🔑 `warehouse.view` | Журнал переездов. Фильтры `warehouse_id`, `product_id`, пагинация |
| POST | `/warehouse/transfers` | 🔑 `warehouse.create` | Перевезти товар с одного склада на другой |
| POST | `/warehouse/transfers/{id}/revert` | 🔑 `warehouse.create` | Отменить переезд обратным переездом. Дважды один и тот же — отказ |

**Снимок отдаётся через приложение, а не статикой.** Тот же довод, что у файлов
клиента: остаток на складе и то, как выглядит деталь, — сведения фирмы, и ссылка
на них не должна работать у всякого, кто её узнал. Медиа досок отдаётся напрямую
nginx, но там это осознанный обмен — витрину показывают клиенту.

Номер товара в адресе снимка обязателен и участвует в отборе: без него номер
снимка от соседней позиции отдавал бы чужую картинку тому, кто его угадал.

Размеров два, оба делаются при загрузке: `view` — длинная сторона до 1600,
`thumb` — до 320. Пропорции сохраняются (в отличие от аватара, где центральный
квадрат безобиден: у детали он отрежет ровно те края, по которым её узнают).
Показывают везде, где место под один, **первый по порядку** — признака «главная»
нет, его завёл бы инвариант «ровно одна». Порядок задаётся целиком
(`PUT …/photos/order`), а не по одному шагу: частичная перестановка требует
знать, что было до неё, и двое, двигающие соседние снимки, получили бы порядок,
которого не задавал ни один.

**Заводить склады — не то же, что двигать товар.** Приход, расход и перемещение
остаются на `create`: кладовщик двигает товар каждый день. А завести,
переименовать или закрыть склад — решение структурное, вроде «завести юрлицо», и
оно на `warehouse.manage`; склады заводят раз в год.

`many` в ответе `/warehouses` считает сервер, а не экран. Правило «выбор склада
появляется, когда складов больше одного» посчитанное на фронте стало бы вторым
экземпляром того же правила, а два экземпляра расходятся молча. По той же причине
раскладка «где и сколько» (`by_warehouse` у товара) приходит только при
нескольких складах: при единственном она повторяла бы общий остаток.

`warehouse_id` в теле движения необязателен — не прислали, попадёт на основной
склад. Пока склад один, форма о нём вообще не спрашивает.

Переезд отдан **отдельным списком**, а не строками в общей истории: там он
читается плохо — две строки подряд с разными знаками, и понять, одно это событие
или два разных, можно только по времени и по памяти. Здесь он одна запись: что,
сколько, откуда, куда и кто.

**Суммы закрыты отдельным правом.** `warehouse.view_amounts` прячет
себестоимость, поэтому один и тот же ответ у двух сотрудников разный. Право
проверяется и в ответах пишущих ручек тоже: умолчание «показывать суммы» в
сериализаторе делало из `PATCH` обход — кладовщик не видел закупочную цену в
списке и в карточке, но получал её в ответ на переименование товара.

Про `POST /warehouse/moves`:

- Расход и списание всегда уменьшают остаток, какой бы знак ни прислали.
- **Уход в минус разрешён** и помечается флагом `went_negative` в ответе: товар
  часто отдают раньше, чем заносят приход, и запрет заставил бы либо не
  записывать расход, либо выдумывать приход. Обе лжи хуже честного минуса.
- `quantity` принимается строкой и разбирается на сервере через `Decimal`.
  Лишние знаки после запятой отвергаются (`422 quantity_too_precise`), а не
  округляются: молчаливое округление и есть причина, по которой одно и то же
  списывают дважды.
- Нулевое количество — `422 zero_quantity`; движение по услуге — `422
  service_has_no_stock`; несуществующая заявка — `404 deal_not_found`.
- Движение не редактируется и не удаляется: ошибку исправляют обратным движением.

**Нехватка перед отгрузкой считается по ТОВАРУ, а не по строке.** У бумаги
(заказа, акта) минус спрашивается подтверждением — `confirm_negative`, — и
подтверждение это единственное, чем «увести склад в минус» отличается от
«увести случайно». Разбор: один товар в бумаге встречается дважды запросто (две
цены, два комментария, два исполнителя), а каждая строка сверялась с остатком
поодиночке. Две строки по 5 при остатке 6 проходили обе — списать предстояло
10. Остаток уходил в минус молча, мимо подтверждения. Теперь строки одного
товара складываются, и в отказе названа ОБЩАЯ потребность: иначе человек сверяет
и не понимает, почему 6 меньше 5.

## Наклейки (блок `labels`, по умолчанию выключен, зависит от `warehouse`)

Штрихкоды товаров, поиск сканером и печать наклеек. **Сканер здесь ничего не
требует от сервера**: он работает как клавиатура — «печатает» цифры в поле и
жмёт Enter, — поэтому со стороны API это обычный запрос с кодом в адресе, без
единого драйвера и разрешения браузера.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/labels/products/{id}/barcodes` | 🔑 `labels.view` | Коды товара |
| POST | `/labels/products/{id}/barcodes` | 🔑 `labels.create` | Привязать код: `{code, kind, pack_size_milli, is_primary}` |
| POST | `/labels/products/{id}/barcodes/internal` | 🔑 `labels.create` | Выдать собственный код — тому, у чего заводского нет |
| POST | `/labels/products/{id}/barcodes/{barcode_id}/primary` | 🔑 `labels.create` | Какой код печатать на наклейке |
| DELETE | `/labels/products/{id}/barcodes/{barcode_id}` | 🔑 `labels.delete` | Отвязать код |
| GET | `/labels/scan/{code}` | 🔑 `labels.view` | Товар по отсканированному коду. Не нашли — `404` с самим кодом внутри, чтобы экран сказал «код 20000127 не найден» и предложил завести товар прямо отсюда |
| GET | `/labels/print` | 🔑 `labels.view` | **HTML на печать**: `?product_id=` списком, `copies`, `preview`, `locale`. Пустая пачка — `422 nothing_to_print` |
| GET | `/labels/settings` | 🔑 `labels.view` | Размер наклейки и что на ней печатать |

**Действий у области три, а не четыре.** Смотреть и печатать — одно и то же
(печать это и есть просмотр страницы), а `edit` не нужен: код не правят, его
отвязывают и привязывают заново. Зато привязка отделена от просмотра отдельным
правом: ошибочно привязанный чужой код означает, что сканер молча подставляет не
тот товар, и заметить это можно только на инвентаризации.

`pack_size_milli` — сколько единиц товара в этой упаковке, в тысячных: штука
`1000`, блок из десяти `10000`. Отсканировали коробку — в заказ уходит десять штук.

`preview` существует ради ленты: подбирая размер под свой рулон, на страницу надо
смотреть, а каждая примерка «вслепую» стоит намотанной термобумаги, которая не
переиспользуется.

Настройки наклейки отданы своей ручкой, а не куском общих настроек: общие закрыты
`settings.view`, а размер рулона нужен всякому, кто печатает. Отдаёт она поля
**списком в порядке реестра**, а не словарём флагов: порядок и зона нужны экрану
настроек, чтобы нарисовать переключатели так же, как поля лягут на наклейку.

### Что на наклейке: реестр, а не набор `if`

Состав наклейки — то, что у каждого дела своё: мастерской нужна единица
измерения, магазину цена, складу размер упаковки. Угадать набор один раз нельзя,
и дописывать его придётся ещё не раз, — поэтому поля живут одной записью в
`core/services/barcode_service.POLYA_NAKLEYKI`.

| Поле | Зона | По умолчанию | Что это |
|---|---|---|---|
| `name` | над штрихкодом | вкл | название товара |
| `note` | над штрихкодом | выкл | заметка, обрезанная по месту |
| `sku` | строкой | вкл | артикул |
| `unit` | строкой | выкл | единица измерения; штуки не печатаются — штука подразумевается |
| `pack` | строкой | выкл | сколько единиц стоит за этим кодом (`×10`) |
| `min_stock` | строкой | выкл | порог заказа |
| `price` | строкой | выкл | цена — **только тому, у кого есть `warehouse.view_amounts`** |
| `company` | под цифрами | выкл | название фирмы из настроек бренда |
| `printed_at` | под цифрами | выкл | дата печати |
| `qr` | рядом со штрихкодом | выкл | QR на карточку товара |

Всё, кроме названия и артикула, выключено. Место на наклейке — главный дефицит,
и включать за человека что-то значит испортить ему первую же ленту.

**Добавить поле стоит одной записи в реестре**, двух строк перевода и одной
строки в перечне подписей на экране. Больше нигде: настройка сеется по ключу
`label_show_<поле>`, ручка отдаёт флаг сама, шаблон печатает зону списком.
Забыть любую из трёх нельзя — `tests/test_labels.py` краснеет на каждую, и на
подпись поля, которого больше нет, тоже.

**Суммы на наклейке спрашивают право склада.** Это была дыра: печать закрыта
`labels.view` и о суммах не спрашивала вовсе, а цену клала безусловно — то есть
кладовщик без `warehouse.view_amounts` не видел цену на экране и печатал её на
наклейке. Денежные поля помечены в реестре признаком `dengi`, а не проверкой
внутри каждой функции: забыть проверку в новой значит открыть дыру молча, а
забытый признак виден в самой записи.

**QR ведёт в CRM, а не наружу.** Смысл в том, чтобы стоящий у стеллажа навёл
телефон и увидел карточку — остаток, снимки, цену. Постороннему она не откроет
ничего: там вход. При `base_url` на `localhost` QR не печатается вовсе:
телефон по такой ссылке не попадёт никуда, а нерабочий код человек попробует
трижды и решит, что сломан сканер.

### Картинка ужимается, а не вылезает

Штрихкод и QR печатаются с `viewBox`, и это не украшение. Обе библиотеки ставят
SVG только `width` и `height`; пока картинку показывают как есть, этого хватает,
но стоит задать ей размер стилями — рамка ужимается, а рисунок внутри остаётся
прежним и выходит за неё. Обрезанный штрихкод не читается вовсе.

Найдено живым осмотром на двух сразу. QR на рулоне 58×40 вылезал за правый край
на 3,7 мм. А у штрихкода то же самое оказалось **давним**: шаблон обещает
«ширина — во всю наклейку», а полоски всё это время печатались своей природной
шириной 37,4 мм; на рулоне 30 мм это значило выход за край на 8 мм, то есть на
мелких рулонах наклейка не работала никогда.

Единицы `viewBox` — те же, в каких нарисовано СОДЕРЖИМОЕ: python-barcode пишет
миллиметры, и полоски внутри тоже в миллиметрах, а миллиметр в пользовательских
единицах SVG равен 96/25,4 ≈ 3,78. Рамка прямо в миллиметрах дала бы рисунок
ровно в 3,78 раза крупнее — замерено.

Отдельно: **штрихкод с цифрами не уступает места тексту**. Пока обе половины
наклейки были равноправны, десять включённых полей на рулоне 30×20 выдавливали
цифры под нижний край. Теперь текст сверху ужимается и обрезается, а код с
цифрами стоит нерушимо: без них наклейка не значит ничего.

Проверено живьём на всех ходовых рулонах со всеми включёнными полями: 58×40,
58×30, 40×30 и 30×20 — ничего не выходит за край, цифры на месте.

Выключение блока **самих кодов не трогает**: они лежат при товарах и ждут, пока
блок включат обратно.

## Финансы (блок `finance`, по умолчанию выключен)

Роутер закрыт `require_module("finance")` целиком: пропущенный маршрут остался бы
открытым, и выключенный блок продолжал бы отдавать деньги фирмы тому, кто помнит
адрес.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/finance/categories` | 🔑 `finance.view` | Справочник статей |
| POST | `/finance/categories` | 🔑 `finance.manage` | Завести статью |
| PATCH | `/finance/categories/{id}` | 🔑 `finance.manage` | Изменить (направление при правке не принимается) |
| DELETE | `/finance/categories/{id}` | 🔑 `finance.manage` | Убрать статью |
| GET | `/finance/operations` | 🔑 `finance.view` | Операции за период |
| GET | `/finance/operations.csv` | 🔑 `finance.view` | Тот же отбор файлом целиком (страницами по 200, до 50 страниц): дата, статья, направление, сумма, комментарий, автор; BOM и имя файла по периоду — как у выгрузок отчётов |
| POST | `/finance/operations` | 🔑 `finance.create` | Записать операцию |
| GET | `/finance/rules` | 🔑 `finance.view` | Правила разнесения |
| POST | `/finance/rules` | 🔑 `finance.manage` | Завести правило |
| PATCH | `/finance/rules/{id}` | 🔑 `finance.manage` | Изменить правило |
| DELETE | `/finance/rules/{id}` | 🔑 `finance.manage` | Убрать правило |
| POST | `/finance/payments` | 🔑 `finance.create` | Принять оплату или вернуть её — решает знак суммы |
| PATCH | `/finance/accruals/{operation_id}` | 🔑 `finance.create` | Поправить сумму начисления: было 80, стало 140 |
| GET | `/finance/documents/{id}/money` | 🔑 `finance.view` | Деньги по бланку: получено, остаток, состояние, начисления; `refunded` — отдано обратно по возвратам заказа («получено» не уменьшает). У возврата `total` — сумма к возврату, `refunded` — сколько отдано, `paid` — рассчитан |
| GET | `/finance/deals/{id}/money` | 🔑 `finance.view` | Сколько получено по заявке |
| GET | `/finance/profit` | 🔑 `finance.view` | Доход минус расход за период, с разбивкой по статьям |
| GET | `/finance/budgets` | 🔑 `finance.view` | Планы и факт по ним |
| POST | `/finance/budgets` | 🔑 `finance.manage` | Завести план |
| PATCH | `/finance/budgets/{id}` | 🔑 `finance.manage` | Изменить план |
| DELETE | `/finance/budgets/{id}` | 🔑 `finance.manage` | Убрать план |

**Пустой остаток к оплате означает две разные вещи, и различает их `status`.**
`GET /finance/documents/{id}/money` отдаёт `due: null` в двух случаях: блок
бланков выключен (тогда и `status: null` — суммы нет, сравнивать не с чем) и
бланк отменён (`status: "cancelled"`). У отменённого «сколько ещё с человека
взять» не равно нулю и не равно сумме — **вопрос не задан**; раньше здесь честно
считалось «сумма минус полученное», и у отменённого заказа с возвращёнными
деньгами получалась полная сумма заказа, то есть долг, которого нет. Ноль на его
месте был бы враньём той же природы, только обратным: он читается как
«рассчитались».

То же и с `paid`: у отменённого нет ни «оплачен», ни «не оплачен». Что на самом
деле с деньгами, говорит `received`, и отмена его не трогает — **отмена бумаги и
возврат денег разные действия**, и сделать можно только одно из двух. Экран
заказа этим и пользуется: при непустом `received` у отменённого он показывает «К
возврату», а не «платить нечего».

**Правка статьи частичная.** `PATCH /finance/categories/{id}` принимает любое
подмножество полей — как и правка правила. Незаполненное означает «не меняй».
Направление в теле принимается, но сменить его нельзя: отказ `direction_is_fixed`
внятнее, чем «лишнее поле».

**Операцию не правят и не удаляют — ручек нет и не будет.** Ошибку исправляют
обратной операцией; поправка начисления заводит корректирующую запись на разницу
и той же датой, а исходная остаётся на месте. Поэтому в реестре прав у области
только три действия: `view`, `create`, `manage`. `edit` и `delete` не объявлены —
право, которое ничего не даёт, хуже его отсутствия, тот же довод, что у журнала
действий.

**`view_amounts` здесь нет и быть не может.** У заявок и склада суммы отделены от
раздела, потому что «ведёт заявку, но не видит маржу» — осмысленная просьба.
Финансы это деньги целиком: раздел без сумм — пустая таблица, поэтому `view` и
есть право на деньги.

`manage` — распоряжаться справочником статей и планами: решение структурное,
вроде «завести юрлицо» или «завести склад». Расходы заносят каждый день, а статьи
заводят раз в год.

Ни одна из этих ручек не складывает операции сама: прибыль, факт по бюджету и
деньги бланка приходят из агрегатов репозитория. Колонок `paid_total`, `paid_at`
и `is_paid` в базе нет — все четыре числа рождаются заново на каждый запрос, и
отдельного маршрута «пересчитать» нет и быть не может: пересчитывать нечего.

## Отчёты (блок `reports`)

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/reports/funnel` | 🔑 `reports.view` | Воронка за период |
| GET | `/reports/funnel.csv` | 🔑 `reports.view` | Она же выгрузкой |
| GET | `/reports/revenue` | 🔑 `reports.view_amounts` | Деньги за период: `received_*` (пришло в кассу) и `won_*` (сумма выигранных заявок), плюс `basis` — чем меряем |
| GET | `/reports/revenue.csv` | 🔑 `reports.view_amounts` | Она же выгрузкой |
| GET | `/reports/sources` | 🔑 `reports.view` | Откуда пришли клиенты. Деньги в нём прячет `reports.view_amounts` |
| GET | `/reports/debts` | 🔑 `reports.view_amounts` | Долги клиентов: бумаги (заказы покупателя, акты), по которым получено меньше выписанного — `items` (номер, вид, клиент, сумма, получено, остаток), `total_due`, `count`; без периода. Нужны блоки `finance` и `documents` |
| GET | `/reports/debts.csv` | 🔑 `reports.view_amounts` | Он же выгрузкой |
| GET | `/reports/sources.csv` | 🔑 `reports.view` | Он же выгрузкой |

Период у всех отчётов общий и приходит зависимостью `Period`: `from`, `to`
(даты) и `tz_offset` (минуты из `Date#getTimezoneOffset()`). Датами, а не
готовыми метками времени, чтобы ссылка на выгрузку осталась обычным адресом,
который можно положить в `href` и переслать; границы дня при этом считаются по
местному календарю. Одним классом-зависимостью, а не пятью одинаковыми
аргументами в каждом роуте: разъехавшиеся умолчания у трёх отчётов на одном
экране — это три разных периода под одной подписью. В каждом ответе период и
валюта повторяются: отчёт без подписи периода — просто набор чисел, распечатку
потом не соотнести ни с чем.

**В отчёте о выручке два числа про деньги, и они разные.** `received_*` — сколько
пришло в кассу; по решению владельца («банк один») это и есть выручка, и только
так виден заказ, оплаченный мимо заявки. `won_*` — сумма выигранных заявок:
величина полезная, но деньгами не являющаяся. Чем меряется выручка сейчас,
говорит `basis`: `cash` при включённом блоке «Финансы», `deals` при выключённом —
тогда операций не существует вовсе, и `received_*` приходят **пустыми, а не
нулевыми** («блока нет» и «денег не приходило» — разные ответы). Экран берёт ось
из `basis`, а не из своей карты блоков. Разбор правила — в
[01-obzor.md](01-obzor.md).

Выгрузка `revenue.csv` следует за этим: колонка «Выручка» переименована в
«Выиграно на сумму», а «Получено» добавлена **последней** — уже сложившиеся у
людей таблицы поверх этой выгрузки не должны поехать столбцами. При выключённой
кассе ячейка пустая, а не «0,00».

**У отчёта о выручке своё право.** Это деньги целиком, а не отчёт, среди прочего
содержащий суммы; прятать в нём числа значило бы отдавать пустую таблицу, честнее
закрыть его тем же правом, что и суммы. С переходом на кассу охват этого права
фактически расширился: под ним теперь и деньги фирмы, а не только суммы заявок. У отчёта по источникам иначе — там без
денег остаётся осмысленная картина, поэтому он открыт по `reports.view`, а суммы
из него вынимаются.

`deals.view_others` решает, ЧЬИ заявки попадают в отчёт, а не только какие
карточки видно на экране. Без этого запрет декоративен: заявок в списке три, а в
отчёте оборот всей фирмы — и узнать его как раз и было целью.

Выгрузка обязана совпадать с экраном: иначе право обходится кнопкой «скачать», а
это самый вероятный обход из всех. Имя файла несёт период — в папке «Загрузки»
через месяц лежит пять выгрузок, и «reports.csv (3)» не отвечает, какая из них за
июль.

## Телефония (блок `telephony`)

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/telephony/calls` | 🔑 `telephony.view` | Журнал: `?direction=in\|out`, `?outcome=`, `?client_id=`, `?deal_id=`, `?user_id=`, `?number=` (приводится к нормализованному виду), `?since=`, `?until=`, пагинация. Незнакомое значение `direction`/`outcome` — `422`, а не пустая выдача, неотличимая от «звонков нет» |
| GET | `/telephony/calls/{id}` | 🔑 `telephony.view` | Карточка звонка |
| PATCH | `/telephony/calls/{id}` | 🔑 `telephony.edit` | Привязать разговор к заявке или отвязать: `{"deal_id": 7 \| null}`. `422 deal_other_client` — заявка чужого клиента |
| POST | `/telephony/calls/{id}/callback-task` | 🔑 `telephony.create` | Напоминание перезвонить по пропущенному. `422 call_not_missed`, `409 module_disabled` — блок напоминаний выключен |
| POST | `/telephony/click-to-call` | 🔑 `telephony.create` | Просит АТС набрать: `{"number": "…", "from_ext": "…", "deal_id": 7}`. `422 telephony_not_configured`, `400 pbx_unavailable`, `409 pbx_call_id_taken` — станция выдала ключ, который уже занят прошлым разговором |
| GET | `/telephony/settings` | 🔑 `settings.manage` | Настройки подключения. Секреты не отдаются — только `has_api_token` / `has_webhook_secret` |
| PATCH | `/telephony/settings` | 🔑 `settings.manage` | Провайдер, адрес команды набора, токен, внутренний номер, смещение зоны АТС, код страны. `422 bad_telephony_url` — адрес станции не http(s) или слишком длинный |
| POST | `/telephony/settings/secret` | 🔑 `settings.manage` | Новый секрет подписи вебхука; возвращается **один раз** |
| POST | `/telephony/webhook` | 🔓 | События звонка от АТС. Подпись обязательна (см. [07-bezopasnost.md](../ekspluatatsiya/07-bezopasnost.md)). Отказы, в порядке проверок: `429 webhook_rate_limited` (600 в минуту с адреса, ДО сверки подписи), `401 webhook_not_configured` (секрет не задан — приём выключен), `401 bad_signature`, `422 bad_payload` (не JSON или не объект). Выключенный блок отвечает `200 {"status": "ignored", "reason": "module_disabled"}` — станции незачем повторять то, что мы не примем никогда |

Поля звонка: `duration_sec = null` — длительность неизвестна (не то же, что `0`),
`outcome = null` — звонок ещё идёт. `counterparty` — номер собеседника: у входящего это
звонивший, у исходящего — вызываемый.

Формат события вебхука: `call_id` (обязателен), `direction`, `from`, `to`, `started_at`
(ISO 8601, лучше со смещением), `duration`, `outcome`, `recording`, `operator_email`.
Клиента в теле не спрашиваем и не принимаем — он определяется по номеру.

Вебхук **не закрыт блоком**: у АТС нет способа узнать, что владелец выключил телефонию,
и на ошибку она уходит в ретраи. При выключенном блоке запрос принимается (`200`) с телом
`{"status": "ignored", "reason": "module_disabled"}` и ничего не пишет; подпись
проверяется в любом случае.

## Телеграм: переписка с клиентами (блок `telegram`)

Один бот фирмы, общение только один на один. **Клиент обязан начать разговор
сам** — телеграм не позволяет боту написать первым тому, кто его не запускал;
отсюда ручка приглашения со ссылкой и QR-кодом.

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/telegram/settings` | 🔑 `telegram.manage` | Состояние подключения. Токен НЕ отдаётся — только `token_tail` из четырёх знаков и признаки `configured` / `webhook_secret_set`. Чаты отдаются как есть: секрета в идентификаторе нет |
| PUT | `/telegram/settings` | 🔑 `telegram.manage` | Токен, имя бота, чат утренней сводки (`digest_chat`), чат тревог о сервере (`alerts_chat`), `retention_months` — сколько месяцев хранить переписку (`0` — вечно, и это умолчание; числом, а не строкой). Пустой токен означает «не меняй»: экран настоящего не знает и вернуть не может. `422 bad_bot_token`, `422 bad_chat_id` |
| DELETE | `/telegram/settings` | 🔑 `telegram.manage` | Отключить бота: снимает вебхук у телеграма и стирает токен с секретом. **Переписка остаётся** — отключение про связь, а не про данные |
| POST | `/telegram/connect` | 🔑 `telegram.manage` | Сказать телеграму, куда доставлять (`setWebhook`). Отдельно от сохранения токена: адрес зависит от того, как сайт виден снаружи, и меняется при переезде. `422 telegram_needs_https` — телеграм принимает вебхук только по HTTPS |
| GET | `/telegram/invite` | 🔑 `telegram.view` | Ссылка `t.me/имя?start=метка` и QR-код к ней. `?label=` — только буквы, цифры, дефис и подчёркивание (требование телеграма). `422 telegram_username_missing` |
| GET | `/telegram/chats` | 🔑 `telegram.view` | Диалоги, свежие сверху: `?q=`, `?source=` (метка из ссылки, точным совпадением, а не подстрокой), пагинация. У каждого `unread` — **личный** счётчик непрочитанного, `has_avatar` (стоит ли просить картинку) и `is_premium` |
| GET | `/telegram/sources` | 🔑 `telegram.view` | Какие метки источника встречаются и по сколько диалогов на каждой. Без него отбор по метке пришлось бы предлагать полем ввода, то есть предлагать угадывать |
| PATCH | `/telegram/chats/{id}` | 🔑 `telegram.create` | Привязать диалог к карточке клиента или отвязать: `{"client_id": 7 \| null}`. Только руками: автоматически связывается лишь точное совпадение номера |
| POST | `/telegram/chats/{id}/deal` | 🔑 `deals.create` | Завести заявку по переписке. Диалог обязан быть привязан к карточке — `422 telegram_chat_not_linked`, заявка без клиента бессмысленна. Пустое название берётся из последнего входящего |
| POST | `/telegram/chats/{id}/task` | 🔑 `tasks.create` | Завести напоминание по переписке. Привязки к карточке НЕ требует: «перезвонить этому человеку» осмысленно и до того, как выяснили, кто он. Привязан диалог — клиент подставится |
| POST | `/telegram/chats/{id}/client` | 🔑 `clients.create` | Завести карточку клиента по переписке и сразу её привязать. Тела у запроса нет: всё нужное уже знает диалог, а форма посреди разговора означала бы, что кнопкой перестанут пользоваться. Ответ несёт `created` — карточку могли не завести, а привязать к найденной по точному совпадению номера |
| POST | `/telegram/chats/{id}/client/refresh` | 🔑 `clients.edit` | Перенести в карточку то, что телеграм узнал о человеке позже. Заполняется **только пустое**. Ответ несёт `updated` — список перенесённых полей; пустой список законен и отказом не является |
| GET | `/telegram/chats/{id}/messages` | 🔑 `telegram.view` | Лента. `?before=` — листание вглубь по идентификатору (не по смещению: на живой переписке оно врёт), `?after=` — дочитать пропущенное после обрыва. Показ ленты сдвигает границу «прочитано» |
| POST | `/telegram/chats/{id}/messages` | 🔑 `telegram.create` | Ответить текстом: `{text, reply_to_id?}` — ответ на конкретное сообщение нашим номером строки, телеграмовы наружу не отдаются вовсе. Строка заводится ДО обращения к телеграму, поэтому двойное нажатие не отправляет дважды. Отказ телеграма не откатывает запись: `send_state=failed` и причина в `send_error` |
| POST | `/telegram/chats/{id}/files` | 🔑 `telegram.create` | Ответить файлом: картинка, видео или документ. Форма, а не JSON: `file`, `caption`, `reply_to_id` (пустая строка — «без привязки»: `null` в форму не положить) |
| GET | `/telegram/chats/{id}/messages/{msg}/file` | 🔑 `telegram.view` | Отдать вложение. Через приложение, а не статикой: переписка с клиентом не публичная картинка, и ссылка не должна работать у всякого, кто её узнал |
| POST | `/telegram/chats/{id}/messages/{msg}/fetch` | 🔑 `telegram.view` | Забрать видео, которое сразу не тянули. Идемпотентно |
| POST | `/telegram/chats/{id}/read` | 🔑 `telegram.view` | «Я это увидел»: сдвигает личную границу прочитанного. Нужна потому, что дочитывание (`?after=`) границу НЕ двигает — оно приносит сообщения и в свёрнутую вкладку. Различает эти два случая только браузер |
| GET | `/telegram/chats/{id}/avatar` | 🔑 `telegram.view` | Фотография профиля собеседника. Освежается по требованию, но не чаще раза в сутки: аватар не приходит с сообщением, за ним надо ходить отдельно. `404 telegram_no_avatar` — обычное дело, а не отказ: экран рисует инициалы |
| GET | `/telegram/chats/{id}/emoji` | 🔑 `telegram.view` | Именной эмодзи собеседника — статичной картинкой: сам эмодзи Lottie, и браузер его не рисует. Забирается при приёме сообщения, не чаще раза в сутки и только у премиума. `404 telegram_no_emoji` — как и у аватара, обычное дело, а не отказ |
| POST | `/telegram/chats/{id}/presence` | 🔑 `telegram.view` | «Я в этом чате» / «ушёл». Отвечает списком смотрящих. Ключ со сроком годности в Redis — уборки не требует |
| GET | `/telegram/stream` | 🔑 `telegram.view` | Поток событий (SSE): новые сообщения и смена присутствия. Соединение живёт пять минут, дальше браузер переподключается сам. Первым сообщением — `{"type": "ready", "bus": …}`; ложь означает недоступный Redis, то есть событий не будет ни одного при живом соединении, и экран переходит на запасное перечитывание |
| POST | `/telegram/digest/send` | 🔑 `telegram.manage` | Отправить утреннюю сводку прямо сейчас — посмотреть оформление, не дожидаясь утра. Отвечает состоянием, а не отказом: `{"status": "sent"}`, `{"status": "skipped", "reason": …}` (бот не настроен или блок выключен), `{"status": "failed", "error": …}` — причина от телеграма дословно. Ненастроенный канал не отказ ручки: экран могли открыть раньше, чем ввели токен |
| POST | `/telegram/webhook` | 🔓 | Обновление от телеграма. Секрет заголовком `X-Telegram-Bot-Api-Secret-Token`; сверка постоянным временем, ограничитель ДО сверки. Отказы: `422 telegram_not_configured` (бот не подключён), `429 webhook_flooded` (600 в минуту с адреса), `401 bad_webhook_secret`. `503 limiter_unavailable` — Redis не отвечает; телеграм повторит доставку, и это правильно |

**Действий у области три: `view`, `create`, `manage`** — и каждое отсутствующее
отсутствует намеренно. `edit` и `delete` не объявлены: отправленное сообщение не
правят и не удаляют, клиент его уже прочитал, и «исправить» переписку задним
числом значило бы завести способ подделать доказательство; ошиблись — пишут
следом, как в жизни. `view_others` нет, потому что у диалога нет владельца:
закрепление и ответственный отменены, конфликты показываются баннером, люди
договариваются сами — фильтровать такому праву нечего. `manage` — настройки бота:
писать клиентам приходится каждый день, а подключать бота раз и навсегда.

Заведение заявки, напоминания и карточки спрашивают право **на то, что
заводится, а не на телеграм**. Иначе право читать переписку тихо давало бы право
заводить работу и карточки.

Срок хранения переписки — та самая настройка `retention_months`; старое убирает
`core/services/telegram_uborka.py`. Ноль означает «вечно» и стоит по умолчанию:
молчаливая уборка переписки с клиентом хуже разросшейся базы.

Приём отвечает **200 почти всегда**: любой иной ответ телеграм считает
недоставкой и повторяет часами. Отказом отвечаем ровно на неверный секрет.

## Заявки с сайта

| Метод | Путь | Права | Описание |
|---|---|---|---|
| POST | `/public/leads` | 🔓 | Заявка с формы на сайте. Ключ приёма — в заголовке `X-OpenCRM-Intake-Key`. Ответ на принятую заявку всегда один — `202 {"status": "accepted"}`: ни «завели», ни «узнали своего», ни ловушка не отличимы снаружи. Отказы, в порядке проверок: `429 lead_rate_limited` (30 обращений за 10 минут с адреса — считается ДО ключа, иначе подбирающий ключ не считался бы), `401 bad_intake_key` (ключа нет или он не тот — один ответ на оба случая), `422 contact_required` (ни почты, ни телефона), `422 bad_email` / `email_too_long` / `phone_too_long` (адрес и номер не обрезаются, а отвергаются: обрезанный адрес — другой адрес), `429 lead_intake_flooded` (потолок новых карточек в час), `409 no_responsible` (заявку некому отдать: ответственный не назван, а root удалён). `503 limiter_unavailable` — Redis не отвечает, и ограничитель отказывает, а не пропускает |
| GET | `/leads/settings` | 🔑 `settings.manage` | Состояние приёма. Ключ **не отдаётся**: только `has_intake_key` |
| POST | `/leads/settings/key` | 🔑 `settings.manage` | Новый ключ. Возвращается **один раз**: `201 {"key": "…"}`. Прежний перестаёт работать в ту же секунду |
| DELETE | `/leads/settings/key` | 🔑 `settings.manage` | Закрыть приём: без ключа публичная ручка отвечает как несуществующая |
| PATCH | `/leads/settings/manager` | 🔑 `settings.manage` | Кому достаются заявки. `{"manager_id": null}` — владельцу системы. `404 manager_not_found` — такого сотрудника нет |

Три последние отвечают тем же телом, что и `GET`, — экран не перезапрашивает
состояние после каждого действия.

Состав ответа `GET /leads/settings`:

| Поле | Что значит |
|---|---|
| `has_intake_key` | Задан ли ключ. Ложь — приёма нет вовсе |
| `chosen_id`, `chosen_name` | Кто **выбран** в настройке. `null` — никто |
| `manager_id`, `manager_name` | Кто получит заявку **сейчас** |
| `intake_path`, `intake_header` | Куда и с каким заголовком шлёт сайт |
| `intake_fields`, `honeypot_field` | Состав тела и имя ловушки |

Пара `chosen_*` и `manager_*` расходится ровно в одном случае — назначенного
отключили, и заявки достаются владельцу системы. Отдай ручка только
действующего, экран показывал бы владельца выбранным (враньё про настройку);
только выбранного — умалчивал бы, что заявки уходят не ему. Обе половины вместе
дают экрану сказать это вслух.

`intake_fields` и `honeypot_field` отдаются, потому что по ним экран настроек
рисует готовую команду для того, кто пишет форму на сайте. Набери их разметка
экрана сама — и разъехавшаяся инструкция была бы хуже отсутствующей: по ней
делают форму, форма не работает, виноватым выглядит продукт. Расхождение с телом
запроса ловит `tests/test_leads.py`.

Тело: `name`, `email`, `phone`, `message`, `website`. Обязательного поля как такового
нет, но **почта или телефон нужны хотя бы одно** — иначе `422 contact_required`:
заявка, по которой не с кем связаться, даёт пустую строку в справочнике.

`website` — **ловушка**. В форме поле скрывается стилями: человек его не видит и не
заполняет, бот заполняет всё подряд. Заполненное поле означает, что заявка не
записывается вовсе, а ответ при этом выглядит как успех — скажи мы «отказано»,
подбирающий за десяток попыток вычислил бы лишнее поле и перестал его трогать.

Что происходит с принятой заявкой:

- клиент ищется по присланному адресу (сравнение через `lower()`, целиком — `_` и `%`
  в адресе это знаки, а не шаблон) и по нормализованному номеру (`phone_norm`, то есть
  `067…` находит карточку с `+380 67…`). Нашёлся — заявка цепляется к нему, **второй
  карточки не будет**; известную карточку форма при этом не правит: знание чужого
  номера не должно давать права переписать в ней почту;
- не нашёлся — заводится карточка с меткой `lead` и источником `site` (отдельной
  колонки «статус» в системе нет; метка фильтруется списком клиентов, источник
  считается отчётом по источникам);
- заводится заявка на **первом открытом этапе воронки** — на том, что настроен у
  бизнеса, а не на зашитом;
- ответственный получает напоминание (блок `tasks`; выключен — заявка принимается
  без напоминания). Кто ответственный — настройка `leads_manager_id`; не задана или
  сотрудник отключён — заявки достаются владельцу системы.

Ответ **одинаковый всегда**: ни идентификатора клиента, ни номера заявки, ни слова о
том, завели карточку или узнали своего. Иначе по разнице ответов кто угодно проверял
бы, есть ли такой человек в базе.

Отказы: `401 intake_not_configured` — ключ приёма не задан, приёма нет вовсе (так
выглядит свежая установка); `401 bad_intake_key`; `413 body_too_large` — тело больше
мегабайта, отсекается по `Content-Length` **до чтения**; `422 bad_email`,
`422 email_too_long`, `422 phone_too_long` — адрес и номер не обрезаются, обрезанный
адрес это другой адрес; `429 lead_rate_limited` — слишком часто с одного адреса
(30 за 10 минут, считаются все обращения, а не одни отказы);
`429 lead_intake_flooded` — форма завела больше 60 новых карточек за час.

Повтор той же заявки в течение 5 минут (двойное нажатие, повторная доставка с сайта)
цепляется к уже заведённой работе: ни второй карточки на доске, ни второго напоминания.

**Заявку шлёт сервер сайта, а не страница в браузере.** Ключ, вставленный в HTML,
знает каждый посетитель; и принимается только JSON — тело другого типа проходит мимо
потолка размера. Ключ здесь не подпись: он закрывает ручку от сканеров и даёт способ
отозвать доступ, но не доказывает, что тело не подменили (у формы нет места, где
хранить секрет, — в отличие от АТС). От потока защищают ограничитель и потолки.

Настройки блока (`site_settings`, в общий `GET /settings` не входят): `leads_intake_key`
— сам ключ, `leads_manager_id` — ответственный. Правят их ручки выше, а показывает
экран «Настройки → Заявки с сайта» (`web/frontend/crm/src/screens/LeadsSettings.tsx`):
состояние приёма, создание ключа, выбор ответственного и готовая команда с
подставленным свежим ключом.

Карточка, заведённая формой, получает источник `site` — он же есть в справочнике
источников (`database/models/client.py`), поэтому в карточке и в отчёте виден
названием, а не ключом, и выбирается руками: человек, который посмотрел сайт и
позвонил, пришёл оттуда же, а форму при этом не заполнял.

## API сайта магазина (`/site/*`, по ключу)

Отдельная поверхность для чужих программ: сайт магазина, витрина, маркетплейс,
лендинг. Всё под `/api/v1/site/`, сессии сотрудника нет — вместо неё **ключ с
областями** в заголовке `X-OpenCRM-Api-Key`. Устройство и доводы —
[16-api-sayta.md](../ustroystvo/16-api-sayta.md) (что сделано и чем отличается от замысла — его §17);
здесь — справочник для того, кто пишет сайт.

### Как это устроено в двух словах

- **Запрос шлёт сервер сайта, а не страница в браузере.** Ключ в HTML знает
  каждый посетитель. Единственное, что тянет браузер, — снимок товара, и у него
  адрес без ключа (`/media/product/…`).
- **Наружу уходит только то, что нельзя спрятать правом:** каталог, наличие
  склада, названного в ключе, свои заказы, регистрация клиента, заявка. Закупочной
  цены, штрихкодов, чужих заказов, остатков подсобки и карантина нет ни при каком
  ключе — это отсутствующие поля, а не настройка.
- **Ключ не может больше блока.** Выключен склад — `catalog.read` и `stock.read`
  отвечают `403 module_disabled`, что бы ни стояло в ключе. Порядок проверок: блок
  включён → есть область.
- **Наличие — мнение с датой, правда устанавливается при заказе.** Ответ о
  наличии несёт `as_of` и `ttl_sec`; `POST /site/orders` пересчитывает заново и
  отказывает числами, если не хватает.
- **Ничего не хранится дважды.** Остаток, бронь и «доступно» считаются запросом;
  отменённый заказ освобождает товар сам.

### Ключ и области

Ключ выдаётся на экране «Настройки → Ключи API сайта» или консолью
`./opencrm.sh apikey new --name … --scopes …`. Строка ключа показывается **один
раз** — в базе лежит отпечаток, и «показать ещё раз» не существует: потеряли —
выпустите новый. У ключа есть имя, видимая приставка (первые восемь знаков —
сличить строку в конфиге сайта с записью в CRM), срок (365 дней по умолчанию,
`0` — бессрочный, такие в списке красным), потолок запросов в минуту (120),
потолок срока брони (1440 минут), режим наличия, порог «мало» и `ttl_sec` (60).

| Область | Что открывает | Блок |
|---|---|---|
| `catalog.read` | карточки: артикул, имя, единица, описание, цены, снимки | `warehouse` |
| `stock.read` | наличие по складу ключа, в режиме ключа | `warehouse` |
| `orders.write` | создать заказ (занять товар) и отменить его | `orders` |
| `orders.read` | состояние заказов, созданных **этим же** ключом | `orders` |
| `customers.write` | завести карточку клиента или узнать свою | — |
| `leads.write` | принять заявку с формы | — |

`stock.read` требует склад **типа «магазин»** (`warehouses.kind = shop`): сайту
виден остаток ровно этого склада. Ключ без склада законен и отдаёт одни услуги —
прайс сферы услуг, у которой нет полки. Закрыли склад или сменили его тип —
ключ не переводится на соседний молча, а отвечает `409 warehouse_not_serving`.

**Ротация** (`rotate`) выпускает новый ключ с теми же полями и оставляет старый
жить ещё сутки, чтобы сайт успели переключить. **Отзыв** — отметка, строка
остаётся в списке серым: «а был ли у нас ключ для маркетплейса» должно иметь
ответ. Выдача, отзыв, ротация и правка пишутся в журнал действий
(`apikey.*`); сами обращения — нет, для них есть «последнее обращение» и адрес.

### Соглашения

- Пагинация `?page=&per_page=`, ответ `items`/`total`/`page`/`per_page`.
- Даты — ISO 8601 с `Z`. Деньги — целые в минорных единицах в полях `*_minor`,
  валюта одна на систему и приходит полем `currency`.
- Количества **в ответах** — целые в тысячных (`*_milli`: 1500 = 1,5),
  **в запросах** — строкой, которую разбирает `Decimal` (`"1.5"`); лишние знаки
  → `422 quantity_too_precise`. Сервер не округляет чужое число молча.
- Отказ всегда одной формы: `{"error": {"code": …, "message": …, "details": …}}`;
  `details` только там, где отказ требует выбора (нехватка, неизвестный товар).
- `GET /catalog` и `GET /catalog/{id}` отдают `ETag` и понимают `If-None-Match`
  → `304`. Это добавка на редко меняющихся ручках, а не механизм синхронизации —
  для него есть лента `/changes`.

### Общие отказы

| Код | HTTP | Когда |
|---|---|---|
| `bad_api_key` | 401 | ключа нет, он не ASCII или отпечаток не найден — один ответ на все три случая |
| `api_key_expired` | 401 | срок вышел |
| `api_key_revoked` | 401 | ключ отозван |
| `module_disabled` | 403 | блок выключен; проверяется **до** области |
| `scope_required` | 403 | у ключа нет области; её имя — в `message` |
| `warehouse_not_serving` | 409 | склад ключа закрыт, перестал быть магазином или не назван, а нужен |
| `rate_limited` | 429 | потолок ключа в минуту исчерпан; промахи неизвестным ключом — 30 за 10 минут с адреса |
| `limiter_unavailable` | 503 | Redis не отвечает. **Показывай кэш**, а не пустую полку |
| `body_too_large` | 413 | тело больше 1 МБ, отвергнуто до чтения |

### Чтение

| Метод | Путь | Область | Описание |
|---|---|---|---|
| GET | `/site/catalog` | `catalog.read` | Страница карточек, `?page=&per_page=` (до 200). Виден товар, по складу ключа было хоть одно движение, и все услуги; ключ без склада — одни услуги |
| GET | `/site/catalog/{product_id}` | `catalog.read` | Одна карточка; `404 product_not_found` — не опубликован или удалён |
| GET | `/site/changes` | `catalog.read` | Лента изменений: `?since=<курсор>&limit=<до 200>`. Без `since` — полная выгрузка |
| GET | `/site/stock` | `stock.read` | Наличие: `?id=17,42` либо `?sku=A,B`, до 200 позиций (`422 too_many_ids`) |

**Карточка** (`GET /site/catalog`):

```json
{
  "items": [{
    "id": 17,
    "sku": "ABC-1",
    "name": "Matrix 15.6\" FHD",
    "unit": "pcs",
    "description": "Матовая, 30 pin, разъём слева.",
    "is_service": false,
    "prices": [{"list": "default", "price_minor": 12500}],
    "pack_size_milli": 1000,
    "photos": [
      {"url": "/media/product/9f2c….webp", "thumb_url": "/media/product/9f2c…-thumb.webp"}
    ],
    "updated_at": "2026-09-04T10:12:03Z"
  }],
  "total": 132, "page": 1, "per_page": 100,
  "currency": "USD"
}
```

- `sku` пустым не бывает — артикул обязателен и уникален, товар адресуется и
  `id`, и `sku`.
- `description` — поле «описание для сайта» карточки товара, просто текст без
  разметки; заметка кладовщика наружу не уходит никогда.
- `prices` — массив с одного дня: сегодня список один, второй прайс не сломает
  читателей. **Пустой массив — цены нет:** карточка на сайте остаётся, кнопки
  «купить» у неё быть не должно, а заказ на такой товар отвергается
  `422 price_not_set`.
- `pack_size_milli` — кратность упаковки основного штрихкода; сам код не
  отдаётся.
- Распроданный до нуля товар карточку **сохраняет** и показывает «нет в
  наличии»: иначе адрес страницы умирал бы в момент продажи последней штуки.

**Наличие** (`GET /site/stock?id=17,42,99`):

```json
{
  "as_of": "2026-09-04T14:32:11Z",
  "ttl_sec": 60,
  "recheck_after": "2026-09-04T14:47:00Z",
  "items": [
    {"id": 17, "sku": "ABC-1", "unit": "pcs", "state": "many", "available_milli": 12000},
    {"id": 42, "sku": "ABC-2", "unit": "pcs", "state": "none", "available_milli": 0},
    {"id": 99, "sku": "A-000099", "unit": "hour", "state": "always"}
  ]
}
```

- `available = остаток магазинного склада − вся бронь` (открытые заказы и
  открытые заявки по всей системе; резерв по складам не режется намеренно —
  разбор в `16-api-sayta.md` §4). Ошибка, если и есть, — в безопасную сторону:
  сайт покажет меньше, чем в зале, и никогда больше.
- Точность задаёт **ключ**, а не запрос: `exact` — число `available_milli` и
  `state`; `bucket` — только `state` из `many` / `few` / `none`; `boolean` —
  только `many` / `none`. При `bucket` и `boolean` поля `available_milli` **нет
  вовсе**, а не `null`. Порог `few` — `min_stock_milli` товара, если задан, иначе
  порог ключа; наружу порог не сообщается.
- `always` — услуга: остатка у неё не бывает.
- `recheck_after` — ближайший срок истечения брони среди открытых заказов:
  секунда, в которую картинка устареет сама. Позиции, которых на складе ключа
  нет (не опубликованы), в ответе опускаются.

**Лента изменений** (`GET /site/changes`):

```json
{
  "as_of": "2026-09-04T14:32:11Z",
  "next_since": "dHwyMDI2LTA5LTA0VDE0OjMyOjAxWg",
  "has_more": false,
  "recheck_after": "2026-09-04T14:47:00Z",
  "ttl_sec": 60,
  "items": [ { "id": 17, "sku": "ABC-1", "…": "карточка целиком",
               "stock": {"state": "many", "available_milli": 12000} } ]
}
```

- Курсор — **непрозрачная строка**: сайт хранит и возвращает её не читая.
  Внутри два вида — полная выгрузка по номеру записи и «после метки времени»;
  оба листаются `limit`/`has_more`, `next_since` берётся из каждого ответа.
- Товар попадает в ленту, если после курсора правилась его карточка, было
  движение по складу ключа или менялся открытый заказ с этим товаром. У каждой
  карточки — поле `stock` в том же виде, что у `/stock`, чтобы не ходить дважды.
- `next_since` **отстаёт от `as_of` на десять секунд**, и это защита, а не
  ошибка: транзакция, начатая до `as_of` и зафиксированная после, иначе
  пропала бы навсегда. Перечитать карточку второй раз бесплатно — она полная.
- Истёкшая бронь меняет наличие, не меняя ни одной строки, — этого лента не
  увидит; на то и `recheck_after`. `422 bad_cursor` — строка не наша.

Порядок работы сайта: холодный старт — `GET /site/changes?limit=200` до
`has_more: false`, дальше раз в N секунд `?since=<курсор>` (тридцать секунд для
магазина разумно, меньше `ttl_sec` бессмысленно), перед корзиной —
`GET /site/stock?id=…`, при оформлении — `POST /site/orders`.

### Запись

| Метод | Путь | Область | Описание |
|---|---|---|---|
| POST | `/site/orders` | `orders.write` | Заказ со сроком брони. `201` — новый, `200` — повтор с тем же `site_ref` (тот же заказ, второй не заводится) |
| GET | `/site/orders/{site_ref}` | `orders.read` | Свой заказ по чужому номеру; чужой или несуществующий — `404 order_not_found` |
| POST | `/site/orders/{site_ref}/cancel` | `orders.write` | Снять бронь: заказ уходит в `cancelled`, товар свободен. После накладной — `409 order_already_fulfilled` |
| POST | `/site/customers` | `customers.write` | Завести карточку клиента или узнать свою; ответ всегда одной формы, `202` |
| POST | `/site/leads` | `leads.write` | Та же заявка, что `/public/leads`, только по ключу сайта; `202 {"status": "accepted"}` |

**Заказ** (`POST /site/orders`):

```json
{
  "site_ref": "web-2026-000418",
  "reserve_minutes": 120,
  "comment": "Самовывоз после 18:00",
  "customer": {"name": "Anna Petrenko", "email": "anna@example.com", "phone": "+380671234567"},
  "items": [
    {"id": 17, "quantity": "2"},
    {"sku": "ABC-2", "quantity": "1.5"}
  ]
}
```

Ответ:

```json
{
  "site_ref": "web-2026-000418",
  "number": "2026-000123",
  "status": "issued",
  "reserved_until": "2026-09-04T16:32:00Z",
  "reserve_expired": false,
  "total_minor": 33750,
  "currency": "USD",
  "waybills": [],
  "lines": [{"id": 17, "sku": "ABC-1", "name": "Matrix 15.6\" FHD", "quantity_milli": 2000, "price_minor": 12500}],
  "created_at": "2026-09-04T14:32:00Z", "updated_at": "2026-09-04T14:32:00Z"
}
```

- `site_ref` обязателен и уникален по всей системе (до 64 знаков, регистр
  различается). **Повтор с тем же `site_ref` возвращает тот же заказ и `200`** —
  сайт повторяет запрос на таймауте, и без этого одна оплата превращалась бы в
  два заказа. Гарантию даёт уникальный индекс, а не проверка в коде.
- `number` — наш сквозной номер бланка; он же на бумаге и в адресе публичной
  страницы состояния `/d/{number}` — его стоит показать покупателю.
- Бронь — это заказ со сроком. `reserve_minutes` — просьба сайта, потолок —
  у ключа (`max_reserve_minutes`, `422 reserve_too_long`); не прислали — берётся
  потолок. Срок истекает **лениво**: истёкшая бронь перестаёт держать товар в
  свою секунду, заказ при этом остаётся открытым и виден менеджеру с отметкой
  «бронь истекла» (в CRM — `GET /orders?reserve=expired`).
- Покупатель ищется по `customer.email` / `phone` тем же способом, что у формы
  заявок; не нашёлся — заказ без карточки, имя на бумаге. Карточку сайт заводит
  отдельно через `/site/customers`.
- Строка адресуется `id` **или** `sku`. Товар должен быть опубликован на складе
  ключа; иначе `422 product_unknown` с перечнем ненайденных в `details.items` —
  молча выбросить строку из заказа нельзя.
- Нехватка считается **по товару, а не по строке** (две строки по 5 при
  остатке 6 не проходят обе) под замком на товар, и отвечает числами:

```json
{"error": {
  "code": "not_enough_stock",
  "message": "Not enough stock for 1 item(s)",
  "details": {"items": [{"id": 17, "sku": "ABC-1", "requested_milli": 5000, "available_milli": 3000}]}}}
```

Отказы заказа: `422 site_ref_required` / `site_ref_too_long` / `lines_required` /
`product_unknown` / `price_not_set` / `bad_line_quantity` / `quantity_too_precise` /
`reserve_too_long`, `409 not_enough_stock` (наружу 409, а не 422, как внутри CRM:
дело в состоянии склада, а не в форме запроса), `409 warehouse_not_serving`.

**Отмена** работает, пока заказ открыт и по нему нет накладной. Выписана
накладная — товар физически ушёл, дальше это возврат, и он оформляется в CRM
человеком. Повторная отмена отменённого безвредна и отвечает `200`.

**Регистрация клиента** (`POST /site/customers`):

```json
{"name": "Anna Petrenko", "email": "anna@example.com", "phone": "+380671234567",
 "consent": true, "consent_ref": "terms-2026-05"}
```

Ответ всегда `202 {"customer_ref": "9f2c…", "status": "accepted"}` — и когда
карточку завели, и когда узнали свою: разница ответов была бы оракулом «ваш ли
клиент этот человек». `customer_ref` — производная от пары (ключ, клиент), а не
наш номер: два ключа получают разные ссылки на одного человека, отозванный ключ
обнуляет свои. Известную карточку регистрация **не правит** — новые сведения
ложатся текстом в ленту клиента, переносит их человек. Согласие обязательно
(`422 consent_required`) и хранится в журнале действий (`customer.registered`,
источник `site_api`, `source_ref` = `consent_ref`). Потолок новых карточек — 200
в час на ключ, `429 customers_flooded`. `422 name_required` / `contact_required`.

### Снимок товара

| Метод | Путь | Чем закрыт | Описание |
|---|---|---|---|
| GET | `/media/product/{filename}` | uid 128 бит; блок `warehouse` включён; товар опубликован | `<uid>.webp` и `<uid>-thumb.webp` из карточки; `Cache-Control: public, max-age=31536000, immutable`, `X-Content-Type-Options: nosniff`. Неопубликованный товар — `404`, а не «есть, но не покажем» |

### Ключи API сайта (настройки)

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/settings/api-keys` | 🔑 `settings.manage` | Все ключи, включая отозванные и истёкшие; `alive` — сколько живых («наружу открыто: N ключей» на экране), словари `scopes`, `stock_modes`, имя заголовка |
| POST | `/settings/api-keys` | 🔑 `settings.manage` | Выдать: `name`, `scopes[]`, `warehouse_id` (обязателен при `stock.read`, склад типа `shop`), `days` (365; 0 — бессрочный), `stock_mode`, `few_threshold_milli`, `rate_per_min`, `max_reserve_minutes`, `ttl_sec`. Ответ `201` содержит `key` — **один раз**. `422 unknown_scope` / `scope_required` / `warehouse_required` / `warehouse_not_shop` / `unknown_stock_mode` |
| PATCH | `/settings/api-keys/{key_id}` | 🔑 `settings.manage` | Имя, режим наличия, порог и потолки. Области и склад не правятся — на них выпускают новый ключ |
| POST | `/settings/api-keys/{key_id}/revoke` | 🔑 `settings.manage` | Отзыв отметкой; строка остаётся |
| GET | `/settings/api-keys/{key_id}/stats` | 🔑 `settings.manage` | Сводка обращений за 30 дней: `today`, `week`, `month`, `rejected_month`, `avg_per_day`, `peak_hour`, `rate_per_min`, `by_category[]`, `by_day[30]`, `by_hour[24]`. В списке ключей у строки — `hits_30d`. Устройство — docs/16 §18. `404 api_key_not_found` |
| POST | `/settings/api-keys/{key_id}/rotate` | 🔑 `settings.manage` | Новый ключ с теми же полями (`201`, `key` один раз); старый живёт ещё `grace_hours` (24). `409 api_key_revoked` — отозванный не ротируется |

Те же действия из консоли: `./opencrm.sh apikey list|new|show|revoke|rotate`
(флаги `--name --scopes --warehouse --days --stock --few --rate --reserve-max --ttl --grace`).

### Что изменилось в соседних ручках

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/warehouses/{warehouse_id}/site` | 🔑 `warehouse.view` | Сколько карточек этого склада на сайте и сколько без цены: `{published, without_price}`. Отвечает и на «что случится, если сменить тип» ДО нажатия, и на строку экрана «На сайте: 132 позиции, 4 без цены» |

- `POST`/`PATCH /warehouses` принимают `kind` из `stock` / `shop` / `transit` /
  `defect` (по умолчанию `stock`; смена типа — в журнал `warehouse.kind_changed`,
  уход из `shop` экран подтверждает с числом карточек); `warehouse_out` отдаёт `kind`.
- Товар: `site_description` в `POST`/`PATCH /warehouse/products` и в ответе.
- Заказы: `GET /orders?reserve=expired` — открытые заказы с истёкшей бронью; у
  каждого заказа `site_ref`, `reserved_until`, `reserve_expired`.

### Проверка руками

```sh
KEY=…   # то, что напечатала выдача

curl -sS https://crm.example.com/api/v1/site/catalog?per_page=3 -H "X-OpenCRM-Api-Key: $KEY"
curl -sS "https://crm.example.com/api/v1/site/stock?sku=ABC-1" -H "X-OpenCRM-Api-Key: $KEY"
curl -sS -X POST https://crm.example.com/api/v1/site/orders \
     -H "X-OpenCRM-Api-Key: $KEY" -H "Content-Type: application/json" \
     -d '{"site_ref":"probe-1","reserve_minutes":10,"items":[{"sku":"ABC-1","quantity":"1"}]}'
```

Третью команду выполнить **дважды**: второй ответ обязан быть тем же заказом с
кодом `200` — это и есть проверка идемпотентности. Потом отменить пробный заказ
(`POST /site/orders/probe-1/cancel`), иначе он держит единицу товара десять минут.

### Живые обновления

| Метод | Путь | Права | Описание |
|---|---|---|---|
| GET | `/live` | 👤 | Поток намёков «перечитай» (SSE) для вкладки сотрудника. Первое сообщение — `resync` (и когда догнать нечем), дальше `change` с `id:` номера потока (он же `Last-Event-ID` при переподключении), `mode: off` при выключенной настройке `realtime_enabled` (`reason: disabled`) и лежащем Redis (`bus_unavailable`). Права и живость сессии — на каждое сообщение; соединение живёт не дольше пяти минут. Разбор — [12-zhivye-obnovleniya.md](../ustroystvo/12-zhivye-obnovleniya.md) |

Намёк — `{"topic", "action", "id", "scope_key", "actor_id", "module"}`, без
данных: клиент перечитывает обычным `GET`, и права проверяет он же. Выключатель
— «Настройки → Обслуживание → Живые обновления», значение едет в
`GET /workspace` полем `realtime_enabled`.
## Служебные ручки без сессии

Пять адресов внутри `/api/v1`, у которых нет ни сессии, ни права. Каждый закрыт
чем-то другим, и чем именно — важнее самого списка.

| Метод | Путь | Чем закрыт | Описание |
|---|---|---|---|
| GET | `/metrics` | границей сети | Метрики приложения текстом в формате Prometheus. Блоком `monitoring` закрыт тоже — но снаружи его не пускает nginx: `location = /api/v1/metrics { deny all; }` и он же префиксом |
| POST | `/alerts/webhook` | проверкой, что запрос не шёл через nginx | Доставка от Alertmanager: тревоги, зажёгшиеся или погасшие. Отказы: `403 alerts_external` (пришёл снаружи, через nginx), `403 alerts_bad_key` (задан `OPENCRM_ALERTS_SECRET`, а заголовок `X-OpenCRM-Alerts-Key` не сошёлся), `429 alerts_flooded` (100 в минуту). Неразбор тела — `200`, преходящая беда — `503`: разбор ниже |
| GET | `/alerts/ready` | тем же | Готова ли CRM принимать тревоги. Ответ «нет» — рабочее состояние, а не отказ |
| GET | `/arcade/leaderboard` | ничем — данных фирмы не касается | Таблица результатов змейки со страницы обслуживания |
| POST | `/arcade/scores` | тем же | Записать счёт |

Метрики закрыты в nginx **дважды**: точным совпадением и префиксом с `^~`.
Одного точного мало — проверено запросами по живому nginx:
`//api/v1/metrics`, `/api/v1/./metrics`, `/api/v1/foo/../metrics` и
`/api/v1%2fmetrics` в запрет упираются, а `/api/v1/metrics/` со слэшем на конце
точному совпадению не равен и уходит в приложение. Сегодня это безопасно по
случайности — Starlette отвечает перенаправлением обратно на запрет, — но
`redirect_slashes` это настройка фреймворка, а не наше решение, и молчаливо
поменять её вправе любое обновление зависимостей. Граница сети не должна зависеть
от такой мелочи.

Приём тревог отличается от вебхука телеграма ответами: там повтор вредил (клиент
получил бы своё сообщение дважды), здесь повтор — **способ доставки** тревоги,
которая не ушла, пока телеграм моргал. Поэтому преходящий отказ отдаётся `503`, а
неразбор (не тот JSON, чужое тело) — двухсоткой: повторять то, чего мы не
понимаем, незачем. И это единственная в системе ручка, где **недоступный Redis не
закрывает приём**: ограничитель здесь стоит против потока, а не против подбора, а
цена отказа наивысшая — тревоги не доставлялись бы вообще, включая правило
`RedisDown`, то есть служба молчала бы именно о той поломке, которая её и
заткнула.

Аркада публична целиком: играют посетители сайта, пока он обновляется, и никакого
аккаунта у них нет. Ни одного действия над данными фирмы здесь нет — только своя
таблица результатов.

## Публичные маршруты (витрина, вне /api)

| Метод | Путь | Описание |
|---|---|---|
| GET | `/b/{token}` | Страница витрины. SSR: OG-теги, первые работы, blurhash. Если PIN — форма PIN. Если недоступна — страница «доступ закрыт» (единый вид для отозванной/истёкшей/неопубликованной/несуществующей — не раскрываем причину) |
| POST | `/b/{token}/pin` | Проверка PIN. Успех → подписанная cookie доступа на этот токен (живёт сессию браузера). Перебор ограничен (см. [07-bezopasnost.md](../ekspluatatsiya/07-bezopasnost.md)) |
| GET | `/b/{token}/data` | JSON с работами для гидрации/дозагрузки (те же проверки доступа) |
| GET | `/media/{work_uid}/{filename}` | Файлы работ. **Отдаёт приложение**, nginx только проксирует |
| GET | `/branding/{filename}` | Логотипы и картинка для соцсетей. Их nginx раздаёт с диска (`/srv/storage/branding/`) |
| GET | `/avatars/{filename}` | Аватары сотрудников, так же с диска |
| GET | `/d/{number}` | Состояние заказа по QR с квитанции, без входа в систему |
| GET, HEAD | `/healthz` | Живо ли приложение: `status`, `schema`, `redis`, `maintenance`. На него завязаны проверка здоровья docker и откат обновления |

**Медиа раздаёт приложение, и это правка по безопасности, а не недосмотр.**
Раньше файлы отдавал nginx прямо с диска, и они не закрывались ничем. Проверено
прогоном: клиент один раз открыл витрину и запомнил адреса картинок; после смены
PIN, отзыва ссылки, истечения срока, снятия доски с публикации и даже удаления
ссылки страница отвечала 401/404, а файлы — 200, причём анониму и без PIN.
Витрина закрыта, работы открыты — то есть отзыв ссылки не отзывал ничего.
Сегодня обработчик проверяет блок `boards`, собирает путь через
`media_service.public_file` и спрашивает `share_service.media_is_public` (или
сессию сотрудника); отказ выглядит как «нет такого» — сказать «есть, но не
покажем» значило бы подтвердить, что работа существует. В nginx на этом месте
стоит `proxy_pass`, а не `alias`.

`X-Accel-Redirect` (приложение проверяет, байты отдаёт nginx) был бы быстрее и
**не сделан нарочно**: такой ответ живёт ровно до тех пор, пока внутренний адрес
в конфиге совпадает с заголовком в коде, а файлы nginx примонтированы из чекаута
и меняются в момент `git checkout` — то есть раньше, чем пересоберётся контейнер
приложения. В это окно новое приложение встречает старый конфиг, редирект уходит
в никуда, и на витринах у всех клиентов пропадают картинки. Приём стоит взять,
когда нагрузка потребует, но вместе с проверкой совпадения, а не вместо неё.

`/d/{number}` — **единственный публичный путь, отдающий данные заказа**, и держит
его ограничитель, а не неугадываемость номера. Номера сквозные, потому что тот же
номер напечатан на квитанции и зашит в QR; сделать его неугадываемым, не сменив
бумагу на руках у людей, невозможно. Отличить «нет такого» от «есть, но не
показываем» можно прямо по коду ответа, и обещать обратное нельзя. Значит вопрос
не в том, возможен ли перебор, а сколько он стоит: без ограничения — нисколько,
тысяча запросов подряд отдаёт весь список заказов мастерской. Ограничитель по
адресу (`DOCUMENT_STATUS_MAX_LOOKUPS` в `web/api/deps.py`) поднимает эту цену с
полуминуты до часов и оставляет живому человеку двадцать обращений за десять
минут; считаются **все** обращения, а не одни промахи — перебор с первого номера
идёт по существующим заказам, то есть по удачным ответам. Закрыть полностью можно
было бы ключом в ссылке, и это осознанно не сделано: ключ обесценит все уже
напечатанные квитанции, а решать, менять ли бумагу на руках у клиентов, не
программисту. Выключенный блок `documents` отвечает здесь так же, как
несуществующий номер.

## Пример: создание ссылки

```http
POST /api/v1/boards/12/shares
Content-Type: application/json

{ "expires_at": "2026-08-22T00:00:00Z", "pin": "4821" }
```

```json
{
  "id": 7,
  "board_id": 12,
  "url": "https://studio.example.com/b/xK9fQ2mNpL7vTzR3aW8bYc",
  "token": "xK9fQ2mNpL7vTzR3aW8bYc",
  "is_active": true,
  "expires_at": "2026-08-22T00:00:00Z",
  "has_pin": true,
  "created_by": 3,
  "created_at": "2026-07-22T12:00:00Z",
  "revoked_at": null,
  "views_count": 0,
  "unique_views_count": 0,
  "last_viewed_at": null
}
```

Три последних поля — статистика: она приходит вместе со ссылкой всюду, где
ссылку отдают списком или в карточке доски.
