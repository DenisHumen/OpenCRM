"""индексы под «отобрать и показать по порядку» + ширина ключа этапа в журнале

Revision ID: f9b41c7e2d08
Revises: c5e19a3d7b46

Индексы в проекте ставились по одному признаку: внешний ключ, дата, `deleted_at`.
Этого хватает, чтобы НАЙТИ строки, и не хватает, чтобы отдать их в нужном
порядке. Каждый список в CRM устроен одинаково — отобрать по признаку и показать
полсотни свежих сверху, — и на однопризнаковом индексе вторая половина работы
делается заново на каждый запрос: база достаёт все подходящие строки и строит из
них временное дерево сортировки. Пятьдесят строк на экране стоят разбора всей
выборки.

Замерено на базе с 30 000 заявок, 20 000 клиентов, 200 000 движений склада,
100 000 писем, 100 000 записей ленты, 50 000 звонков и 100 000 записей журнала
(после ANALYZE, медиана семи прогонов, SQLite 3.49):

    общая лента                      32.1 мс  ->   0.10 мс
    письма ящика                     63.7 мс  ->   0.04 мс
    счётчик непрочитанных писем      70.2 мс  ->   1.41 мс
    остатки склада, страница списка  23.0 мс  ->   0.42 мс
    остатки склада, весь справочник  1088  мс ->  18.3  мс

На перекошенных данных (клиент с 3000 записей в ленте, 3000 писем и 2000
звонков — то, как выглядит постоянный заказчик через пару лет, а не равномерное
распределение) пары «по кому + когда» дают ещё вчетверо-впятеро: лента клиента
0.41 -> 0.09 мс, письма клиента 0.63 -> 0.13 мс, звонки клиента 0.42 -> 0.14 мс.

`quantity_milli` в `ix_stock_moves_product_qty` — не для поиска. Остаток
считается как `SUM(quantity_milli) GROUP BY product_id`, и без количества в
индексе база шла по индексу товара, но за каждым числом лезла в таблицу: двести
тысяч случайных чтений. С ним запрос закрывается индексом целиком.

`is_read` первой колонкой — признак двузначный, и обычно такие в индекс не
ставят. Но «непрочитанные» — единственный фильтр на экране почты, а счётчик к
нему читал таблицу целиком.

---

Чего здесь НЕТ и почему.

**Списки заявок и клиентов и колонки канбана остались медленными** (12–17 мс на
странице списка, ~9 мс на колонку). Они сортируются после выборки по
`deleted_at IS NULL`, и напрашивающийся составной индекс `(deleted_at,
updated_at)` эту сортировку действительно убирает — список заявок 8.4 -> 0.13 мс.
Но он же ЛОМАЕТ всё остальное, что читает живые записи целиком: сводка по
клиентам за месяц 4.7 -> 49 мс, отчёт по источникам 34 -> 132 мс, выручка
16 -> 46 мс.

Причина не в форме индекса, а в статистике. `sqlite_stat1` хранит СРЕДНЕЕ число
строк на значение: удалённых пятьсот на тридцать тысяч, значений `deleted_at`
пятьсот с небольшим, среднее выходит 60 — при настоящих 29 500. Планировщик
считает `deleted_at IS NULL` очень избирательным условием и берёт такой индекс на
любой запрос про живые записи, включая те, где надо пройти всю выборку. А идёт он
по нему в порядке даты, то есть в таблицу лезет случайными чтениями вместо
последовательных. Гистограмму по значениям собирает STAT4, и в этой сборке SQLite
он не включён (`PRAGMA compile_options` без `ENABLE_STAT4`), так что `ANALYZE`
делу не помогает.

Проверены и отвергнуты по замерам: индекс без `deleted_at` (планировщик его
просто не берёт — узкий `ix_*_deleted_at` выигрывает конкурс), частичный
`WHERE deleted_at IS NULL` (то же самое), удаление узких индексов по `deleted_at`
(воронка отчёта 34 -> 392 мс: она ходит по этому индексу как по покрывающему).
Сумма времени по двадцати двум запросам: как есть 263 мс, лучший из вариантов
262 мс — то есть выигрыш в одном месте ровно равен проигрышу в другом.

Чинить это индексом нельзя, чинится оно в запросах — и это уже не миграция.

---

**Вторая правка: `deal_stage_changes.from_stage` и `to_stage` с 20 до 32.**

Та же ошибка, что чинила `b2c48f7a91d5`, и в соседней таблице — там её тогда не
заметили. Ключ этапа генерируется `pipeline_service._free_key`: имя обрезается до
24 символов, при совпадении добавляется суффикс — итого до 27. Хранится он в трёх
местах: `pipeline_stages.key` (32), `deals.stage` (32 после b2c48f7a91d5) и здесь,
где так и осталось 20.

SQLite длину VARCHAR не проверяет, поэтому весь набор тестов молчит. На MySQL,
куда проект собирается (`scripts/migrate_to_mysql.py`), в строгом режиме запись в
журнал этапов откажет, в нестрогом — обрежет. Второе хуже: отчёт по воронке
склеивает `deal_stage_changes.to_stage` с `pipeline_stages.key`
(`database/repositories/reports.py`), обрезанный ключ ни с чем не склеится, и этап
с длинным названием покажет ноль входов. Ошибки не будет — будет неверное число.
"""

from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op

revision: str = "f9b41c7e2d08"
down_revision: Union[str, None] = "c5e19a3d7b46"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None


#: (имя, таблица, колонки). Список один на накат и откат: разъехавшись, они
#: оставили бы после `downgrade` половину индексов, и следующий `upgrade` упал бы
#: на «index already exists» — на базе, которую как раз откатывали.
INDEXES: tuple[tuple[str, str, list[str]], ...] = (
    # Лента сортируется по времени СОБЫТИЯ, а не записи: звонок вчерашний,
    # занесли сегодня. Индекса на `happened_at` не было ни одного.
    ("ix_client_notes_happened_at", "client_notes", ["happened_at"]),
    ("ix_client_notes_client_happened", "client_notes", ["client_id", "happened_at"]),
    ("ix_client_notes_deal_happened", "client_notes", ["deal_id", "happened_at"]),
    # Почта: письма ящика, письма клиента, фильтр «непрочитанные».
    ("ix_mail_messages_account_sent", "mail_messages", ["account_id", "sent_at"]),
    ("ix_mail_messages_client_sent", "mail_messages", ["client_id", "sent_at"]),
    ("ix_mail_messages_unread_sent", "mail_messages", ["is_read", "sent_at"]),
    # Склад: остаток и история движений товара.
    ("ix_stock_moves_product_qty", "stock_moves", ["product_id", "quantity_milli"]),
    ("ix_stock_moves_product_happened", "stock_moves", ["product_id", "happened_at"]),
    # История звонков в карточке клиента.
    ("ix_phone_calls_client_started", "phone_calls", ["client_id", "started_at"]),
)


#: Колонки журнала этапов и их ширина «до» / «после».
STAGE_KEY_COLUMNS = ("from_stage", "to_stage")


def upgrade() -> None:
    for name, table, columns in INDEXES:
        op.create_index(name, table, columns)

    # batch_alter_table: в SQLite смена типа — это пересоздание таблицы, и
    # alembic делает его за нас, сохраняя внешние ключи на `deals` и `users`.
    with op.batch_alter_table("deal_stage_changes") as batch:
        for column in STAGE_KEY_COLUMNS:
            batch.alter_column(
                column,
                existing_type=sa.VARCHAR(length=20),
                type_=sa.String(length=32),
                existing_nullable=False,
            )


def downgrade() -> None:
    # Обратно к 20 символам. Ключи длиннее при этом обрежутся — откат безопасен
    # только пока таких ключей не завели, что для отката и верно.
    with op.batch_alter_table("deal_stage_changes") as batch:
        for column in STAGE_KEY_COLUMNS:
            batch.alter_column(
                column,
                existing_type=sa.String(length=32),
                type_=sa.VARCHAR(length=20),
                existing_nullable=False,
            )

    for name, table, _columns in reversed(INDEXES):
        op.drop_index(name, table_name=table)
