"""четыре пары под то, что открывают каждый день

Revision ID: b3f18d5a2e47
Revises: a1c7f4e29d63

Скорость на боевом объёме не мерил никто: набор гоняется на десятках строк, где
быстро всё. Стенд собран настоящий — MySQL 8.0, 200 000 клиентов, 400 000
заявок, 2 700 000 движений склада, 600 000 смен этапов, 500 000 записей журнала,
300 000 записей ленты, 200 000 финансовых операций, 150 000 бланков; имена,
фирмы и товары распределены по Ципфу, заявки у клиентов не поровну. Замеры — со
стороны сервера (время в драйвере), медиана семи прогонов после прогрева, и
«было» со «стало» сняты подряд на одном и том же прогретом буферном пуле:
единственная разница между колонками — эти четыре индекса.

Что нашлось и чем закрыто:

    список клиентов, стр. 1           208 мс ->  29 мс
    список клиентов, стр. 50          259 мс ->  30 мс
    список заявок, стр. 1             442 мс ->  49 мс
    список заявок, стр. 50            651 мс ->  51 мс
    доска (канбан)                   1243 мс -> 172 мс
    сводка                            890 мс -> 513 мс
    клиенты: поиск частой фамилии     477 мс -> 214 мс
    клиенты: фильтр по метке          380 мс -> 173 мс
    заявки: поиск частого слова      1284 мс -> 714 мс
    палитра, слово с совпадениями     930 мс -> 426 мс

Причина у всех одна и та же, и она не в отборе, а в ПОРЯДКЕ. Каждый список в
CRM устроен одинаково — «отбери живые и покажи полсотни свежих сверху», — а
узкий индекс по `deleted_at` умеет только первую половину. Вторую база делала
заново на каждый показ: доставала все 196 000 живых карточек и строила из них
временное дерево сортировки ради пятидесяти строк на экране (`Using filesort` в
EXPLAIN). Пятидесятая страница стоила дороже первой, потому что дерево то же, а
выбросить надо больше.

---

**Почему пара «живые + свежие» появилась только сейчас.**

В `f9b41c7e2d08` её рассматривали и отвергли — но замеры там были на SQLite, где
`sqlite_stat1` хранит СРЕДНЕЕ число строк на значение, а гистограммы (STAT4) в
той сборке нет. Планировщик считал `deleted_at IS NULL` очень избирательным
условием и тащил такой индекс в каждый запрос про живые записи, включая те, где
надо пройти всю выборку: сводка 4.7 -> 49 мс, отчёт по источникам 34 -> 132 мс.

База с тех пор одна — MySQL, и вывод пришлось проверять заново. Проверено: на
живых 400 000 заявках сводка и отчёты своих планов от пары `(deleted_at,
updated_at)` не меняют вовсе (воронка 34.0 -> 32.8 мс, выручка 23.0 -> 21.8 мс,
источники 29.1 -> 29.7 мс — то есть в пределах разброса). Вывод «этого индекса
не будет» был верен для того движка и неверен для этого.

Хвост `id` явно не дописан нигде, кроме канбанной пары: во вторичный индекс
InnoDB кладёт первичный ключ сам, а сортировка в проекте всегда «поле, потом id»
(`database/query._with_tiebreak`) — то есть совпадает с тем, что индекс и так
даёт. Канбанная пара — исключение, и почему, разобрано следующим абзацем.

---

**`id DESC` в индексе колонки канбана — не украшение.**

`by_stage` сортирует `sort_order ASC, id DESC`, то есть В РАЗНЫЕ СТОРОНЫ. Индекс
целиком по возрастанию такой порядок не даёт, и MySQL, взяв его, всё равно
сортировал бы выборку. Проверено обоими способами на стенде:

    (deleted_at, stage, sort_order)            доска 1230 мс, план `index_merge` + filesort
    (deleted_at, stage, sort_order, id DESC)   доска  180 мс, план `range`, сортировки нет

Одна колонка при этом стоит 185 -> 3.5 мс; колонок пять, отсюда и вся доска.
Убывающие колонки в индексах MySQL умеет с 8.0, а другой базы у продукта нет.

---

**`amount` третьей колонкой — чтобы не ходить в таблицу за каждым числом.**

`SUM(amount) GROUP BY stage` (итоги над колонками доски) и `COUNT(*) GROUP BY
stage` (плитки сводки) читают ВСЮ живую выборку — 394 000 строк. С `amount` в
индексе запрос закрывается индексом целиком:

    SUM(amount) GROUP BY stage     357 мс ->  72 мс
    COUNT(*)    GROUP BY stage     331 мс ->  58 мс

**Эта пара обязана стоять вместе с канбанной, а не вместо неё.** Оставь одну
канбанную — планировщик возьмёт её же и на суммы, а `amount` в ней нет, и он
полезет в таблицу за каждым числом: 496 мс, то есть ХУЖЕ, чем было без индексов
вовсе. Замерено; снимать их поодиночке нельзя.

---

**Чем за это платим на записи.**

Индекс — это плата за КАЖДУЮ вставку и правку, и назвать её надо числом.
Замерено на той же таблице в 400 000 строк, пачками по 200 заявок (медиана пяти
пачек, первая — прогрев):

    вставка 200 заявок      7–8 мс  ->  8–12 мс
    правка  200 заявок      3   мс  ->  6–8  мс

То есть доли миллисекунды на строку: заявку заводит человек руками, и таких
записей в CRM единицы в минуту. Разменивать их на девятикратные списки — обмен
очевидный. Плата была бы неприемлемой на `stock_moves`, которые сыплются
пачками при каждой отгрузке, — поэтому там ничего и не добавлено (см. ниже).

---

**Чего здесь НЕТ и почему.**

*Индексов под поиск подстрокой.* `search_text LIKE '%слово%'` не берёт индекс ни
один — ведущий `%` закрывает эту дорогу, а FTS и FULLTEXT закрыты устройством
проекта (разбор — `docs/03-database.md`). Поиск остаётся полным проходом, и это
осознанно.

Более того, у пары `(deleted_at, updated_at)` есть ЦЕНА, и она ровно здесь.
Поиск, не нашедший ничего, стал медленнее:

    палитра, слово без совпадений      996 мс -> 1292 мс
    список заявок, такое же слово     1272 мс -> 1573 мс

Причина понятна: имея индекс с готовым порядком, MySQL идёт по нему и дёргает
строки по первичному ключу вразнобой, надеясь набрать полсотни совпадений
раньше конца таблицы; при нуле совпадений он доходит до конца, но случайными
чтениями вместо последовательных. На словах, которые находятся, пара наоборот
выигрывает вдвое (палитра 930 -> 426 мс, список заявок 1284 -> 714 мс), потому
что первые полсотни попадаются рано.

Обмен принят сознательно: списки открывают десятки раз в день и они стали
быстрее в девять-двенадцать раз, а поиск по слову без совпадений — редкость, и
он был медленным и остаётся медленным (полтора-два деления одной и той же
секунды). Ускорить его индексом нельзя в принципе.

*Индекса под счёт для страницы.* `page_of` вторым запросом считает точное
«найдено N»: на списке клиентов это 25 мс, на журнале 53 мс, на движениях склада
208 мс. Убрать его — значит убрать номер последней страницы, а это изменение
поведения, а не ускорение; там, где точное число не показывают, в проекте уже
стоит `page_without_total` (палитра). Оставлено как есть и названо.

*Индекса `(product_id, warehouse_id, quantity_milli)` под раскладку остатка по
местам.* EXPLAIN показывает `Using temporary`, и соблазн велик — но замер
говорит, что вся страница склада с остатками занимает 23 мс, из них раскладка
40 мс на прогреве и единицы после. Индекс — это плата за КАЖДУЮ запись, а
`stock_moves` и так несёт двенадцать индексов и пополняется чаще всех. Не
трогаем.

*Индекса `(deleted_at, name)` у товаров.* План `type: ALL` с сортировкой
выглядит плохо, но товаров пять тысяч, и страница отдаётся за 5 мс. Оптимизация
того, что и так быстро, — чистый убыток на записи.
"""

from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op

revision: str = "b3f18d5a2e47"
down_revision: Union[str, None] = "a1c7f4e29d63"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None


#: (имя, таблица, колонки). Список один на накат и откат: разъехавшись, они
#: оставили бы после `downgrade` половину индексов, и следующий `upgrade` упал бы
#: на «index already exists» — на базе, которую как раз откатывали.
#:
#: Колонки — выражениями, а не строками: у канбанной пары хвост идёт по
#: убыванию, и строкой это не записать.
INDEXES: tuple[tuple[str, str, list], ...] = (
    ("ix_clients_alive_updated", "clients", ["deleted_at", "updated_at"]),
    ("ix_deals_alive_updated", "deals", ["deleted_at", "updated_at"]),
    (
        "ix_deals_alive_stage_sort",
        "deals",
        ["deleted_at", "stage", "sort_order", sa.text("id DESC")],
    ),
    ("ix_deals_alive_stage_amount", "deals", ["deleted_at", "stage", "amount"]),
)


def upgrade() -> None:
    # Индексы — последними и по уже заполненным данным: ни таблиц, ни колонок
    # эта миграция не заводит, поэтому строить их можно сразу.
    for name, table, columns in INDEXES:
        op.create_index(name, table, columns)


def downgrade() -> None:
    # Снимаются в обратном порядке — и снимаются здесь ЗАКОННО: правило «индексы
    # перед `drop_table` не трогать» (см. откат `e4451c527c34`) про другое —
    # там таблица уносит их сама, а внешний ключ держится как раз на индексе.
    # Здесь таблицы остаются, а снимаемые индексы ни одного ключа не
    # обслуживают: у всех четырёх первая колонка — `deleted_at`, а не ссылка.
    for name, table, _columns in reversed(INDEXES):
        op.drop_index(name, table_name=table)
