"""индекс под счёт категорий в списках бумаг

Revision ID: a3f81c62d947
Revises: e2b64f1a7c85

**Индекс по `kind` уже отвергался замером, и это не отмена того решения, а
ответ на другой вопрос.** Прежний замер (`b8d41f7a2c95`) мерил СТРАНИЦУ списка:
`WHERE kind IN (…) ORDER BY created_at DESC LIMIT 50`. На ней база идёт по
времени и останавливается на пятидесятой подходящей строке, поэтому выигрыш
оказался в пределах шума, а при доле накладных в 1 % индекс даже проигрывал.

Потом на те же два экрана добавились СЧЁТЧИКИ КАТЕГОРИЙ — «сколько квитанций,
сколько актов, сколько накладных» над свёрнутыми группами. Они на пятидесятой
строке остановиться не могут по устройству: это `GROUP BY` по всей выборке, и
зовутся они на каждый заход в список. Их не мерил никто.

Замер: MySQL 8.0.46, 200 000 бумаг, 226 595 строк перечня, 2000 товаров, доли
видов как на живой базе мастерской (квитанций 55 %, актов 20 %, заказов 20 %,
накладных 5 %). Медиана семи прогонов, три независимых захода после `ANALYZE`:

                                          без индекса   с индексом
    список бланков, страница 1                  63 мс        38 мс
    список накладных, страница 1                47 мс         9 мс
    список заказов, страница 1                  48 мс        18 мс
    счёт по видам (чипы над списком)            66 мс        26 мс
    счёт по состояниям (категории заказов)     195 мс        23 мс
    сортировка по состоянию                    140 мс       108 мс
    страница 20                                 53 мс        41 мс
    резерв по всем товарам (`promised`)        495 мс       310 мс
    резерв по одному товару                    3.7 мс       3.8 мс

Один заход в список бумаг стоил около 310 мс на счёт и выборку — стал около 67.

**Соседи проверены отдельно, и это не формальность.** Индекс, начинающийся с
`kind`, — приманка для планов чужих запросов; в этом проекте такое уже было: у
заявок индекс, начинающийся со `stage`, увёл отчёт по воронке с 34 на 308 мс.
Здесь под ударом были сортировка по состоянию и `documents_repo.promised`
(там `kind` стоит в соединении от `document_lines`) — обоим стало лучше, а не
хуже.

**Первый прогон показал у резерва 1246 мс — и это была неправда.** Число
получено сразу после `CREATE INDEX`, на буферном пуле, забитом страницами
построения. Три повтора с `ANALYZE TABLE` дали ровные 310 мс. Замер на свежем
индексе врёт втрое, и по одному прогону такие решения принимать нельзя.

**Отвергнутые формы**, померенные тем же набором:

- `(kind, created_at)` — счёт по состояниям остаётся 206 мс: без `status` в
  индексе группировать нечем;
- `(status, kind)` — списки заметно хуже (накладные 27 мс против 9): отбор идёт
  по виду, и вид обязан стоять первым;
- `(kind, status)` — почти всё то же, но сортировка по состоянию 149 мс против
  108: без `created_at` порядок внутри группы строится заново.

Индекс создаётся ПОСЛЕДНИМ шагом и по уже заполненным данным — на населённой
таблице `CREATE INDEX` удлиняет окно `alembic upgrade head`, которое идёт в
`docker/entrypoint.sh` до старта приложения. Замерено там же: на 200 000 бумаг
построение занимает 5,6 с, то есть обновление удлиняется на эти секунды, и
заглушка обслуживания в это время уже показана.
"""

from alembic import op

revision = "a3f81c62d947"
down_revision = "e2b64f1a7c85"
branch_labels = None
depends_on = None

IMYA = "ix_documents_kind_status_created"


def upgrade() -> None:
    op.create_index(IMYA, "documents", ["kind", "status", "created_at"])


def downgrade() -> None:
    # Внешних ключей на этих колонках нет, поэтому отказ 1553 («нельзя снять
    # индекс, которым проверяется ключ») здесь невозможен — снимаем прямо.
    op.drop_index(IMYA, table_name="documents")
